---
scope: Inflozo v1 — whole-system architecture spine governing all 15 epics (E0–E15), 130 FRs, 9 NFRs
purpose: build-substrate
altitude: initiative
updated: 2026-08-19T11:06
---

- (direction) Inputs read in the preamble's precedence order: verification files > research companions > normative companions > prd.md > addendum.md > spike-compiler. Mandated and not re-opened: single-source section runtime, 7.3 compiler mechanics, 7.4 theme structure, addendum AD1/AD2.
- (event) Re-ran spike-compiler on this machine (node 22.23.2, jsdom 30.0.1, handlebars 4.7.9, gscan 6.4.2): 16/16 renderer assertions pass. Spike is reproducible, not just claimed.
- (version) Vercel Functions verified against vercel.com/docs/functions/limitations (last_updated 2026-07-01): Pro max duration 800s GA / 1800s beta, default 300s; memory default 2GB/1vCPU, max 4GB/2vCPU; function bundle 250MB uncompressed; request+response body hard cap 4.5 MB.
- (constraint) EXECUTED, contradicts PRD 7.3: Handlebars 4.7.9 has no composable backslash escape. Only exactly one backslash escapes a mustache and it is consumed; 2+ backslashes evaluate LIVE. The PRD's stated remedy ('the backslash is escaped too') was executed and is FALSE - C:\\{{title}} is live at every backslash count. Fix: HTML numeric entity encoding of braces in user text; round-trips every case with zero parse failures.
- (constraint) EXECUTED: Handlebars fails to parse a mustache immediately followed by a closing brace - ':root{--a:{{@custom.x}}}' is a Parse error (greedy triple-stash). FR-Q5's inline token block in default.hbs emits exactly this shape. Emitter must always terminate a CSS declaration with ';' after a mustache; CI asserts zero '{{{' and zero '}}}' in compiled output.
- (constraint) 7.6 item 19 RESOLVED BY EXECUTION on gscan 6.4.2, both v5 and v6 specs: GS100 is an ERROR (not warning); it fires when ANY declared config.custom key is unreferenced (2-of-3 still fails); a reference inside a {{#if}} guard COUNTS; a reference from a partial COUNTS. FR-Q5's always-declare-always-reference design is confirmed sufficient - case 'all three referenced' scores 0/0 on both specs.
- (constraint) NEW gscan rule the PRD never names: GS051-CUSTOM-FONTS, warning on BOTH v5 and v6. Requires --gh-font-heading AND --gh-font-body to appear in the SAME .css or .hbs file (lookahead-pair regex, checks/051-custom-fonts-css-properties.js). Without it every Inflozo theme carries a standing warning and FR-J6's 0/0 target is unmeetable.
- (constraint) EXECUTED: card_assets exclude restores gscan's Koenig rules for the excluded card - excluding 'callout' produced 10 warnings demanding .kg-callout-* classes. FR-Q7's claim is confirmed and quantified: excluding a card obliges the theme to style every class gscan checks for it, or the 0/0 target breaks.
- (decision) MEASURED (not a platform default), 40-section stress fixture, 70 section renders across 7 templates, 173 files, 9.75 MB uncompressed / 9.66 MB zip, node 22.23.2 on 4 cores: render+serialize 770 ms (11.0 ms/section), assemble 14 ms, gscan 210 ms, zip 222 ms, TOTAL 1.21 s, peak RSS 289 MB. Theme scored gscan 0 errors / 0 warnings at that scale.
- (decision) Compile function sizing derived from the measurement with a 10x headroom factor over the measured 1.21 s (Vercel Fluid 1 vCPU is slower than the 4-core reference; Storage asset reads and font subsetting are I/O the fixture did not pay): maxDuration 300s (the Pro default, ~250x measured), memory 2048 MB (the Pro default, ~7x measured peak). No plan upgrade and no gscan-boundary split is needed - the 7.1 contingency does not trigger.
- (decision) Image renditions (400/800/1600 + original, FR-J3) are generated at UPLOAD time in the browser via the same canvas.toBlob pass FR-K2 already mandates, never at compile. Keeps 'sharp' and every native image binary out of the compile function, keeps compile CPU-bound-free of image work, and is why the measured compile stays inside the platform default.
- (constraint) Vercel's 4.5 MB request/response body cap is architectural: the 9.66 MB stress-fixture theme zip cannot cross a function boundary. Deploy streams the zip from the function to Ghost (server-to-server, uncapped); FR-J12 export hands the browser a short-lived Supabase Storage signed URL, never a function response.
- (decision) Full schema built from FR-A..FR-Q (not from 7.5's eight rows): 27 tables. New beyond both 7.5's sketch and its 'known to require' list: passkey_labels (FR-A3 rename; Supabase's Beta passkey API carries no user label), project_template_prefs (FR-D22 subject + FR-D16 member-state coverage - storing these on project_templates would falsely materialize an untouched template and break FR-D6's synthesis), deployed_template_names (7.4's collision kind 2 needs an append-only registry, not a warning), site_credentials, billing_events, checkout_consents (FR-L1's legal record), template_binding_checklist (FR-I6).
- (decision) EXECUTED against PostgreSQL 17.11 in Docker: SCHEMA.sql applies clean; the RLS proof (RLS-TEST.sql) passes every assertion - 0 tables without RLS, 34 policies over 26 tables, exactly 2 deliberate server-only tables. Cross-tenant insert/update blocked, is_admin self-grant blocked, deploys forge blocked, vote-as-another-user blocked, the 17-setting cap, the 6-digit-hex rule and the credit.* lock all hold. E1's exit criterion now has a runnable artifact rather than a sentence.
- (decision) Vault refs live in site_credentials, a table with RLS on and NO policy (deny-all to every client role). NFR-3's 'never sent to any client' becomes structural rather than a column-list discipline every future SELECT has to remember. Postgres RLS is row-level, so a policy on sites could not have achieved this.
- (decision) AD1's base_revision is compared against a single monotonic projects.revision, not a per-template revision. AD1 speaks of one 'local doc' and one comparison; per-template revisions would make the three-outcome hydrate table ambiguous when templates disagree.
- (version) Stack verified against the live npm registry 2026-08-19: next 16.3.1, @supabase/supabase-js 2.112.3 (PRD floor 2.105.0 met), gscan 6.4.2, jsdom 30.0.1, handlebars 4.7.9, stylelint-plugin-use-baseline 1.4.5, browserslist-config-baseline 0.5.0, eslint-plugin-compat 7.0.2, size-limit 13.0.3, resend 6.20.0, dodopayments 2.47.0, web-features 3.35.0. Ghost main (6.58.1-rc.0) bundles gscan 6.4.2 exactly, so FR-J6's pin at 6.4.2 satisfies 'at or above the newest bundled by any supported target' - read from TryGhost/Ghost ghost/core/package.json, not assumed.
- (constraint) Supabase Vault: pgsodium is pending deprecation but Vault's API surface (vault.secrets, decrypted_secrets) is explicitly unchanged and its internals migrate off pgsodium. FR-C3's posture holds; added as a standing watch item rather than a blocker.
- (decision) Paradigm: functional core / imperative shell, with the core (section-runtime, ghost-shim, theme-compiler) shared verbatim between browser and Node function. Chosen because 'the same code ran' is the only proof that makes 7.3's by-construction claim true; anything the core reaches for at runtime is a second source of truth. Enforced by a CI dependency-boundary check, not by review.
- (decision) 7.6's 21 items each get TWO owners: E0(b) executes all of them (8's stated scope, unchanged), and a standing owner epic carries the consequence, re-confirms before shipping and re-runs on version bumps. An item with only a spike owner is an item nobody re-checks in eighteen months. Register written to VERIFY-AT-BUILD.md, plus two new items this run found by execution (GS051, the Handlebars escape/brace pair).
- (event) Reviewer gate: lint_spine.py returns 9 low findings, all category 'placeholder' and all false positives - intentional path/attribute patterns, several ({control}, custom-{name}.hbs) quoted verbatim from the PRD's own vocabulary, so renaming them would diverge from the source. All 4 mermaid diagrams parse against the real mermaid parser (mermaid.parse in jsdom).
- (question) AD-18 decides that a Ghost Admin font choice WINS over the Style Pack pairing, because declaring --gh-font-* without honouring them would satisfy gscan while lying. Consequence for Umang: a canvas rendered in the pairing can differ from a live site whose owner set fonts in Ghost Admin, which is a new entry on 1.2's carve-out list. Flagged rather than absorbed.
- (constraint) Self-review caught a real leak in the first schema draft: FR-M3's unapproved-image gate lived only in the suggestions_public view while the base table stayed granted, so anon read image_path around it. Fixed with two mechanisms. Also recorded a Postgres trap proven by probe: a column-level REVOKE is a silent no-op while a table-level GRANT stands - the table grant must be revoked and the allowed column list granted back. Consequence: FR-M4 moderation is no longer an RLS policy but a service-role server route, which AD-8 already covers.
- (event) Counts derived rather than restated, from sections-inventory.md (normative for scope): 34 category declarations summing to 484 designs, 70 [Free] (68 first-two pairs + the A29 #7 and A31 #10 re-tiers). Reconciles exactly with FR-G1, FR-G2 and G5. No count in this spine is quoted independently of that derivation.
- (event) Reviewer gate run with three parallel lenses (version/reality-check, adversarial divergence, input reconciliation). All three returned substantive real findings; the gate changed the spine materially rather than confirming it. 26 ADs went to 35, the schema went from 27 to 29 tables plus column-level write surfaces and Storage policies, and AD-11's headroom moved from ~250x to ~140x.
- (constraint) Version lens caught this run committing the project's own cardinal sin: the Stack table claimed 'every version was read from the live npm registry' while four rows were recalled. Re-checked directly - TypeScript latest is 7.0.2 not 5.x, pnpm 11.22.0 not 10.x, Node 24.x is Vercel's default not 22, React 19.2.8 is a peerDependency not bundled. Corrected and recorded rather than quietly fixed.
- (constraint) Two platform facts were wrong and load-bearing, both re-confirmed against the vendor: (1) Next 16 deprecated middleware.ts in favour of proxy.ts on the Node runtime, and NFR-3's per-session CSP is a Node concern, so this is a correction not a rename; (2) a Vercel env var cannot change without a redeploy, which is exactly the capability FR-A2 asks a feature flag for - so flags became rows in feature_flags, read server-side per request.
- (decision) Six invariants added at the gate, each closing a pair of epics that obeyed every existing AD and could still build incompatibly: AD-30 dark overrides resolve to tokens and only the token block and base stylesheet mention a mode; AD-31 AD-8/AD-9 bind COLUMNS and RLS is not the mechanism (column GRANT plus triggers that hold against the service role too); AD-32 Storage is governed separately and a cascade deletes rows not bytes; AD-33 every cron has an owning epic; AD-34 the FR-J17 gate is a compile stage and is inside AD-11's budget; AD-35 a design file has one owning epic and a pre-gate pilot is provisional.
- (decision) Product surfaces the first draft had dropped and the reconciliation lens caught: the 28 non-placeable treatments and the card design module had no store at all (now project_treatments, and FR-Q9 requires it to survive when no host section exists), site-wide singletons had no home (template_key 'site'), Supabase Storage had zero policies across four buckets, and FR-K3's quota metered assets.bytes while AD-12 stores four objects per asset (now stored_bytes).
- (decision) MEASURED at the gate and folded into AD-11: the FR-J17 quality gate costs 697 ms on the stress fixture (79 hbs files, 569 elements, 4610 CSS declarations) and gscan runs TWO specs not one (v5 217 ms + v6 88 ms, both 0/0). Compile total is ~2.0 s, not 1.21 s. Headroom 140x rather than 250x - the number moved and the decision did not, which is the point of measuring.
- (event) Final gate state: SCHEMA.sql applies clean to PostgreSQL 17.11; RLS proof 23 PASS / 0 FAIL; 0 tables without RLS; 37 policies over 28 tables; 3 deliberate server-only tables. lint_spine.py returns 14 findings, all low, all 'placeholder/verify' false positives on intentional path and attribute patterns. All 4 mermaid diagrams parse. All 35 ADs carry Binds/Prevents/Rule.
