---
title: 'Inflozo — §7.6 verify-at-build register, with owners'
type: architecture-companion
status: final
created: '2026-08-19'
---

# Verify-at-build — every item, with an owning epic

§7.6 lists 21 external-world facts and instructs that each be re-confirmed before the epic
that depends on it. Until now none of them had an owner. This closes that.

**Two owners per item, deliberately.** **E0(b)** executes *every* item against a real Ghost
before the epics that rest on them — that is §8's stated scope and §4's binding build order,
and it does not change. The **standing owner** is the epic that carries the consequence: it
re-confirms before it ships, it owns the fallback if the item is refuted, and it re-runs the
check on any version bump. An item with only a spike owner is an item nobody re-checks in
eighteen months.

**Refutation is a scope change, raised before the dependent epic begins** — not a bug found
inside it. AD-23 governs the form: every result lands in `fixtures/` as a dated recording
with the command that produced it.

| # | Item | Executed by | Standing owner | Consequence if refuted |
| --- | --- | --- | --- | --- |
| 1 | ⛔ Ghost(Pro) 2026 plan lineup + the Starter custom-theme restriction | E0(b), then the §4 T4 gate | **E15** (launch gate) · consumed by E3 | Public launch and all Ghost(Pro)-aimed marketing stay blocked. Rides with item 2. **Read, not executed — 2026-09-03, ghost.org/pricing:** plans **Starter** $18 · **Publisher** $29 · **Business** $199 (USD/mo billed yearly) · Custom; Starter's row shows **No** for both *Marketplace themes* and *Custom themes*, footnote "Our Starter plan comes with a built-in default theme with plenty of layout and color settings. On higher plans, you can install and configure any custom theme you prefer". Supports B15's *Publisher* correction and S8a′/S11a's *no manual upload on Starter either* (F-072). A pricing page changes, so this stays ⛔ until the `hostSettings` capture below. |
| 2 | ⛔ The `hostSettings.limits` shape from `GET /admin/config/`, per host | E0(b), then the §4 T4 gate | **E15** (launch gate) · probe built in E3 | **PARTIALLY CLOSED 2026-08-20 (self-hosted half).** `hostSettings` is **ABSENT** from `GET /admin/config/` on self-hosted Ghost 5.130.6 and 6.58.0 — the key does not exist, so FR-C2's probe must treat **absence** as 'not Ghost(Pro)' rather than read a flag. `MEASUREMENTS.md` §15h. The Ghost(Pro) half still blocks launch and still waits on a Starter trial. **The shape, read in source 2026-09-03 (F-065):** `customThemes` is an **allowlist of theme names**, not a yes/no flag — `@tryghost/limit-service` `config.js:49` declares `customThemes: {}` as a FlagLimit/AllowlistLimit; Ghost 6.54.1 `api/endpoints/themes.js:53-54` runs `errorIfWouldGoOverLimit('customThemes', {value: themeName})` on upload and `services/themes/installer.js:25` treats `allowlist.length === 1` as "no other themes allowed". So FR-C2's probe must test **Inflozo's frozen theme name** (FR-J10) against the list; "present" ≠ "blocked". Still unobserved on a real Ghost(Pro) payload. |
| 3 | Theme-upload size limits per host, measured on T1–T3 | E0(b) | **E7** (FR-J3 budget) · E8 (FR-K5) | **CLOSED for the Ghost-CLI self-hosted shape, 2026-08-20.** The operative ceiling is **1 GB**, not 50 MB: Ghost-CLI writes `client_max_body_size 50m` on the plain-HTTP vhost and **`1g` on the SSL vhost**, and every real deploy is HTTPS. ~100x headroom over the 10.09 MB stress theme. Ghost(Pro) will differ, so E7/E8 remain standing owners. §15h. |
| 4 | Supabase passkey API status and `supabase-js` ≥ 2.105.0 behaviour | E0(b) | **E2** (FR-A2/A3) | **CLOSED BY EXECUTION 2026-08-20 — present, and DISABLED by default.** `supabase-js@2.112.3` resolves `auth-js@2.112.3`, which carries a full WebAuthn surface and lists `'webauthn'` as an MFA factor type. On the live project, `POST /auth/v1/factors` with `factor_type: webauthn` returns **422 `mfa_webauthn_enroll_not_enabled`**, while the same call with `totp` returns **200** — so the endpoint and session work and WebAuthn specifically is off. `GET /auth/v1/settings` reports **`passkeys_enabled: false`**, a project setting rather than a missing capability. The predicted consequence holds: the flag stays off, magic link stays primary. **New, and not anticipated here: there are TWO switches** — `feature_flags.passkeys` and Supabase's `passkeys_enabled` — and if they disagree the user is offered a flow the platform refuses. **E2 must read the platform setting, not only its own flag.** `MEASUREMENTS.md` §20. |
| 5 | Dodo fee schedule and MoR terms | E0(b) | **E12** (FR-L1/L2) · Appendix F | **PARTIALLY CLOSED 2026-08-20 — MoR behaviour observed, fee schedule still needed.** A 1500 USD product with a GB billing address was charged **1100 GBP + 220 GBP tax = 1319 GBP**: Dodo converted the currency and computed **20% UK VAT itself**, which is FR-L1's "taxes handled by Dodo" observed rather than assumed. **`settlement_amount` equals `total_amount` at the payment level, so the API does not expose Dodo's own fee**; the figures below came from the payment record. **CLOSED PROVISIONALLY 2026-08-20:** on a real £13.19 charge — £2.20 VAT, **£1.09 fee** — the fee is **9.92% of pre-tax revenue** and the merchant keeps **90.1%**. **Dodo is merchant of record, confirmed by the owner.** **Appendix F stands:** recomputed across every plausible rate/fixed split the blended net lands between **$12.16 and $12.62** against the assumed **$12.61**, moving cash break-even from 11–15 subscribers to **~11–16**. **Residual, and it is small:** one observation cannot separate the percentage from the fixed component, which is what makes the 60/40 mix matter — settle it with a second reading at the **annual** price when one occurs, rather than chasing it now. §19h, §19i. |
| 6 | The full Dodo event catalog, and whether Dodo sends the annual renewal notice as MoR | E0(b) | **E12** (owns email 6 and FR-P2's carve-out) | **CATALOGUE CLOSED BY EXECUTION 2026-08-20; the statutory half is still open.** Dodo emits **47** event types, not the seven FR-L2 names — read from the registered endpoint's own `filter_types`. All seven exist; the other forty are the problem. **⚠️ `subscription.cancelled` EXISTS**, and FR-L2 states in normative text that it does not — captured live, signature-verified, on a real cancellation. Also unhandled: `dunning.started`/`dunning.recovered` (a better signal for the 7-day grace than inferring from `payment.failed`), and five `subscription.*` events — `expired`, `paused`, `unpaused`, `plan_changed`, `update_payment_method` — of which **`paused` has no state in AD-28's resolver at all**. **Still open:** no event among the 47 resembles a pre-renewal reminder, which is suggestive but not proof, since an email need not have a webhook. Needs a written answer from Dodo support; if it is no, FR-P1 grows a sixth email. `MEASUREMENTS.md` §19. |
| 7 | gscan's current v6 warning set | E0(b), then **before E7 exits and at every category gate** | **E7** (FR-J6) · executed by E9/E10 per gate | A single unavoidable library-wide warning makes every category gate unmeetable. gscan stays pinned at 6.4.2 until a library release re-runs the whole inventory. |
| 8 | Whether Ghost release notes are usable as compatibility data | E0(b) | **E3** (FR-C5) | **CLOSED 2026-08-20 — usable as a signal, not as structured data.** Ghost's GitHub releases are dated and machine-readable but **thin and inconsistent**: bodies of 572–1,452 characters across the three sampled, theme mentions in two of three, and **no structured breaking-change flag anywhere**. FR-C5's per-release broadcast therefore degrades to the designed fallback — a manual owner-authored notice — and the `ghostCompat` mechanism is unaffected. |
| 9 | Whether the Owner's Staff Access Token lifts `GET /themes/` | E0(b) | **E7** (FR-J10, FR-J13) | **CLOSED BY EXECUTION 2026-08-20, on both majors.** The Owner's Staff Access Token **DOES** lift `GET /themes/` (200, themes listed). An integration token does not — **and the rejection differs per major: 501 on Ghost 5.130.6, 403 on Ghost 6.58.0**, which AD-24's mapping table must fold into one Inflozo code or the same cause reads as two different failures. Confirmation is worth more than this row assumed: FR-J10's uniqueness stops resting on `project_site_bindings` alone, and FR-J13's snapshot and FR-J16's drift check both gain a working primary path rather than only a fallback. §15h. |
| 10 | Snapshot re-upload acceptance, including a theme that fails Ghost's own gscan | E0(b) | **E7** (FR-J13) | **PARTIALLY CLOSED 2026-08-20 — rejection IS possible.** A theme carrying `GS005-TPL-ERR` was **rejected at upload with HTTP 422 `ThemeValidationError`** on both majors, naming the rule and the offending file. gscan errors therefore split into **fatal at upload** and **reported but activated** (§13a's `GS110` case), so FR-J6 cannot treat "Ghost accepts it anyway" as a general property. FR-J13's restore must assume rejection is possible and surface the 422 detail rather than retrying. **Still open:** whether a snapshot of a user's *original* theme — which may fail today's gscan for unrelated reasons — is accepted; that needs a real customer theme, not a synthetic one. §15j. |
| 11 | `partials/content-cta.hbs` as the paywall override point, per Ghost version | E0(b) | **E7** (emission) · consumed by E5 (FR-H6 editor) | **CLOSED BY EXECUTION 2026-08-20, and it carries a condition.** `partials/content-cta.hbs` **does** replace Ghost's default `gh-post-upgrade-cta` at the `{{content}}` cutoff on both majors — the expensive refutation did not happen. **But the override only takes effect if the theme ALSO references the partial explicitly from a template.** Reproduced 2/2 each way on both majors: the partial alone renders Ghost's core default; the partial plus `{{> "content-cta"}}` renders the theme's. Dropping the file in and expecting it to work is exactly what a theme author does, and it fails silently. **Library rule for E7 and A32: the compiler emits an explicit `{{> "content-cta"}}` reference wherever it emits the partial, asserted at compile rather than left as a convention.** §15b. |
| 12 | Ghost 6 API pagination — `?limit=all` removed, max 100 per page | E0(b) | **E4** (`{{#get}}` shim) · E5 (Count control) | **CLOSED BY EXECUTION 2026-08-20 — the majors differ.** Ghost 6.58.0 caps server-side at **100** and silently rewrites the request (`?limit=all` and `?limit=200` both echo `limit: 100`); Ghost 5.130.6 applies **no cap** (echoes `all` and `200` respectively). FR-H2's ceiling of 100 and its ban on `limit="all"` are both correct — and the Ghost 5 half is permissive, so an over-fetching design is invisible on 5.x. `GS090` catches it on the v6 spec only, re-confirming item 33. §15d. |
| 13 | The four docs-vs-code conflicts in `research-ghost-binding-contexts.md` — chiefly `@even`/`@odd` parity | E0(b) on a scratch Ghost | **E4** (shim + binding contexts) · **blocks E9** | **CLOSED BY EXECUTION 2026-08-20, identically on Ghost 5.130.6 and 6.58.0. E9 IS UNBLOCKED.** All four resolve as the companion predicted — the code wins over the docs every time. **(1) `@even`/`@odd` track the 1-based `@number`, so the FIRST ITEM IS ODD** — the docs' "true if the `@index` is even" is wrong, and a library authored against it stripes backwards in every alternating design. (2) `error-404.hbs` ran `{{statusCode}}`, `{{meta_title}}`, `{{t}}`, `{{img_url}}` and a live `{{#get}}` inside a real 404 — the docs' prohibition is a recommendation. (3) `@config.image_sizes.m.width` → `800`, so more than `posts_per_page` passes through. (4) `{{> "deep/nested/thing"}}` resolved three levels down. **Residual now closed (§15j):** a single `error.hbs` serves **every** status — `templates.js:23` resolves `['error-{code}', 'error-{n}xx', 'error']` — and it rendered with full helper support for both a **404** and a **400** render failure, on both majors. |
| 14a | Retitling a page with a template selected — does the template still apply? | E0(b) | **E7** (FR-I1) · E5 (FR-D6) | The load-bearing premise under FR-I1, FR-D6, FR-O1 and the whole A30 re-point. The *previous* version of this premise was found false mid-encode and forced a rebuild of five sections. |
| 14b | `{{#get "tiers"}}` on a site with a hidden tier — the direct research-vs-research conflict | E0(b) | **E4** (shim) · E5 (FR-H6) | **CLOSED 2026-08-20 by execution, identically on Ghost 5.130.6 and 6.58.0. Both companions were half right.** With one paid tier at `visibility: none`: an unfiltered `{{#get "tiers"}}` reports `tiers.length` = **2** but `{{#foreach}}` yields only the **1** public row; `filter="visibility:public"` gives 1 and 1 (consistent); `filter="visibility:none"` and `filter="type:paid"` each give length **1** and **zero** rows. The visibility filter applies to the serialized rows, after the count — so `length` is the pagination total and does not respect it. `research-ghost-binding-contexts.md` read the row behaviour, `research-ghost-membership-pages.md` read the count behaviour, and each generalised. FR-H6 is safe as written. **Residual hazard, now a library rule:** emptiness must be tested on a *filtered* get, never on `tiers.length` — otherwise a site whose only paid tier is hidden renders a pricing section with a heading and no cards. |
| 14c | The Template dropdown's label transform on a multi-word name | E0(b) | **E7** (FR-I3 naming step) | **CLOSED BY EXECUTION 2026-08-20, identical on both majors.** The transform is: strip `custom-`, split on hyphens, **Title-Case each word**, join with spaces. `custom-member-home.hbs` → **`Member Home`**; `custom-two-word.hbs` → **`Two Word`**. §7.4's live preview can now show the true label. **Newly recorded and not previously assumed:** Ghost offers custom templates for **`["page", "post"]`** — posts as well as pages, where FR-I1's binding flow assumed pages only. `MEASUREMENTS.md` §15e. |
| 14d | `data-portal="upgrade"` for a logged-in free member | E0(b) | **E4** (Appendix B) · E5 (FR-F6 Link Picker) | The Link Picker's Upgrade action already emits `account/plans`, which is the safe form. |
| 15 | What a theme may style inside `{{comments}}`'s native output | E0(b) | **E4** (FR-H3 comments fixture) · **blocks E10's A28** | **CLOSED BY EXECUTION 2026-08-20 — and the answer is 'almost nothing'.** `{{comments}}` emits **exactly one element**: a `<script>` loading `comments-ui.min.js` from **`cdn.jsdelivr.net`**, with no server-rendered DOM at all. Three binding consequences for A28's ten designs: (1) the thread is **not reachable by theme CSS**, so A28 is confined to the chrome around it — as this row anticipated, now with evidence; (2) it is **third-party JS from an external origin**, which any CSP a generated theme carries must allow, and which sits outside NFR-2's JS budget while still being on the page; (3) it carries its own **`data-color-scheme="auto"` and `data-accent-color` from Ghost's settings, not the Style Pack**, so a dark canvas and a live thread can disagree and FR-D7 cannot reach it — that belongs on §1.2's carve-out list. §15c. |
| 16 | Regenerating the style-guide fixture against the pinned Ghost target | E4 | **E4** (FR-H3) · re-run at every target bump · re-confirmed E15 | A renderer-derived fixture turns a changed card into a visible diff instead of silent canvas drift. The diff is reviewed, never accepted. |
| 17 | Re-computing the Baseline floor before each category block, and re-verifying licences at every bump | E4 (tooling) | **E4** (FR-G8) · executed by E9/E10 per block | `web-features` moves under a fixed date pin, so the resolved floor is recomputed and diffed. A bump carries a render-matrix re-run. |
| 18 | `{{total_members}}`'s rounding and `+` suffix; `{{content_api_key}}` rendering in a theme context | E0(b) | **E4** (shim, Appendix B core helpers) | **CLOSED BY EXECUTION 2026-08-20 — and `prd.md` is wrong for the common case.** Measured at two counts on both majors: **45 members renders `45`** — exact, no rounding, **no `+`** — and **57 renders `50+`**. The PRD says the value is "a string, always, since Ghost rounds it down and appends `+`"; below 51 it does neither, and **every Inflozo user starts below 51**. Brackets read from `core/frontend/utils/member-count.js`: ≤50 exact · 51–100 to 10 · 101–1,000 to 50 · 1,001–10,000 to 100 · 10,001–100,000 to 1,000 · above, `humanNumber` lowercased. **The majors also differ:** Ghost 6 counts `gift` in the total and Ghost 5 does not, and at ≤50 Ghost 5 returns a raw number where Ghost 6 returns a comma-formatted string. `{{content_api_key}}` renders correctly in a theme context on both, which gives A23's Custom Overlay designs their client-side path. §15f. |
| 19 | `GS100`'s exact trigger on a fixture, on both specs | **CLOSED BY EXECUTION, 2026-08-19** — see `MEASUREMENTS.md` | **E7** (FR-Q5/FR-J6) — re-prove on any gscan bump | Confirmed: error level; fires when *any* declared key is unreferenced; `{{#if}}` guards and partials both count as references. FR-Q5's design is sufficient. |
| 20 | The NQL build Ghost actually resolves at runtime (`"catalog:"` in Ghost's `package.json`) | E0(b) | **E7** (FR-I2) | **CLOSED BY EXECUTION 2026-08-20, and the premise was wrong.** Neither major runs what FR-I2 was proved against. **Ghost 5.130.6 resolves `@tryghost/nql` 0.12.7** (nql-lang 0.6.3); **Ghost 6.58.0 resolves 0.13.4** (nql-lang 0.7.0). The proof covered 0.13.x, so it covered Ghost 6 and **never covered Ghost 5** — which NFR-7 supports publicly and T3 tests permanently. This is AD-34's pattern a third time, after gscan and `member-count.js`. **Executed at runtime on both, identical, no defect reproduced:** relative dates (`published_at:>=now-30d`) and nested parentheses (`(tag:a+featured:true),(tag:b)`) both work. FR-I2's rules hold — now on observed grounds rather than on a version neither major runs. §15g. |
| 21 | The announcement-bar seed against a running Ghost — all three settings readable, and that clearing the content stops the script | E0(b) | **E3** (FR-C4 connect read) · E5 (the shim) | **CLOSED BY EXECUTION 2026-08-20.** All three settings are readable through the Admin API with the Owner's Staff Access Token — `announcement_content`, `announcement_visibility` (a JSON **string**, `"[\"visitors\"]"`, not an array) and `announcement_background`. FR-C4's seed is executable exactly as designed. §15h. |

## ⛔ The read-only GitHub token expires 2027-09-05 (DW-5)

**Status: OPEN with a date, recorded 2026-09-11 by Story 3.9.** Until now the date lived in one
comment in `tools/probe/.env.example` and in Story 1.1's `## Verification`, which is a note and not
a register row — and this document is where this project keeps a dated external fact with a trigger,
alongside the Ghost(Pro) trial below.

**The fact.** `GITHUB_TOKEN` in `tools/probe/.env` is a fine-grained token scoped to this repository
and to reads only — no organisation membership, every write refused 403, both executed rather than
assumed, and strictly weaker than the SSH key on this machine, which can push. **It expires
2027-09-05.**

**Trigger — before 2027-09-05, and the sooner half of "whenever a CI read starts answering 401".**
There is nothing to buy and nothing to wait for: the owner issues a new fine-grained token with the
same scope and pastes it into `tools/probe/.env`, which is gitignored.

**Consequence when it lapses, and it is the reason this has a row rather than a comment: nothing
announces it.** Every story from then on reads "did CI go green" through this token (DW-7 made CI
the publishing gate, so that read is how a story knows its own push published). GitHub answers
**401** to an expired token exactly as it does to a wrong one, so the first symptom is a probe
reporting a failure that is not there — on a story that has nothing to do with it, with no
forewarning, on a day nobody is looking for a credential problem.

**Owner:** whichever story is in flight on the day it lapses inherits it; the register row exists so
that story spends a minute rather than an afternoon. **Definition of done:** a new token in
`tools/probe/.env`, this row's date updated to the new expiry, and
`tools/probe/.env.example`'s comment left pointing here rather than carrying a second copy of the
date (standing rule 4 — a figure written twice is a figure that goes stale once).

## ⛔ Owner action — Dodo's annual renewal notice (VERIFY item 6, statutory half)

**Status: OPEN, assigned to the owner, 2026-08-20.** The event-catalogue half of item 6 is closed by
execution (§19a–c). This half cannot be answered by any probe: **no event among Dodo's 47 resembles a
pre-renewal reminder**, but an email need not have a webhook, so absence of an event is suggestive
and not proof.

**Action:** ask Dodo support, in writing —

> As merchant of record, does Dodo Payments send subscribers an advance notice before an annual
> subscription auto-renews? If so, how many days ahead, and does it satisfy California's Automatic
> Renewal Law and EU/UK consumer notice requirements?

**Why it must be in writing:** the exposure is statutory, not technical, and a support reply is what
belongs in the file if the obligation is ever tested.

**Consequence if the answer is no:** **FR-P1 grows a sixth transactional email** — the annual renewal
reminder — carved out of FR-P2, and **E12 builds it**. That is a scope change to a shipped epic, so
it wants answering before E12 opens rather than during it.

**No deadline pressure:** unlike the Ghost(Pro) item below, nothing expires. It simply must be
answered before E12 plans its email set.

## ⛔ The one action item with a date on it

Items 1 and 2 are the only two that block **public launch** rather than an epic, and neither can be
executed until somebody obtains a Ghost(Pro) Starter site. That is a purchasing action, not an
engineering one, so it is written here as an action item with a trigger rather than left as a note.

**Action:** open a **Ghost(Pro) Starter trial**, capture what `GET /admin/config/` actually returns,
and verify FR-C2's Preview-only detection against that recording.

**Trigger — do it when E13 closes and E14 opens, not before.** The timing is a real constraint in
both directions:

- **Not earlier.** A Ghost(Pro) trial is time-boxed. Bought during E7 it expires long before the
  launch gate needs it, and it would have to be bought twice.
- **Not later.** **E14 is the first epic that cannot ship without the answer.** FR-N4's *Hosting
  requirements* docs page describes a Starter limitation this document inferred from a payload
  nobody has observed, and §4's gate forbids that page shipping as verified until the capture
  exists. Every marketing claim aimed at Ghost(Pro) users sits behind the same gate. Discovering
  this at E15 means either a launch delay or shipping a docs page that states an unverified fact.

**Owner:** E14 acquires and captures · **E15** verifies the detection against the recording and
holds the launch gate. **Definition of done:** a real Starter `hostSettings.limits` payload checked
into `fixtures/ghost/` with its capture date and command (AD-23), FR-C2's probe verified against it,
and the `ghostpro_preview_probe` feature flag flipped on.

**If the trial cannot be obtained:** that is a scope decision for the owner, not an engineering
workaround — the honest fallback is shipping with the Ghost(Pro) path documented as untested and
withholding all Ghost(Pro)-aimed marketing, which is what §4 already specifies.

---

## Two items this architecture run added to the register

Neither was in §7.6, and both were found by execution rather than reading.

| # | Item | Standing owner | Status |
| --- | --- | --- | --- |
| 22 | `GS051-CUSTOM-FONTS` — a warning on **both** specs, requiring `--gh-font-heading` and `--gh-font-body` in the same file | **E6** (token emission) · **E7** (compile validation) | Confirmed present in gscan 6.4.2. Closed by AD-18. Re-check the rule's regex on any gscan bump. |
| 23 | Handlebars' escape is not composable, and a mustache may not abut a closing brace | **E0(a)** (in scope now) · **E7** (emitter + CI assertion) | Confirmed against `handlebars@4.7.9` — the same version Ghost `main` declares. Closed by AD-5, which supersedes §7.3's stated remedy. |
| 24 | A Vercel environment variable cannot be changed without a redeploy | **E1** (flag store) · **E2**, **E3** (the two flags) | Confirmed against Vercel's docs. FR-A2 asks for exactly the capability an env-var flag cannot give, so flags are rows in `feature_flags`. Closed by the Feature-flags convention. |
| 25 | Next 16 deprecated `middleware.ts` in favour of `proxy.ts`, on the Node.js runtime | **E1** | Confirmed against `nextjs.org/blog/next-16`. NFR-3's per-session CSP is a Node-runtime concern, so this is a correction and not merely a rename. Re-check at every Next major. |
| 26 | The two Baseline floors do not track each other | **E4** (FR-G8 tooling) · executed by E9/E10 per block | `browserslist-config-baseline` honours the `widelyAvailableOnDate` pin; `stylelint-plugin-use-baseline` inlines a frozen data map and offers **no date option**. §7.6 item 17's "recompute and diff the resolved floor" therefore covers the browserslist half only. Closed by AD-34's note; the stylelint half is diffed at every plugin bump. |
| 27 | `size-limit` needs a preset, and its default metric is brotli | **E7** (NFR-2 gate) | Confirmed by installing and running it. `@size-limit/file` is required or the gate measures nothing, and NFR-2's 40 KB number means brotli, not gzip. |
| 28 | The WebAuthn surface landed in `@supabase/auth-js` 2.75.0, not 2.105.0 | **E2** (FR-A2) | Read from the registry across seven versions. The PRD's ≥ 2.105.0 floor is a safe over-pin, not a capability boundary — which does not change §7.6 item 4, since whether the Beta API is *usable* is still unexecuted. |

## Items Round 2 added to the register

Both were found by execution. Item 29 is unresolved and is the only entry here without an answer.

| # | Item | Standing owner | Status |
| --- | --- | --- | --- 
| 29 | **`storage.buckets`' own default RLS and grant state on real Supabase** — AD-32 governs `storage.objects` with three policies and says "no bucket is public", which is a claim about a row in `storage.buckets`. Nothing governs that table. | **E1** (schema) · **E8** (assets), **E3** (snapshots) | **RESOLVED 2026-08-20 by execution, and it splits.** Run against the real storage schema — `storage-api v1.61.7` migrating a `supabase/postgres:17.6.1.140` database, not a stand-in. **The headline risk is refuted:** `storage.buckets` ships **RLS enabled with zero policies**, so `authenticated` selects **0 rows** and `update storage.buckets set public = true` affects **0 rows**. A client cannot read the bucket list or flip `public` on `site-snapshots`. AD-32's "no bucket is public" holds by default and needs no new policy. **A second finding replaces it:** the platform's own migration `0046-buckets-objects-grants.sql` executes `grant all on storage.buckets, storage.objects to service_role, authenticated, anon`. `grant all` confers **TRUNCATE, and TRUNCATE is not subject to RLS** — executed as `authenticated`, `truncate storage.objects cascade` **succeeded** and destroyed the row. This is R2-3's finding reproducing in the real platform rather than in `PRELUDE.sql`, and it lands on the two buckets AD-29 calls irreplaceable. **Not reachable today:** `storage` is not in Supabase's default `PGRST_DB_SCHEMAS` (`public, graphql_public`), so no browser client can issue it. The exposure needs a path that runs SQL as `authenticated` against `storage` — an exposed schema, or a `SECURITY INVOKER` function in `public` that touches it. **Standing owner action:** `revoke truncate on storage.buckets, storage.objects from anon, authenticated;` in the E1 migration, plus an assertion in `supabase/tests/rls.sql`, because Supabase manages this schema and may re-grant on a storage-api upgrade. |
| 30 | **Ghost identifies a theme by the uploaded zip's filename, not by `package.json.name`** | **E7** (deploy) | **Confirmed** against Ghost 6.58.0, 2026-08-19: four byte-identical zips uploaded as `probe2.zip`, `inflozo-site.zip`, `inflozo-site-v2.zip` and `MyTheme.zip` produced four separate themes, all with `package.json.name = inflozo-probe`. The filename is also the activate and delete key. Closed by AD-19's theme-identity rule. Re-check on any Ghost major, since it governs whether a deploy replaces or accumulates. |
| 31 | **The gscan version each supported Ghost major BUNDLES** — the gate's Ghost-5 verdict is only as good as this pairing, and `checkVersion: 'v5'` on a newer gscan is not a substitute | **E7** (compile gate) · **E15** (Ghost version sweep) | **Confirmed** 2026-08-20: Ghost 5.130.6 bundles gscan **4.49.7**; Ghost 6.58.0 bundles **6.4.2**. Read from `ghost@sha256:a0506f3f…48bdb5` and `ghost@sha256:c917e2a3…a70a3f2`. Closed by AD-34's pairing rule; `MEASUREMENTS.md` §13 carries the verdict comparison and the spec diff. **Refutation consequence:** if a Ghost minor changes its bundled gscan, the gate's verdict for that major is stale and FR-J6's 0/0 claim is false for it — silently, because Ghost activates a theme despite reported errors. Re-read at every supported Ghost minor, not every major. |
| 32 | **Ghost's `{{t}}` helper HTML-escapes its output**, so a translation override cannot carry a literal brace by any route | **E5** (FR-Q6 surface) · **E7** (FR-Q8 backstop) | **Confirmed** 2026-08-20 against Ghost 6.58.0. A locale value `Use &#123;name&#125; here` renders as `Use &amp;#123;name&amp;#125; here` — the visitor reads a literal `&#123;`. Combined with `'{'` throwing on `intl-messageformat` 5.4.3, there is **no** input that produces a literal brace, which is why FR-Q8 refuses rather than repairs (string catalog S0, V9). Side effect worth keeping: an override cannot inject markup — `<script>` renders as visible text — so this input's risk is the 500, not injection. **Refutation consequence:** if a Ghost version stops escaping `{{t}}`, the override surface becomes an injection vector and NFR-3's carve-out list must grow. Re-check at every Ghost major. |
| 33 | **`GS090-NO-LIMIT-ALL-IN-GET-HELPER` is a v6-spec rule only** | **E4** (library authoring) · **E7** (gate) | **Confirmed** 2026-08-20 by reading both specs in both gscan majors: the rule is absent from the v5 spec in gscan 4.49.7 *and* 6.4.2, and present in the v6 spec of both as a **warning**, alongside `GS090-NO-LIMIT-OVER-100-IN-GET-HELPER` (also warning, v6 only). So `limit="all"` in a `{{#get}}` costs a standing warning on Ghost 6 against FR-J6's 0/0 target and is unchecked on Ghost 5. Library rule: no emitted `{{#get}}` uses `limit="all"`, and none exceeds `limit="100"`. |

## Items Round 4 added to the register

Three deferred by the owner's decision sheet, each with the reason it was deferred rather than closed.

| # | Item | Standing owner | Status |
| --- | --- | --- | --- |
| 34 | ~~**What Ghost accepts in a `color`- and `image`-typed custom setting**~~ **CLOSED 2026-08-21 — HELD** | **E0(b)** (execute) · **E7** (FR-Q5 emission) · **E5** (ghost-shim parsing) | **CLOSED BY EXECUTION 2026-08-21 — the `<style>` block context HOLDS, and Round 4's S1 is refuted.**
Two probe defects had to be cleared first, and both are worth recording because each produced a
convincing false result. (1) Round 4 used an **integration token**, which Ghost refuses for settings
writes — and the *control* write of a valid `#1f6feb` was refused identically, which is why it was
marked SUSPECTED rather than held. (2) This round's first attempt used a **staff token passed raw**;
a Staff Access Token is `id:secret` and must be minted into a JWT exactly like an Admin API key.
The corrected attempt then hit a third: theme settings do **not** live on `/settings/` — writes there
return `200` and silently do nothing, control included. They live on
**`/ghost/api/admin/custom_theme_settings/`**. Executed there, with a real staff session, against the
stress theme's own `config.custom`:

    #ff0000                                       -> 200, stored          <- THE CONTROL, works
    '#fff; } body{display:none} .x{color:#fff'    -> 422 Validation error
    '#fff;background:url(https://evil.example/…)' -> 422 Validation error
    'notacolour'                                  -> 422 Validation error

**Ghost validates `color`-typed custom settings strictly**, so the `<style>` block — which unlike an
inline `style` attribute *can* carry selectors — is not reachable this way. **Note what this does NOT
change:** a tag's `accent_color` is a different field on a different endpoint and remains loose
(§21e, AD-36(4)), so `ghost-shim` still parses every bound colour. This result removes a suspected
escalation; it does not remove the rule. **The lesson that generalises:** three separate probes
returned a plausible "held" that was really a broken control — *a result whose control did not pass
is not a result*. ~~OPEN — deferred deliberately (Round 4, S1; owner chose "test it properly first").** `default.hbs` emits `--accent: {{@custom.accent_colour}}` inside a `<style>` element and `url({{@custom.dark_logo}})` unquoted. Unlike AD-3's inline-`style` carve-out, that context permits **selectors**, so the payload class is strictly larger than the tag-`accent_color` finding AD-36(4) closes. **Could not be executed in Round 4 and the reason matters:** Ghost refuses `PUT /admin/settings/` to an integration token — `403 API tokens do not have permission to access this endpoint` — and the **control write with a valid `#1f6feb` was refused identically**, so the 403 says nothing about validation. Needs a staff session. **Note the dependency direction:** AD-36 already parses these values in `ghost-shim` regardless of the answer, so this item determines how much Ghost's own validation was ever contributing, not whether Inflozo needs the check. |
| 35 | **FR-J11's rate limit must cover the export path, and a per-account compile budget** | **E7** (FR-J11, FR-J12) | **CLOSED as a requirement 2026-08-31 — FR-J11 now states both halves; E7 implements.** The counting method is **per-account over compiles**, whatever they are called, with the per-site deploy limit inside it — which is what closes the hole, since FR-J12's export runs the same pipeline and writes `exports` with a null `site_id`. The budget is sized against the measured $0.000427/deploy rather than an imagined worst case, and is stated as billing hygiene, not capacity. *(Original diagnosis, kept:* a story requirement, not a schema change, Round 4 S2.*)* The limit is derived from `deploys.created_at` scoped by site; FR-J12's export runs the same compile pipeline and writes `exports` with `site_id` null, so the counting method appears to miss it. Unexecutable today — neither route exists. **The owner additionally directed that the same story design a proper per-account compile budget**, not merely extend the per-site count. Round 4 measured the ceiling this is defending against: one deploy costs **$0.000427** (3.9 s compile + 8.8 s upload/activate), a Pro account at 25 sites deploying flat out reaches **$76.84/month** and **0.88 concurrent invocations** — so this is a billing-hygiene item, not a capacity one, and the budget should be designed with that number in view rather than against an imagined worst case. |
| 36 | **NFR-2's CSS budget re-measured on real designs** | **E9/E10** (first category gate) · **E7** (the gate itself) | **OPEN — deferred to the first category gate (Round 4, H2; owner chose "re-measure before trusting it").** Round 4 measured `screen.css` at **1.36 KB brotli** (42 KB raw, 2.2 KB gzip) against NFR-2's 40 KB — a margin so large it is more likely to be an artifact of the fixture than a property of the product. The stress theme's CSS is synthetic and highly repetitive, which is close to the best case for a compression ratio. Real designs exist only from E9 onward, so the honest reading is that **the mechanism is proven and the number is not yet meaningful**; re-measure at the first category gate against authored stylesheets, and treat that figure as the baseline. |

## Item added 2026-08-20 by the owner's answer, and the gate it creates

| # | Item | Standing owner | Status |
| --- | --- | --- | --- |
| 37 | ~~**Dodo's annual renewal notice — the mechanism exists; the TIMING is the open half**~~ **SUPERSEDED by item 37b** — the owner chose to send Inflozo's own reminder at 30/7 days, which resolves the timing exposure rather than waiting on an answer about Dodo's. Kept for the reasoning trail. | **E12** (FR-P1) · ⛔ **launch checklist** (enable the setting) | **VERIFY 6's statutory half is now ANSWERED, and it splits.** Owner confirmed 2026-08-20: Dodo sends an **Upcoming Renewal Reminder** customer email ~**2 days** before renewal, **disabled by default**, enabled at *Settings → Communication → Customer Emails*. **What closes:** FR-P1 does not grow a sixth email and E12's scope is unchanged — the reason the question was asked. Round 3's probe was correct that no webhook event exists (47 types, none pre-renewal) and correct that this proved nothing about email. **What stays open, and it is now the sharper question:** ~2 days is normal for a **monthly** plan and is very likely **short of the statutory window for an ANNUAL one** — California's Automatic Renewal Law is commonly cited as requiring 15–45 days for terms of a year or more. Appendix F assumes a **60% yearly** mix, so the annual plan is the majority case. **This is a flag, not a legal opinion.** Two actions: confirm the requirement for the jurisdictions Inflozo sells into, and ask Dodo whether the 2-day timing is configurable. **If it is neither configurable nor compliant, the original consequence returns by another route** — Inflozo sends its own reminder, FR-P1 grows a sixth email, E12 builds it. **And a second, smaller failure mode that is easy to lose:** the setting is **off by default**, so this is also a launch-checklist item with an owner, or subscribers are simply never warned and nothing surfaces the omission. |

## Closed 2026-08-20 by the owner's decision, and one platform fact re-tested

| # | Item | Standing owner | Status |
| --- | --- | --- | --- |
| 37b | **FR-P1 grows a sixth email — Inflozo sends its own renewal reminder** | **E12** (FR-P1, AD-33's seventh cron) | **DECIDED 2026-08-20, supersedes the open half of item 37.** The owner chose to send our own reminder rather than depend on Dodo's, at **30 days before an annual renewal and 7 before a monthly one**, and to leave **Dodo's ~2-day reminder ON** — the two do not collide because they fire at different moments (ours is the heads-up, Dodo's is the final nudge). **The reason is control, not redundancy:** the compliance exposure was always the *timing*, ~2 days is very likely short of the statutory window for an annual term, Appendix F assumes a **60% yearly** mix, and Dodo's timing is not ours to set. Built: `public.renewal_reminders`, whose **primary key `(user_id, period_end)` IS the never-send-twice rule** — a database fact rather than something the cron remembers. **New consequence created by owning it:** a silent failure of that job now means nobody is warned at all, so it goes on the NFR-9 alerting path. Cost is small — the renewal date, the Resend integration and the cron pattern all already exist. **Still worth confirming for the record** (no longer blocking, since 30 days almost certainly clears it): the exact statutory window for the jurisdictions Inflozo sells into. |
| 29b | **The storage DELETE guard is new, and it does NOT cover TRUNCATE** | **E1** (schema) · **E8**/**E3** (the buckets) | **RE-TESTED 2026-08-20 and D6 STAYS OPEN.** Found while resetting the probe project: Supabase now ships `protect_buckets_delete` and `protect_objects_delete`, which refuse a direct SQL `DELETE` on the storage tables **even to `postgres`** — `ERROR 42501: Direct deletion from storage tables is not allowed. Use the Storage API instead.` That is the platform enforcing exactly the trap AD-32 is written about (a row delete orphans the bytes), and it is good behaviour. **But both triggers fire on DELETE only.** Re-executed as `authenticated`: `truncate storage.objects` **still succeeds**, and TRUNCATE is still granted to `anon` and `authenticated` by Supabase's own migration. **So the platform now guards the recoverable verb and leaves the destructive one open** — TRUNCATE bypasses RLS entirely and takes every row. The control that actually holds is unchanged and is the one `RLS-TEST.sql` asserts: **`storage` is not a PostgREST-exposed schema.** Keep it that way, and never add a `SECURITY INVOKER` function in `public` that touches storage. Re-check after any storage-api upgrade — this is the second time the platform has moved under this item. |

## Raised by the reliability round, 2026-08-20

| # | Item | Standing owner | Status |
| --- | --- | --- | --- |
| 38 | **Storage has no undo, and five code paths can delete from it** | **E1** (mechanism) · E2/E7/E8 (the five paths — FR-C6's snapshot orphan rule moved from E3 to E7 on 2026-09-09, AD-33, DW-75; still five, not six) | **RULED 2026-08-31 — the owner chose to keep a SECOND COPY of the two unrecoverable buckets** (`site-snapshots`, the customer's pre-Inflozo theme and their only way back; and `deploy-artifacts`, which compile cannot rebuild because FR-J14 moves the library monthly). A copy no delete path can reach. **Both preconditions are now EXECUTED (§34), and the first strengthens the ruling rather than softening it:** (i) **Supabase Storage offers no versioning, no soft delete and no retention window** — its own documentation says nothing about recovering a deleted object — so the second copy is **necessary, not merely prudent**, and the "rely on what Supabase already gives us" alternative the decision offered **does not exist**. (ii) Pricing read from Supabase's own page: Pro includes **100 GB**, then **$0.0213/GB/month**, egress **250 GB** then **$0.03/GB**. The copy is written once and read only in a recovery, so it costs storage and essentially no egress — **free below the allowance, about $1.07/month at 50 GB beyond it**. Round 4's "single-digit dollars" was the right order and is now a read figure. **Both expire — re-read before E1 builds, since versioning may have shipped by then and would replace the copy.** **AD-29's bar on adding a sixth deletion path stands until the mechanism exists.** *(Original diagnosis, kept:* a decision, not a test — five deletion paths across four epics, and the risk is deletion, not durability.*)* AD-29 states Storage has no PITR and stops there. Counted from the spine: **five deletion paths across four epics** (FR-A5 purge — E2; FR-B3 project delete, FR-J7 artifact retention and FR-C6 snapshot orphan rule — E7; FR-K3 over-quota — E8), and **two buckets this project calls unrecoverable** — `site-snapshots` (a customer's pre-Inflozo theme; if it is gone, FR-J13 restore is impossible and the customer cannot go back) and `deploy-artifacts` (D10: not regenerable, because compile is pure in a library FR-J14 moves monthly, so a six-month-old artifact cannot be rebuilt — which is what bounds FR-J9 rollback). **The risk is deletion, not durability** — object storage does not lose bits, but five delete paths are five chances for a bug to remove something nobody can recreate. **Not blocking today** (no customer data exists) but it is cheaper to decide before the code assumes there is no backup, and AD-29 now forbids adding a sixth deletion path until it is answered. **Cost is the reason to decide rather than default:** a second copy of this data is single-digit dollars a month at any plausible v1 scale — *verify current pricing before committing, per standing rule 1*. Worth confirming at the same time: whether Supabase Storage offers versioning or a retention window that would make a separate copy unnecessary. |
| 39 | **Ghost has no lock on theme upload — concurrent uploads of one name return a raw 500** | **E7** (AD-19, AD-24) | **CONFIRMED by execution 2026-08-20, and it is a confirmation of AD-19 rather than a defect in it.** Two concurrent uploads of the same theme name to Ghost 6.58.0: one `200`, the other **`500 InternalServerError` with `EEXIST: file already exists, mkdir '/var/…'`** in the message. **Two consequences.** (1) **AD-19's per-site advisory lock is load-bearing and this is the failure it prevents**, now executed rather than reasoned; FR-J11's rate limit would not have helped, which is the "a rate limit is not a mutex" sentence AD-19 already carries. (2) **AD-24 must map this signature and must not pass it through** — the message leaks the server's directory layout, and "an unexpected error occurred" is not actionable. This is a second case for the treatment §7.6's malformed-`visibility` cascade already gets. Four other deliberate failures (corrupt zip, non-theme zip, activate-nonexistent, fatal gscan error) all returned clean typed `422`s and left the site untouched. |

## Raised 2026-08-21 — the reliability round's own follow-ups

| # | Item | Standing owner | Status |
| --- | --- | --- | --- |
| 40 | **NFR-4's restore drill — half discharged, and the other half is an owner action** | **E1** (the procedure) · **E15** (the launch gate) · ⛔ **OWNER** (PITR add-on) | **PARTIALLY CLOSED 2026-08-20.** NFR-4 requires "PITR enabled … with a restore drill exercised before launch". It had never been run in four rounds. **Run now, and it failed on the first attempt** — two silent defects, both recorded in `MEASUREMENTS.md` §25 and both fixed in **`RESTORE-RUNBOOK.md`**: a `public`-only dump omits `auth` and orphans every row behind 13 failed foreign keys while `pg_restore` still reports success; and `--no-privileges` strips every grant, which in this schema is half the security model. **The logical backup/restore path is now proved end to end and has an acceptance test** (`RLS-TEST.sql` against the restored copy — it is what caught both). **Two halves remain.** ⛔ **Supabase's own PITR restore has never been exercised** — the WAL machinery is confirmed running (`archive_mode=on`, `archive_command` shipping WAL) but triggering a restore is a dashboard action gated on the **paid add-on**, so this needs an owner decision on the plan and then one drill. Appendix F's `[NOTE FOR PM]` about the retention window is the same item. And the drill was run at **41 rows** — those timings are correctness figures, not capacity ones; **re-run against realistic volume before launch.** |
| 41 | **Ghost Admin's backup menu paths differ between Ghost 5 and Ghost 6** | **E3** (build the gate version-aware) · **E15** (capture the real Ghost 6 wording) | **EXECUTED 2026-08-31 — the suspicion is CONFIRMED and it is worse than one wrong path (§33).** Read from each server's built admin bundle: Ghost 5.130.6 carries `Advanced`, `Labs`, `Import/Export` and `Export`; **Ghost 6.58.0 carries none of them** — no `settings/advanced` route, no `settings/labs` route, and `labs` survives only as a settings key read by feature flags. Ghost 6 has **`settings/migration`** instead, and its export UI is behind the `selfServeArchives` lab flag whose own text says it *"replaces the individual export buttons with a single Export data flow"* — so the Ghost 6 surface is **flag-dependent** and any path baked in is wrong for some customers on the day it ships. **Therefore the gate does not hard-code a path for either major: it links to Ghost's own help for the connected site's version and describes what the customer is looking for.** `BACKUP-GATE.md` now says so. **Still open, and narrower:** the exact Ghost 6 label chain, which needs a click-through of a live admin (E15) — the bundle proves the Ghost 5 chain is absent, not what replaced it. *(Original diagnosis, kept:* cited, not executed, which the standing rule makes a hypothesis.*)* `BACKUP-GATE.md` lists every backup option with its exact Ghost Admin path, read from Ghost's documentation on 2026-08-21. **But those docs describe current Ghost, and Inflozo supports 5.x as well as 6.x** — Labs moved under *Settings → Advanced* somewhere in that range, so at least one path is likely wrong for one of the two supported majors. **Verify every path against T1 (6.58.0) and T3 (5.130.6)**, and have the gate either show version-appropriate paths or link to Ghost's own help pages rather than hard-coding a menu path that will drift again at the next Ghost release. **The one fact that must not be got wrong:** Ghost's JSON content export does **not** include images — that is the trap the gate exists to close, and it is confirmed from Ghost's own docs. |

## Raised 2026-09-03 by the step-5b/5c review — read in source, recorded so they are cited

Three claims the prototype and the spines rested on were true and had no citation; a correct claim
without a source is indistinguishable from the five that were wrong (standing rule: cite or execute).

| # | Item | Standing owner | Status |
| --- | --- | --- | --- |
| 59 | **Ghost Admin disables the page editor's Template dropdown when a `page-{slug}.hbs` matches the live slug** — the reason FR-I1 never emits that form | **E7** (FR-I1, FR-I3) · **E5** (FR-D6) · re-read at any Ghost major | **RECORDED — read from the built admin bundle on T3 (5.130.6) and T1 (6.58.0), identical, 2026-09-03 (F-071).** `ghost-admin/components/gh-psm-template-select.hbs` renders its select with `@disabled=matchedSlugTemplate` and the line "Post URL matches {filename}"; `matchedSlugTemplate` filters `activeTheme.slugTemplates` by the post's slug and model name. Not executed against a live page — that needs a page whose slug matches an installed `page-*.hbs`. Cited from `prd.md` §7.4 and B19's re-spec. |
| 60 | **A collection's per-route `limit:` in `routes.yaml`** — the S9 re-spec's override of `posts_per_page` (FR-I2, FR-Q1) | **E7** (routes emission) · re-read at any Ghost major | **RECORDED — in Ghost's code, absent from Ghost's routing docs, 2026-09-03 (F-073).** Ghost 6.x `core/server/services/route-settings/route-settings-parser.js:59,151` — `limit: LimitField` (read locally in 6.54.1; `RESERVED_DATA_KEYS` at :19 lists `limit`); Ghost 5.130.6 `core/frontend/services/routing/CollectionRouter.js:39` `this.limit = object.limit` and `route-settings/validate.js:93` allows `limit` (read over ssh on T3). docs.ghost.org/themes/routing lists `permalink, template, filter, data, order, rss` and not `limit`. Undocumented means it can go with a refactor: re-read at every Ghost major before FR-I2 relies on it. |
| 61 | **"Ghost keeps the previous theme under Settings → Design" holds for the FIRST upload only** | **E7** (FR-J13's degraded path) · **E5** (the Snapshot Gate copy) | **RECORDED 2026-09-03 (F-074).** `GET /admin/themes/` with the staff token lists the previous theme retained beside the active one on both majors (§15h) — true when Inflozo's theme arrives under a **new** name. But FR-J10 freezes the name per site, and Ghost replaces a same-named upload (`core/server/services/themes/storage.js:78` checks `exists(themeName)` and replaces; item 30 — the filename is the identity), so **later Inflozo versions overwrite each other in Ghost and survive only in Inflozo's Deploy History**. The reassurance is correct on the Snapshot Gate (first upload) and wrong anywhere it reads as a general guarantee; B12b's copy and every annotation say "your pre-Inflozo theme". |

## Launch gates added 2026-08-21

| # | Item | Standing owner | Status |
| --- | --- | --- | --- |
| 42 | ⛔ **The Live Supabase project must be on the Pro plan before it takes real customer data** | ⛔ **OWNER** (buy) · **E15** (verify at the launch gate) · E1 (the dump job) | **OPEN — a hard launch gate, decided 2026-08-21.** Read from Supabase's docs: the **Free plan has NO automatic backups at all** — manual dumps only. Pro gets **daily backups, 7 days, downloadable**. That gap, not the PITR question, is the real cliff: a Live project launched on Free has *nothing* behind it, and AD-26 provisions Live as a **fresh project** at go-live, so this is a step that must be performed rather than inherited. **The gate:** Live is on Pro, daily backups confirmed present in the dashboard, and one restore performed from a real daily backup using `RESTORE-RUNBOOK.md` — *a backup nobody has restored from is a hypothesis, which is exactly how the first drill found two defects*. **PITR is deliberately NOT part of this gate.** At **$100/month per 7 days of retention** it is ~8 Pro subscribers of net margin against an 11–16 subscriber break-even, and it buys 2-minute recovery over 24-hour. It is also worth less to Inflozo than to a typical product: **a database rewind cannot rewind the customer's Ghost site**, so the most consequential state is outside its reach, and Storage has no PITR at all. **Revisit trigger, so it is not simply forgotten:** at roughly **50 paying customers, or the first time any customer loses work for any reason.** Until then the cheap middle is a scheduled `pg_dump` to object storage — proven restorable, a few dollars a month, and the only option that exists on Free at all. |
| 43 | **Every finding must reach an owning document, and the gate that enforces it** | **E1** (wire into CI) · every round | **BUILT 2026-08-21 — `tools/doc-audit.py`.** Three propagation audits were run by hand and **every one found something, always in the most recently added thing**: a finding that reached no invariant (§24a), two "class" assertions that had hardcoded their members and already drifted (§27), and the human-facing document still describing a decision that had since been made (§27e). A checklist nobody runs is not a control, so the audit is now a script with the same contract as `RLS-TEST.sql`: **it exits non-zero on drift.** It checks that every document on disk is catalogued and every catalogued document still exists, that `INDEX.md`/`INDEX.html` match disk, that no `§` reference in the spine dangles, that live documents do not restate a volatile count (standing rule 3), and that no finding id sits in `MEASUREMENTS.md` without reaching an invariant, a register row or a schema comment. **It found a real orphan on its first run** — F15's fix was in the schema without its label, so the trace broke. Run `--check` at the end of every round and before any commit that adds a document. |

## Raised 2026-08-21 by the owner — repeating content has no item controls

| # | Item | Standing owner | Status |
| --- | --- | --- | --- |
| 44 | **FR-F1's control vocabulary has no way to add, remove or reorder an item in a repeating block** | **E4** (the control type) · **E5** (the surface) · **E9/E10** (every design that repeats) | **CLOSED as a requirement 2026-08-31 — FR-F1 and `Appendix C` now carry an `Item List` type.** Add · Remove · reorder, offered **only on an authored array**; a Ghost-bound repeat gets a count instead, because an Add button on posts or tags would be a lie. Remove never greys (R-12). Drawn already in `P0-3 Item List Controls`. The other half of the owner's question is recorded as **already guaranteed**: per-item *design* is impossible by construction, since AD-3 writes a control's value onto the section root. **One thing this does NOT close, deliberately:** §7.3's list of 11 categories still disagrees with the content-model scan — it omits **A3** and includes **A22**, whose only repeat is `{{#get}}`-driven (A23's half of the discrepancy is moot, the category is deleted). **Reconcile §7.3's list to the scan before E4 opens.** *(Original diagnosis, kept:* confirmed by search, raised by the owner.*)* The PRD already names the **compiler** half of this: §7.3's missing-construct table, item 1, *"Repeat over a content-prop array… **This is the largest single gap in the library and is named nowhere else**"* — 11 categories, ~166 designs. **The editor half is named nowhere at all.** FR-F1's vocabulary is Segmented Control, Stepper, Toggle, Named Select, Swatch Row, Image Picker, Icon Picker, Link Picker, Text Field/Area, Date Picker — **no list or item-management type**, and a search of the PRD for add/remove/reorder-an-item returns nothing but compiler text. So a user could type into an FAQ's three questions and never add a fourth. **What is NOT missing, and is worth stating because it is the other half of the owner's question:** a control that styles *one* item is impossible by construction — AD-3 writes every control value onto the **section root** and the stylesheet selects on it, so a design control necessarily applies to every item at once and per-item design cannot be expressed. The owner's requirement that items share one design and differ only in content is therefore already guaranteed; only the add/remove/reorder half is missing. **Two kinds of repeat need opposite controls and must not be conflated:** an **authored** array (the user types the items — A3, A5, A8–A16, A23) needs add/remove/reorder; a **Ghost-bound** repeat (A7, A17–A22, A26, A27, A30, A32) has no items to add, and an Add button there would be a lie — it needs a count. **Discrepancy recorded rather than resolved:** §7.3's list of 11 omits **A3** (link columns, social links) and **A23** (popular tag chips), both of which carry authored arrays in their content models, and includes **A22**, whose only repeat is a `{{#get}}`-driven issue preview and is therefore Ghost-bound. `tools/category-prompts.py` uses the content-model scan; §7.3's list should be reconciled to it before E4 opens. |

## Raised 2026-08-21 by the design pass — four registry gaps and the tuple's measured bound

| # | Item | Standing owner | Status |
| --- | --- | --- | --- |
| 45 | **Four places the behaviour registry does not cover, found by designing against it** | **E9/E10** (the three design changes) | **ALL FOUR RESOLVED 2026-08-31 — three by the owner, one already answered in the spec.** **(a) A2-13 Consent — CUT.** No module covers a consent bar and `dismiss` is the wrong idea: dismissing makes a bar go away, consent must be recorded and honoured. The owner chose to cut rather than write a `consent` module for one design; a site needing one uses Ghost's code injection. Recorded in `tools/export-roster.py`'s deletion table with its reason, **number 13 retired**, so the library derives to one design fewer. **(b) A9-12 Filter — ~~the filter comes off, the design stays~~ THE WHOLE DESIGN IS CUT** *(owner, 2026-09-01, superseding the 2026-08-31 reading)*. The reason the control had to go stands — there are no Ghost routes for FAQ rows somebody typed, so `filter-strip`'s degradation cannot hold there. What changed is what remains once it goes: **a plain list of questions and answers, which is what design 3 Open List already is.** Keeping it would have left two designs separated by nothing, which FR-G5's uniqueness bar exists to prevent. Recorded in `tools/export-roster.py`'s deletion table; **number 12 is retired**, and the library derives to one design fewer. **(c) A6 / Portal — already resolved in the spec and needs nothing:** Portal is Ghost's script, so `#/portal/…` does nothing with JS off, no module can fix it, and the specs say so instead of inventing a fallback. **(d) A1-4 Overlay — Sticky and Stay-transparent may not both be on.** Turning Sticky on greys out Stay-transparent with the reason shown, using R-33's pattern, so it needs no new mechanism. (a), (b) and (d) are on **pass two's work list**; nothing here is owed to the architect any more. *(Original diagnosis, kept:* each flagged inside the spec that found it, none resolved there.*)* (a) **A2-13 Consent**: no registry module covers a consent bar; `dismiss` is closest and does not fit (a consent choice is recorded, not dismissed). (b) **A9-12 Filter**: `filter-strip`'s degradation — "filters are `<a href>` links to Ghost routes and work perfectly" — does not hold when filtering authored rows client-side; there are no routes to link to. The spec renders the field only in `core`'s JS branch and asks: either the registry needs a client-side filter module or this field is out of scope. (c) **A6 / Portal**: Portal is Ghost's script, not the theme's, so `#/portal/…` fragments do nothing with JavaScript off — no registry module can fix this and the specs now say so instead of inventing a fallback. (d) **A1-4 Overlay**: `header-scroll`'s degradation leaves `position: sticky` working, so a pinned transparent bar can outlive the hero it was contrast-checked against — a consequence of the registry's own sentence, needing a rule (e.g. no Sticky with Stay-transparent). Also recorded: **A1's hero probe is not a module** — the precondition is resolved server-side, which E9 must implement server-side, not as theme JS. |
| 46 | **The emphasis slot is carrying closed-slot work in two named families — the FR-G5 bound, measured** | **E4/E9 reviewers; no schema change proposed** | **RECORDED, not open — this is the documented bound of FR-G5's proxy, now with its worst instances named.** **A11**: 3 Grid vs 13 Dense share all five closed slots; the true difference is the repeating unit's scale (box size Tiny 22 → Huge 64), which no slot carries. **A12**: 1 Grid, 2 Cards, 9 Faces and 14 Reveal share `grid-of-N · none · page · many · top`; the differences are the item's field set and its behaviour (1 Grid runs nothing, 14 Reveal runs `accordion` — identical closed slots, different modules). The owner already declined widening the schema further (density/scale slots, "Approve — do it now" decision, 2026-08-21): these rows exist so a reviewer comparing either family knows the tuple cannot help them there and human judgement is the check. |

## Closed and raised 2026-08-27 by the Ghost Build Room (step 4b)

Four of `reconcile-designs.md`'s 41 probe families left the list in one session — two closed by
execution against T1/T3, two deleted by an owner ruling — and the rulings created five register items
that had no owner before. Evidence: `MEASUREMENTS.md` §29. Rulings and their propagation targets:
`prds/prd-Inflozo-2026-08-17/reconcile-designs-decisions.md`.

**Probe families that left the register**

| Family | Was | Outcome |
| --- | --- | --- |
| 2 | Does `member-form`'s native `<form>` POST work? | **CLOSED BY EXECUTION — no.** JSON-only, and Ghost 6 requires an `integrityToken` from a separate GET. `member-form` is the **second no-JS waiver** after D2. §29a. |
| 31 | Does Portal's `#/share` modal exist, and what does it hold? | **CLOSED BY EXECUTION.** Present in `portal@2.69`, **absent in `portal@2.51`** — on Ghost 5 a Share button opens sign-in. Unthemeable (shadow-DOM iframe), five fixed destinations, no Mastodon. **D15 held unchanged.** §29b. |
| 8 | Who emits the `/search/` route; the sodo ⌘K collision | **DELETED** by ruling R-24 (native search only). No `/search/` route is emitted by anyone — `#/search` is a fragment, not a route. The ⌘K item survives as a **lint rule**: no Inflozo design may bind ⌘K, and `{{ghost_head exclude="search"}}` is never emitted. |
| 9 | The full-content index fetch and the vendored engine's budget | **DELETED** by ruling R-24. No engine is vendored; NFR-2's re-measurement is cancelled. |
| 1 | Hand-picked order, and the cost of N single-id gets | **HALF CLOSED BY EXECUTION.** `filter="id:[…]"` returns `published_at desc`, not list order, on both majors — so hand-picking costs one get per item. §29d. **The budget half became item 47, and is now CLOSED too — §30.** |

| # | Item | Standing owner | Status |
| --- | --- | --- | --- |
| 47 | **The per-template `{{#get}}` abort threshold — the only number the step-4b rulings left unmeasured** | **E0(b)**, before the inventory merge · standing owner **E9** | ✅ **CLOSED BY EXECUTION 2026-08-27 — and the premise was wrong. There is no per-template budget.** §30. Ghost races **each** `{{#get}}` against `optimization.getHelper.timeout.threshold`, **5000 ms on both majors** (`defaults.json` v5.130.6 and v6.58.0; `get.js` `Promise.race` per invocation). Measured to **150 gets on one template**: every one resolved, no `data-aborted-get-helper`, no `X-Ghost-Degraded-Render`, on T1 and T3. Marginal cost ≈ **10 ms** per pick on Ghost 6, ≈ **8 ms** on Ghost 5. **R-20's cap stands at twelve**, now on a latency budget rather than a platform limit — raising it is a new owner decision. See also item 52. |
| 48 | **Adjacency is a compile-time fact — the mechanism has an owner but no implementation** | **E9** (the compiler) | **CLOSED as a specification 2026-08-31 — the AD and the §7.3 input row both exist now.** The AD is **AD-37** (which says in terms that items 48 and 49 carry the implementation), and §7.3's missing-construct table gains **row 14**: a design DECLARES what it needs to know about the page it sits on, and the compiler answers it at build from the placement list, baking the answer into the markup. No runtime lookup, because Ghost's render cannot see the page the editor saw. What remains is E9 building it. *(Original diagnosis, kept:* ruled, not built — needs an AD and a §7.3 input row before E9 opens.*)* Ruling R-19 closed the review's largest unowned mechanism (route-awareness, ≥ 12 categories: the overlay-header precondition, stacked-padding collapse, duplicate-post suppression, one-share-block-per-route, footer adjacency, margin occupancy) by putting it in the **compiler**, which already knows every placed section and its order. `sections-inventory.md` line 71 is amended, not deleted: sections do not know about each other *at runtime*; the compiler knows about all of them. Needs an AD and a §7.3 input row before E9 opens. |
| 49 | **No render-time design substitution — the rule exists, the dead-code check must learn it** | **E7** (compile CI) | **CLOSED as a specification 2026-08-31 — FR-J3 now states the assertion.** No emitted stylesheet may contain a rule reachable only under another design's root attribute, and compile CI asserts it. The point is stated where it will be read: **the dead-CSS strip is sound only because a design never becomes another design at render** — if one could, the strip would remove CSS the page later needs. It is the check, not the rule, that this item was holding open. *(Original diagnosis, kept:* ruled, not propagated.*)* Ruling R-8 banned a placed design becoming another design at render (14 categories assumed it; ~25 hand-offs in A10 alone, eleven in A11). A design switch stays an **edit-time** act, so AD-3 holds and FR-J3's dead-code strip stays correct. Modules hide their own chrome instead (a rail that fits shows no arrows), and the editor advises the better design. Register 45(i) is therefore **refused with a reason**, not opened. |
| 50 | **The per-module edit-safe table does not exist, and FR-D20 has always cited it** | **The architect, before E4 opens** | **NARROWED 2026-08-31 — the table now EXISTS; what is left is confirming its values.** Ruling R-21 fixed the sense once — **a module is `edit-safe` if it may run inside the editor canvas without interfering with editing** — and research **`§7` gained the column on 2026-08-27**. Verified 2026-08-31: **every module in `§2.1` carries a value in `§7`, with none missing**, so FR-D20's citation is no longer to nothing and that half is CLOSED. **What remains is narrower and still open:** only `lightbox` and `carousel` were ruled by the owner directly (`lightbox` **no** — the defining case: a modal opening over the canvas when the user clicks an image to edit its caption; `carousel` **no**, settling the A14-yes/A15-no disagreement that produced this item). **The rest are the architect's pass against R-21's sense and E4 confirms each against the real canvas** — a wrong value here does not fail a build, it makes the canvas behave unlike the shipped page, which is the defect class NFR-6(c) exists to catch. *(Original diagnosis, kept because it is why the item was raised: `edit-safe` occurred once in `prd.md`, §7.3 carried no table, and research had no column.)* |
| 51 | **Ghost's own overlays are loaded at a floating version and must never be depended on** | **E9** (theme emission) · **E0(b)** re-checks on any Ghost bump | **RECORDED, not open — a standing constraint.** `sodo-search@~1.8` and `portal@~2.69` are served from jsDelivr at a floating minor range, so their internals can change **without a Ghost upgrade on the customer's site** (§29c/§29e). An Inflozo theme may depend only on the documented attribute surface — `data-ghost-search`, `data-portal`, `data-members-*` — never on their markup, class names or internal behaviour. This is the constraint that makes ruling R-24 (native search only) safe and ruling R-29 (our own share list) necessary. |
| 52 | **Ghost 6 dedups identical `{{#get}}` queries within one render; Ghost 5 does not** | **E9** (theme emission) · re-check on any Ghost bump | **RECORDED, not open — measured 2026-08-27, §30c.** `get.js` at 6.58.0 carries `generateCacheKey(resource, apiOptions)` and a per-request `options.data._queryCache` Map holding the in-flight promise, so two identical gets on one page cost one API call; **v5.130.6 has no such code**. It is not available to hand-picked order, which never sends a duplicate id, and at these sizes the query is cheap enough that the difference sits inside run-to-run noise. Recorded so no design is ever costed on the assumption that repeating a get is free — on Ghost 5 it is not. |
| 53 | **`feature_image_caption` is a SafeString, and Ghost 6 sanitises it at render while Ghost 5 does not** | **E9** (A24, A26 emission) · re-check on any Ghost bump | **RECORDED, not open — executed 2026-08-31 on both majors, §31a, `tools/probe/run-verify-e2.py`, two controls passed.** The field STORES HTML verbatim on both, and `{{ }}` and `{{{ }}}` render **byte-identical** — Ghost marks it safe, so the double stash does not escape and **R-10 #7's triple-stash carve-out is WITHDRAWN**; AD-5(2) does not move. Two live consequences: Ghost 6 strips `<em>` and `<strong>` (keeping `<a href>` and `<b>`) where Ghost 5 keeps them, so **an italic caption renders on 5 and not on 6** — A24/A26 state it rather than discover it; and on Ghost 5 a `<script>` in this field is **emitted into the page and a theme cannot escape it**. Authored by trusted staff, so self-inflicted, but "a theme can make this safe" is FALSE. |
| 54 | **`cacheMembersContent` caches a member's page PUBLICLY, keyed only by tier — the mechanism behind AD-38** | **E4** (the shim's `@member` surface) · **E9** · re-check on any Ghost bump | **RECORDED, not open — read in source 2026-08-31 on 6.58.0 AND 5.130.6, §31b.** `frontend-caching.js` is identical on both majors: flag OFF (the default, and absent from both hosts' config) → a member render is `Cache-Control: private`; flag ON → `public`, keyed on `X-Member-Cache-Tier` alone. **So any `@member` value that is not tier-derived — `email`, `name`, `uuid` — server-rendered into a page would be cached publicly and served to every other member on that tier.** A theme cannot detect the flag or opt out. AD-38 / R-28 was a prudent rule with no stated failure mode; this is the failure mode. |
| 55 | **`<details name>` is Baseline NEWLY at the FR-G8 pin, so its Tier-2 allowlist entry is required** | **E4** (the Baseline tooling) · recompute at every pin bump | **RECORDED, not open — read from `web-features` 3.36.0 on 2026-08-31, §31c.** `details-name` is `baseline: low`, `baseline_low_date: 2024-09-03`, reaching **Widely on 2027-03-03** (low + 30 months) — beyond the `widelyAvailableOnDate: 2026-08-18` pin, so **R-15's Tier-2 entry stands and FR-G8's wording is correct**. It also puts a floor under D19: support starts at Firefox **130** against the pin's Firefox **122**, so the browsers that miss out are **Firefox 122–129 only** and nothing else inside the floor. **EXPIRES when the pin passes 2027-03-03** — recompute, never assume (verify item 17). |
| 56 | **`{{comment_count}}` substitutes no placeholder and renders nothing without JavaScript** | **E9** (A28 emission) · **E4** (the string catalog) · re-check on any Ghost bump | **RECORDED, not open — executed 2026-08-31 on both majors, §32, `tools/probe/run-verify-comment-count.py`, control passed.** The helper emits a `<script>` with `data-ghost-comment-count-*` attributes and **no text**; `comment-counts.min.js` (identical on 5.130.6 and 6.58.0) **prepends** the count — `` `${count} ${singular}` `` — and replaces nothing. So `"% comment"` renders the literal **`1 % comment`**: **R-10 #8's "strike `%`" is CONFIRMED** and `appendix-h1` §3.9's stated reason was **false** (for `{{comment_count}}`; `{{plural}}` is a different helper and untested). Catalog values are the **bare noun**. **With JS off nothing is inserted at all** — not a zero, not an empty box — so every A28 no-JS line says that and the `aria-label` goes on the surrounding element. `{{comments}}` itself DOES render server-side and degrades differently. |
| 57 | **Ghost 5 renders Koenig cards through TWO renderers, and six card types emit different classes** | **E9** (A33 emission) · **E4** · re-check on any Ghost bump | **RULED 2026-09-03: the card treatments target the CURRENT renderer only, and the specification says so where a customer can see it.** A post written before Ghost 5's current editor keeps Ghost's own default card styling — readable, just not carrying the customer's chosen treatment. The owner weighed the alternative and refused it: a second selector set would roughly double A33's stylesheet and its testing permanently, and **the header card would need genuinely different rules rather than a second selector**, because the older markup lacks the structure the treatments rely on. Ghost 5 reached end of life in January 2026, so the affected population shrinks on its own. **RECORDED, not open — read in source 2026-09-03 on both majors, §35, `tools/probe/run-verify-a33-cards.py`, control passed.** 5.130.6 ships `kg-default-nodes` (Lexical) **and** `kg-default-cards` (mobiledoc), because a Ghost 5 site can hold posts written before Lexical. **header, file, product, video and embed emit different class sets depending which rendered the post** — most severely the header card, which in mobiledoc carries almost none of the structure A33's treatments style. **So an older Ghost 5 post renders cards A33's selectors will not match.** Nobody asked for this and no spec mentions it; it needs a ruling — style both shapes, or state that the card treatments are Lexical-only and say so where a customer can see it. Also settled in the same read: **`kg-cta-card`, `kg-callout-card`, `kg-product-card` and `kg-header-card` are all CONFIRMED on both majors** (four of A33's six unverified selectors), the product and header inner classes are now known, and **`kg-email-card` DOES NOT EXIST on the web** — the renderer returns an empty container unless the target is email, so that selector matches nothing and is deleted rather than corrected. `kg-nft-card` is still in both builds and A33 draws twenty cards without it: a coverage decision, not a fix. |
| 58 | **Ghost's `img_url` has no focal point, so Image focus is resolved at compile** | **E4** (the control) · **E7** (the compiler) · re-read at any Ghost major | **RECORDED, not open — read in source 2026-09-03, §36.** `img_url` accepts `absolute`, `format` and `size` and nothing else; there is no gravity, crop or focal parameter anywhere in the helper or the image-size utilities. **This is what makes Image focus a CONTROL rather than a data binding** — Inflozo resolves the crop and emits the result, and Ghost never sees the value. It is also why R-51 could add a horizontal axis without Ghost supporting one. **EXPIRES if Ghost adds a focal-point parameter**, at which point deferring the crop to Ghost's image service becomes the better design. |
