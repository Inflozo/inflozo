---
topic: Inflozo PRD
updated: 2026-08-27T19:31
---

- (decision) recovered from PRD v1.0: auth is magic link only via Supabase email OTP; no passwords exist anywhere in the product
- (decision) recovered from PRD v1.0: passkeys ship secondary and feature-flagged (Supabase passkey API experimental, supabase-js pinned >=2.105); magic link stays primary
- (decision) recovered from PRD v1.0: single GA release - no beta, no waitlist drip; 100 percent of PRD ships before go-live
- (decision) recovered from PRD v1.0: all E2E/integration testing runs against live production Vercel+Supabase and two real Ghost targets; deemed safe because zero users pre-launch (owner mandate)
- (decision) recovered from PRD v1.0: single-source WYSIWYG - canvas renders the actual section .hbs+CSS that ships in the theme; React section preview re-implementations explicitly rejected
- (decision) recovered from PRD v1.0: bake dont expose - copy and control values compile into the theme; only 1-2 @custom settings (color_scheme), far under Ghost 20-setting cap
- (decision) recovered from PRD v1.0: deployed sites cost Inflozo $0 - assets bundle into the theme zip and are served by the user Ghost server; infra pays only for editing sessions
- (decision) recovered from PRD v1.0: closed control vocabulary of exactly 10 control types; no units/hex/CSS at section level - raw values live only in the Style Pack editor
- (decision) recovered from PRD v1.0: Dodo Payments as merchant of record (hosted checkout, Dodo handles taxes); young-MoR risk hedged with a swappable billing adapter
- (decision) recovered from PRD v1.0: freemium - Pro $15/mo or $150/yr; infra break-even at 4 Pro subscribers, >95 percent infra margin
- (decision) recovered from PRD v1.0: Free tier = 2 variants per category (68 of ~487) so a Free user can still ship a complete good-looking site
- (decision) recovered from PRD v1.0: downgrade never touches deployed themes; over-limit projects go read-only and Pro-section deploys are blocked with an itemized swap-or-upgrade sheet
- (decision) recovered from PRD v1.0: contextual upgrade prompts at exactly five moments; never a blocking interstitial elsewhere
- (decision) recovered from PRD v1.0: app chrome ships light-only (app dark theme deferred); authored sites fully support dark
- (decision) recovered from PRD v1.0: no per-breakpoint editing in v1 - sections are auto-responsive; device toggles are preview-only
- (decision) recovered from PRD v1.0: strict vertical-stack canvas model, no nested/columns free-form layout in v1 - part of the impossible-to-make-ugly guarantee
- (decision) recovered from PRD v1.0: gscan gates every deploy server-side - errors block, warnings surface but deploy; library target 0 errors 0 warnings
- (decision) recovered from PRD v1.0: Admin API keys encrypted in Supabase Vault and never sent to any client (server proxy mints short-lived JWTs); Content API key goes browser-side as Ghost intends
- (decision) recovered from PRD v1.0: Ghost(Pro) Starter connections (no custom themes) accepted as Preview-only rather than rejected; deploys blocked with docs link
- (decision) recovered from PRD v1.0: canvas renders real .hbs via Handlebars-in-browser with a Ghost-helpers shim; fidelity enforced by golden tests vs live-Ghost-rendered HTML
- (decision) recovered from PRD v1.0: single-editor model with last-write-wins autosave; team seats/collaboration explicitly deferred out of v1
- (decision) recovered from PRD v1.0: one Next.js app on a single domain serves marketing (SSG) + product; server compute on Vercel Node functions, not Edge
- (decision) recovered from PRD v1.0: section registry, style packs, starters and Orbit Weekly sample data live in the repo as versioned data, not in the DB
- (decision) recovered from PRD v1.0: routes.yaml deploy attempts Admin API upload, else a first-class guided manual fallback; deploys never silently skip routes
- (decision) recovered from PRD v1.0: deploy-only vs deploy-and-activate are distinct buttons (owner requirement); rollback = one-click redeploy of any retained artifact
- (event) research: Fantasma (fantasma.io) is a LIVE direct competitor — visual Ghost builder, 155+ sections, Admin API publish, Pro $99/yr — PRD assumes no visual builder exists for Ghost
- (event) research: Ghost(Pro) plans renamed Starter/Publisher/Business; custom theme upload needs Publisher $29/mo — PRD's Starter-block logic holds but plan names/docs need updating
- (event) research: routes.yaml has NO official Admin API upload endpoint (internal/unofficial only) — PRD FR-I4 'attempt automated upload' rides an unofficial API
- (event) research: Supabase passkeys are Beta (May 2026), not GA — PRD FR-A2 experimental-flag framing stays valid, still not launch-critical
- (event) research: Ghost 6 removed ?limit=all (max 100, paginate) — affects Admin/Content API listing code assumptions
- (decision) pricing re-affirmed post-research at $15/mo / $150/yr despite Fantasma $99/yr — premium positioning is deliberate
- (decision) competitive stance vs Fantasma: lead on library scale (487 vs ~155 sections) and generated-theme code quality; further differentiators under brainstorm
- (event) Fantasma teardown: LACKS membership/tier/paywall sections, rollback/versioning, gscan story, image optimization, inline WYSIWYG editing; single Source aesthetic; hard caps all plans
- (event) Fantasma teardown: HAS Free ZIP export (blocked if Pro content), Pro white-label, Pro dark mode, Pro real-content preview via Admin API key, routes.yaml generation w/ manual upload
- (decision) evergreen themes IN (v1): library updates track Ghost releases; users update manually by signing in and redeploying — never automatic
- (decision) rejected: public deploy quality badge/report (nobody trusts a new tool's badge) and free theme-checkup tool (gscan already free from Ghost)
- (decision) Site Remix IN (v1): whole-canvas re-roll of pack/variants, content preserved; correctness is the bar
- (decision) rejected: brand extraction from arbitrary URLs — manual selection is a few clicks anyway
- (decision) no 'Membership Studio' branding: members/pricing/signin stay ordinary composable templates with drag-drop sections (already in PRD)
- (decision) ActivityPub/social-web sections deferred — too complex for first go
- (decision) redesign-my-site wizard IN (v1): 2-3 proposals with user content; must generate structurally varied designs
- (decision) theme ZIP export IN on ALL plans including Free
- (decision) credit removal (white-label) is Pro-only; credits default on everywhere
- (decision) NO live-site preview UX in v1 — would require a working Ghost install just for previews; deploy-only button remains but no preview surfacing
- (decision) deploy insurance IN (v1): pre-activation snapshot of existing theme, multiple pre-deploy checks, safe rollbacks
- (change) PRD updated: competitive rewrite (§1.1/1.4/new 1.5), FR-C7 redesign proposals, FR-D17 Site Remix, FR-J12-J15 (export/snapshot/updates/credits), FR-I4 routes reword, FR-A2 passkey beta, FR-C2 plan names, Appendix B pagination, F table rows, G deferrals, epics E3/E5/E7
- (decision) confirmed: Free must not export OR deploy Pro sections — FR-L3 gating applies to ZIP export identically
- (decision) library versioning: always-latest at compile (user chose simplest); no per-project pinning; any redeploy and the canvas always use current library
- (decision) credit text is 'Built with Inflozo' in both README and footer; Pro can remove
- (decision) no cap on sections per template — deliberate contrast to Fantasma caps; editor must hold 60fps on long pages
- (decision) FR-C7 redesign proposals are re-runnable — also an entry point under dashboard New project for connected sites, not connect-flow only
- (decision) Ghost content is READ-ONLY to Inflozo (new principle P8): deleting a section removes theme code only, never posts/tags/authors/tiers
- (decision) deleted sections recoverable via undo WITH all customizations; undo stack persists across reloads (per browser)
- (decision) local-first persistence: instant local save (zero perceived latency), cloud sync every few minutes + on close/lock-release/deploy/export; autosave toggleable; manual save always available
- (decision) cross-device single-editor lock with request/accept nudge; holder syncs then releases and goes read-only; requester gains edit rights
- (assumption) stale lock auto-releases on heartbeat timeout; revived stale holder goes read-only and discards unsynced local edits with notice — pending user confirmation
- (decision) unanswered nudge: requester notified 'no response; X unsaved edits there', may take over from last synced snapshot after refresh; revived holder goes read-only and is told its edits were not included (resolves FR-D18 assumption)
- (decision) shuffle item overflow: variant renders what its structure fits; surplus items preserved invisibly and restored on shuffle back; sidebar shows 'N items, M shown'
- (decision) inline formatting: bold/italic/underline/link allowed on ANY text prop via Koenig/Medium-style floating selection toolbar; compiles to clean strong/em/u/a; paste stripped to these marks
- (decision) header/announcement/footer are global singletons — edit once, changes every template; pinned Site-wide group with globe badge in Layers
- (decision) Free tier: open canvas, gated exits — Free can place/shuffle/edit Pro sections (subtle Pro badge); blocking only at deploy, ZIP export, and any compiled-code exposure; upgrade moments reduced to four
- (decision) in-app code viewer deferred to future — ZIP export is the only compiled-code surface in v1
- (decision) custom Style Packs stay per-project only (deliberate): new projects likely want new palettes; re-entering hex/fonts is easy; no account-level pack library
- (decision) new projects default to Light + Dark (presets carry hand-paired dark palettes; dark is prepaid work)
- (decision) business metrics added with modest launch targets + counter-metrics: activation >=30% signups to first deploy in 7d, free->Pro >=3% in 90d, Pro monthly churn <5%
- (decision) Resend is the transactional email provider; five transactional emails in v1, no marketing/digest emails; updates-available stays in-app only
- (decision) no time-boxed Pro trial — the open canvas IS the trial; upgrade is the only path to Pro exits
- (event) input reconciliation complete: design-prompt 42 findings, mockups ~31 stale-design + 14 prd-gaps; 16 PRD decisions assembled for owner; stale-design roll-up = UX pass to-do
- (decision) live-infra testing is a MUST: production domains inflozo.com (marketing) + app.inflozo.com (app), real Supabase/Resend/Dodo, exactly as a real user; owner provides Ghost on a DO droplet
- (decision) posts_per_page moves to a dedicated Theme Settings page (Ghost global config), NOT the Home feed Count control — mockup direction wins in modified form
- (decision) NEW v1 scope: custom-settings builder — users define Ghost @custom design settings for their theme via dedicated UI; P3 revised to 'bake by default, expose deliberately'
- (decision) site width + gutters become Style Pack tokens; per-section Content width universal control REMOVED; sections default to full site width
- (decision) notifications center IN v1 — full in-app system (deploy/health/billing/updates events)
- (decision) contact form and docs suggest-an-edit KEPT — both relay to owner via Resend (owner-relay email category added to FR-P)
- (decision) section-level vocabulary stays the ten; kit extras (slider/date/color popover) sanctioned for app surfaces only (Routes Manager, Theme Settings, Style Pack editor)
- (decision) channels + rich All/Any filter builder + collection ordering adopted into FR-I2 — all ship v1
- (decision) adopted: FR-C8 key rotation, FR-K6 asset actions (Replace/Copy URL/Download), template-switcher new-template shortcut, deploy cancel pre-upload only, Appendix H voice canon
- (decision) Ghost(Pro) test target naming updated to Publisher; Starter site verifies Preview-only path
- (change) PRD round-six edits applied: live-infra mandate + domains (§4/§7.1), P3 revised, FR-Q Theme Settings + custom-settings builder (Ghost docs verified: 5 types, cap 20, immutable keys), FR-B7 notifications, FR-P3 owner-relay, FR-C8, FR-K6, site-width tokens (FR-E1/F2/App-A), FR-I2 channels+filters, FR-I3 shortcut, FR-J8 cancel, Appendix H voice canon, epics E7/E13
- (decision) Ghost 5.x sites supported alongside 6.x (ghost-api v5 engines covers both); <=4.x rejected with friendly update message; a 5.x droplet joins the live test matrix
- (decision) FR-Q3 confirmed: binding is the mechanism; all five Ghost setting types available via automatic type mapping (controls -> boolean/select/image/text, Style Pack color tokens -> color)
- (decision) control vocabulary OPEN-ended (owner reversal of 'exactly ten'): any number of control types allowed, must stay simple/visual/named-value and P2-compliant; Date Picker admitted (countdowns)
- (decision) feed model: exactly one main feed per collection template (collection-bound, paginated, sized by global posts_per_page); all other feed sections {{#get}} fixed-Count never paginated; per-collection page size dropped (not a Ghost capability)
- (decision) dark-mode custom settings: Light+Dark themes ship built-ins color_scheme + Dark accent color + Dark logo; color promotion creates the light setting, pairs with the dark built-in; AA caution shown at promotion
- (decision) reviewer-gate decisions: members templates compile only when designed (auto-emit routes); 2 starters (Quiet, Ledger) rebuilt Free-only; Appendix A extracted to companion file; deploy/export gated on edit lock; economics recomputed (break-even ~8-9 Pro)
- (decision) triage: 9 adversarial lows + 8 rubric suggestions deferred as non-blockers (detail preserved in review-*.md for post-final grooming); owner: Umang; revisit at architecture/UX phase
- (event) PRD finalized — v2.0, status final; prose polish applied (18 cosmetic edits, semantics verified unchanged)
- (decision) review triage 1/7: canvas isolation = same-origin iframe + mandatory sanitization of all Content-API-sourced HTML before canvas render (editor-preview only, compiled output untouched); resolves NFR-3 vs 7.3 contradiction
- (decision) review triage 1/7: canvas isolation = same-origin iframe + mandatory sanitization of all Content-API-sourced HTML before canvas render (editor-preview only, compiled output untouched); resolves NFR-3 vs 7.3 contradiction
- (decision) review triage 1/7: canvas isolation = same-origin iframe + mandatory sanitization of all Content-API-sourced HTML before canvas render (editor-preview only, compiled output untouched); resolves NFR-3 vs 7.3 contradiction
- (decision) review triage 2/7: Auto dark compiles as prefers-color-scheme CSS media query, JS toggle overrides via data-mode attribute; no-JS dark render added to FR-G4 acceptance; resolves FR-E4/G4/Q5 contradiction
- (decision) review triage 3/7: FR-A5 deletion cascade extended - suggestions anonymized (author 'Deleted user', image removed), votes + notification rows deleted (GDPR gap closed)
- (decision) review triage 4/7: FR-J14 backward-compat contract - variants never deleted only superseded, schema changes append-only or migration-mapped, unresolvable placed section degrades to nearest variant + notice; no-pinning decision preserved
- (decision) review triage 5/7: 3 dark built-in setting slots AND keys reserved unconditionally in every project (Light-only included) - 17 user slots uniform, mode toggle can never overflow Ghost 20-cap or collide keys
- (decision) review triage 6/7: feed-less collection templates allowed - pre-deploy warning + compiler emits noindex/canonical-to-page-1 guard on /page/N/; no hard main-feed requirement
- (decision) review triage 7/7: theme name freezes at first deploy per project x site (rename = display name only); theme names unique per site, collision prompts rename not overwrite (FR-J10)
- (change) PRD updated per review triage 1-7: FR-A5 cascade, FR-D7 toggle note, FR-E4 prefers-color-scheme compile, FR-G4 no-JS dark criterion, FR-H2 zero-feed guard, FR-H5 wording, FR-Q2/Q5 unconditional 3-slot reserve, FR-J10 name freeze + uniqueness, FR-J14 compat contract, NFR-3 + 7.3 same-origin/sanitize isolation model
- (decision) review triage P1: rollback of previously-deployed artifacts is exempt from FR-L3 Pro exit gating (restores what already ran, mirrors snapshot-restore exemption)
- (decision) review triage P2: downgrade enforcement = block-until-user-deletes; itemized over-limit sheet; no new uploads, no deploys to over-limit connections, asset library read-only until under Free limits; extra sites disconnected by user down to 1; NO dormant/frozen connection state (frozen sites would count toward cap); Inflozo never auto-deletes
- (decision) review triage P3: lock-takeover notice-only RE-AFFIRMED by owner after review challenge - no download-my-version escape hatch; unsynced edits on revived holder are discarded with notice
- (decision) review triage P4: pre-Inflozo snapshots keyed by site URL and RETAINED across disconnect/reconnect (reverses delete-on-disconnect); snapshot captured at first theme upload (not first activate); never re-captured while active theme is inflozo-*
- (decision) review triage P5: suggestions board abuse controls - per-user rate limits, title/body length caps, images via FR-K2 pipeline, new-account throttling; suggestion text publishes immediately but images appear on the PUBLIC board only after admin approval (in-app + admin see them pre-approval)
- (decision) review triage: ~20 remaining behavioral guards + all mechanical editorial fixes delegated to facilitator by owner (obvious-fix rule); each applied change logged
- (change) PRD updated per delegated triage: ~20 behavioral guards applied (FR-A4/A5/B3/C2/C5/C6/D5/D7/D9/D10/D16/E3/F6/H2/H4/H6/I1/I3/I4/J9/J13/K2/L2/L3/M2/M3/M4/Q2/Q3) + 25 editorial fixes (structure S1-S10, prose P1-P9+minors) via subagent; addendum anchors renamed AD1-3
- (event) review-2026-08-17.md fully dispositioned: 7 owner-triaged priority items, 5 owner product calls, remainder auto-applied; 1 finding rejected (P3 takeover notice-only re-affirmed); PRD version bumped 2.0 -> 2.1
- (event) Validate intent run on v2.1: 3 reviewers (rubric, adversarial-general, consistency); grade Poor per formula (2 critical: FR-J14 forced-updates-vs-opt-in, FR-D6 synthesis defaults undocumented; 10 high); report at validation-report.html/.md
- (event) test
- (event) Validate intent run on v2.1 (prior 'test' event line was an accidental diagnostic write - ignore): 3 reviewers (rubric, adversarial-general, consistency); grade Poor per formula (2 critical: FR-J14 forced-updates-vs-opt-in, FR-D6 synthesis defaults undocumented; 10 high, 16 medium, 12 low consolidated); report at validation-report.html/.md
- (event) Validate intent run on v2.1 (prior 'test' event line was an accidental diagnostic write - ignore): 3 reviewers (rubric, adversarial-general, consistency); grade Poor per formula (2 critical: FR-J14 forced-updates-vs-opt-in, FR-D6 synthesis defaults undocumented; 10 high, 16 medium, 12 low consolidated); report at validation-report.html/.md
- (decision) validation triage criticals: C1 keep single live library, delete 'never automatically', add pre-deploy confirm listing library changes; C2 author normative synthesis-defaults tables into sections-inventory.md (owner mandate: consult Ghost theme structure docs first)
- (decision) validation triage highs: H1 starters ship members templates present-but-undesigned (Portal covers flows, no routes.yaml on starter first deploy); H2 DoD deployable set = T1-T3, T4 pass = block works; H3 FR-J13 download endpoint labeled unofficial + verified on all 4 targets + Inflozo themes carry signature metadata - rollback/restore guaranteed only for Inflozo-signed themes, 3rd-party themes never touched, fallback guides to Ghost Settings->Design; H4 no capability detection - inform user upfront Starter cannot deploy, proceed-then-fail with check/upgrade-plan guidance; H5 theme name freezes per project x site, collisions get suffix; H6 main-feed rule extends to tag/author templates (owner mandate: consult Ghost pagination docs first); H7 color promotion accent-only in v1; H8 takeover clears losing device undo journal + message states concrete loss count; H9 G8-G10 are internal goals tracked manually by owner - no telemetry FR; H10 owner rejects lock-in mechanics - retention brainstorm OPEN (owner: Umang, revisit: this session)
- (decision) validation triage mediums: M1 wave counts corrected 156/199/132; M2 Quiet+Ledger rewritten with named Free variants, swap-later clause deleted; M3 inventory version pin dropped; M4 private.hbs conditional emit like members; M5 glossary appendix added; M6 user journeys delegated to bmad-ux pass (delegation sentence in PRD); M7 perf reference chosen by facilitator per fast-and-snappy mandate; M8 G1 clock redefined to start at credentials submitted; M9 AA guarantee scoped to presets/defaults, user token edits transfer responsibility; M10 routes state renamed last-offered + live route verification probe; M11 snapshot keying brainstorm OPEN (owner: Umang, revisit: this session); M12 no rollback on disconnected sites - safety ops require connection; M13 promotion limited to plain-text props (no formatting in Ghost Admin settings); M14 i18n: v1 user-visible copy via editable text props, remaining chrome strings itemized, LTR-only, RTL deferred in App G; M15 SUPERSEDES production-only posture post-launch: two infra sets, existing prod becomes permanent Test, fresh Live set spun up at go-live; M16 owner re-affirms no cut-line - all 487 ship, slip delays launch, sections are the heart of the tool
- (decision) validation triage lows: all 12 applied as recommended - fonts bundled as woff2 in theme zip; FR-D10 fallback exempt from NFR-1 latency + AD3 scoped to default path; image downscale trade-off stated in upload UI; infinite scroll falls back to numbered links; health emails capped 1/site/week; NFR-4 gets PITR + tested restore line; inventory tilde dropped; sibling-shuffle copy up-to-18; A17 primary->main feed; App F notes monthly billing; FR-I3/FR-J14 soft phrases pinned; verify-at-build-time list added to 7.6
- (change) v2.2 hardening applied: all 40 validation triage decisions encoded across prd.md (frontmatter 2.2/2026-08-18), sections-inventory.md (pin dropped, exact 487, main-feed term, + normative Synthesis Defaults section appended - Ghost structure docs consulted), addendum.md (AD1 journal invalidation, AD2 loss accounting, AD3 scoped rejection); FR-H2 archive extension verified against Ghost pagination docs (native paginated posts context on tag/author confirmed); Quiet=A4#1/A18#1/A25#1/A26#1, Ledger=A4#2/A17#1/A10#1/A32#2 all-Free; 2 items remain OPEN pending owner: H10 retention mechanics, M11 snapshot keying
- (decision) post-triage round 2: G1 retargeted to <10 min p75 end-to-end including key creation; FR-Q6 Translations module added (standard chrome-string catalog, {{t}} + locales/ per Ghost translate docs, user overrides + locale export, canvas shim renders overrides) - reverses the locales deferral, only RTL stays deferred; retention levers all four adopted (FR-J14 monthly library cadence commitment, FR-C5 Pro compatibility watch in-app only, FR-L1 yearly-default checkout, App F month-1 churn spike modeled) - CLOSES open item H10; snapshot keying option 1 adopted (FR-C8 edit-URL-in-place carries site record + snapshot, connect flow hints on matching credentials) - CLOSES open item M11; M13 plain-text promotion rule kept pending owner confirmation after elaboration
- (change) v2.3: G1 target, FR-Q6 + {{t}} shim (FR-H5) + locales in FR-J5/7.4 tree + Appendix G narrowed to RTL-only + glossary row + E7 scope; FR-J14 cadence, FR-C5 compatibility watch, FR-L1 yearly default, App F retention modeling; FR-C8 site moves + FR-J13 no-stranded-snapshot
- (decision) M13 CLOSED (owner-confirmed): text-prop binding shows upfront confirm at bind time ('no formatting while editable in Ghost Admin'); on confirm existing marks are stripped and inline formatting is disabled for the prop while bound; unbinding re-enables. FR-Q3 + FR-D4 updated; stays within v2.3
- (event) Stress-test Validate run on v2.3: 6 reviewers dispatched (rubric, regression-vs-40-findings, adversarial on new v2.2/v2.3 material, external-claims web verification, buildability simulation, cross-file consistency); owner selected 1-6, declined scope/economics realism
- (event) Stress-test Validate run on v2.3: 6 reviewers dispatched - rubric, regression audit vs the 40 v2.1 findings, adversarial on new v2.2/v2.3 material, external-claims web verification, buildability simulation, cross-file consistency. Owner selected 1-6, declined scope/economics realism. Reviews land as review-st-*.md
- (event) Stress-test validation of v2.3 complete: grade Poor (13 critical, 41 high, 50 medium, 31 low; 155 pre-dedup, 135 after). No dimension broken - 5 strong, 2 adequate. Root cause found by 3 reviewers independently: v2.2/v2.3 fixes written into home FR only, never propagated. 4 criticals are external platform facts (Ghost integration-token method allowlist) unfindable by re-reading the PRD. v2.1 report archived as validation-report-v2.1.html/.md. Reports at validation-report.html/.md; 6 reviews at review-st-*.md. NOTHING TRIAGED YET - all 135 findings open.
- (decision) C1 OWNER OVERRIDE (rec was 'a'): use the site Owner's Staff Access Token to reach theme download (bypasses integration-token allowlist) INSTEAD of accepting no-snapshot; PLUS new pre-deploy gate requiring user to confirm they have downloaded current theme + routes file before first deploy
- (decision) C2: a - FR-J1 emits no engines.ghost-api, strike from 7.4, page.hbs gates on @page.show_title_and_feature_image, FR-J6 keeps 0/0
- (decision) C3 OWNER OVERRIDE (rec was 'a' fingerprint): b - domain change forces disconnect + reconnect with new API keys + re-read posts/tag/content data; REMOVES FR-C8 edit-URL-in-place adopted in round 2
- (decision) C4: a - credit strings become a locked catalog namespace, not listed in Translations, not overridable on any plan
- (decision) C5 OWNER OVERRIDE (none of a/b/c): paywall CTA is a STANDALONE TEMPLATE with its own editor surface, rendered where the user places the paywall line in the Ghost editor - not a drag-drop section. Separately a generic CTA section exists, placeable, controlled by visibility/member status
- (decision) C6: a - site_snapshots table + site-snapshots bucket keyed to user_id, RLS enforced, no leak across users or sites; FR-A5 offers snapshot download before purge
- (decision) C8: a - re-sequence epics, split E4/E7 compile gate, move FR-C7 out of E3, name the 5 pilot sections, notifications table in E1
- (decision) C9: a - project_site_bindings(project_id, site_id, theme_name, frozen_at) table, exempt from FR-J7 pruning
- (decision) C12: a - pin Inflozo gscan >= newest bundled version across supported targets, upgrade only behind a library release with full re-run, add gscan warning set to 7.6
- (decision) C13: c - cheap version: one broadcast notice per Ghost release confirming library verified, no per-site analysis, no compat data authoring, no variant manifest
- (decision) C7/C10/C11 held for owner elaboration before decision
- (decision) C1 OWNER OVERRIDE (rec was a): use the site Owner's Staff Access Token to reach theme download (bypasses the integration-token allowlist) INSTEAD of accepting no-snapshot; PLUS a new pre-deploy gate requiring the user to confirm they have downloaded the current theme + routes file before first deploy
- (decision) C2: a - FR-J1 emits no engines.ghost-api, strike from 7.4, page.hbs gates on @page.show_title_and_feature_image, FR-J6 keeps 0/0
- (decision) C3 OWNER OVERRIDE (rec was a fingerprint): b - domain change forces disconnect + reconnect with new API keys + re-read posts/tag/content data; REMOVES FR-C8 edit-URL-in-place adopted in round 2
- (decision) C4: a - credit strings become a locked catalog namespace, not listed in Translations, not overridable on any plan
- (decision) C5 OWNER OVERRIDE (none of a/b/c): paywall CTA is a STANDALONE TEMPLATE with its own editor surface, rendered where the user places the paywall line in the Ghost editor - not a drag-drop section. Separately a generic CTA section exists, placeable, controlled by visibility/member status
- (decision) C6: a - site_snapshots table + site-snapshots bucket keyed to user_id, RLS enforced, no leak across users or sites; FR-A5 offers snapshot download before purge
- (decision) C8: a - re-sequence epics, split E4/E7 compile gate, move FR-C7 out of E3, name the 5 pilot sections, notifications table in E1
- (decision) C9: a - project_site_bindings(project_id, site_id, theme_name, frozen_at) table, exempt from FR-J7 pruning
- (decision) C12: a - pin Inflozo gscan >= newest bundled across supported targets, upgrade only behind a library release with full re-run, add gscan warning set to 7.6
- (decision) C13: c - cheap version: one broadcast notice per Ghost release confirming the library was verified, no per-site analysis, no compat data authoring, no variant manifest
- (decision) C7/C10/C11 held for owner elaboration before decision
- (decision) C10 SCOPE DECISION (owner): canvas pulls ONLY list/metadata from the Content API - posts/tags/authors/@site fields. It NEVER pulls post or page body HTML. Post/page sections render dummy placeholder content in the canvas. This removes the arbitrary-untrusted-HTML surface NFR-3 was written against; NFR-3 must be rewritten to match what is actually rendered
- (decision) C10 CLOSED: NFR-3 rewritten as the text-node rule (Content-API values rendered as text nodes, never innerHTML) - no sanitizer library needed. Carve-outs: (1) never read codeinjection_head/codeinjection_foot, (2) validate http/https scheme on all URL fields before use, (3) excerpts stay text-only AND are sanitised. Cross-origin iframe question closed as unnecessary (7.3 same-origin stands, false justification to be corrected). C10 drops critical to low
- (decision) C7 CLOSED: option D - sections authored as annotated HTML, compiler walks the DOM and serializes via outerHTML, Handlebars blocks injected as HTML comment markers and resolved in a final regex pass. No Handlebars parsing or printing anywhere. Rejects option A (partial evaluator + AST printer) as an uncosted multi-week component
- (decision) C7 addendum: exported theme MUST be hand-editable and production grade (Casper/Source quality). Partial rule (Ravi): content sourced from GHOST becomes a real partial (post cards, pagination, bylines, subscribe forms - the foreach context feeds them, no params needed); content sourced from the DESIGN is inlined and baked into the template. Output spec: partials/ structure, meaningful class names derived from section names, one organised stylesheet with tokens at top, proper package.json, section comments, Prettier-formatted, zero builder fingerprints. Cost acknowledged as higher than 'valid output' and touching all 487 sections
- (decision) C7 open item: escaping rule still needs PRD text - user content containing Handlebars syntax must be emitted inert
- (decision) C11 CLOSED: replace the DOM-normalized-diff golden test with three layers - (1) committed .hbs snapshots per variant, per-commit, (2) shim contract tests asserting each shimmed Ghost helper matches real Ghost output for recorded inputs, per-commit, (3) nightly visual/perceptual diff of canvas vs deployed page on a real Ghost. Image URLs are NOT normalized away; the canvas shim emits Ghost-shaped sized URLs instead
- (decision) C11 addendum: WYSIWYG promise rewritten from '100% WYSIWYG' to 'the canvas is exactly what Ghost will render' - a user's own Ghost code injection legitimately changes the live page and is outside Inflozo's control. New requirement: connect-time notice when a site has code injection present
- (decision) NEW FR (drift detection) ADOPTED: before every deploy, read the live theme via the Staff Access Token, compare against the manifest of what Inflozo last deployed, and on difference stop and show exactly what would be overwritten with an offer to download first. Owner constraint: must be exact, zero false positives. Enabled by the C1 Staff-token decision; shares its mechanism with the C11 layer-3 check
- (decision) Ravi (Ghost CMS Expert) persisted as a party member in _bmad/custom/bmad-party-mode.user.toml, plus the ghost-build-room group (memory on) with Winston, Amelia, Sally, Murat, Dana
- (decision) Binding model DECIDED (owner): context-aware binding is baked into the editor - Inflozo only ever OFFERS bindings valid on the current template. Invalid bindings are made unavailable, not warned about. Prevention over warning; the user cannot construct a bad binding. Requires a normative context->bindable-field matrix as the source of truth
- (decision) Binding matrix RESEARCH commissioned (owner mandate: read Ghost docs extensively): two agents building citation-backed references - research-ghost-binding-contexts.md (per-template context matrix, @ variables, foreach scope, get requirements, partial nesting, hash param limits) and research-ghost-empty-values.md (guard patterns in Casper/Source, Handlebars truthiness, empty-field failure modes, fresh-install reality, hide-vs-fallback recommendation)
- (decision) FR-H5 flagged for REWRITE: it specifies the canvas renders real section .hbs via Handlebars-in-browser, an architecture the C7 option-D decision replaced. Helper shim survives, Handlebars-in-browser plumbing does not
- (decision) FR-H5 REWRITTEN (final text agreed): canvas renders sections directly from annotated-HTML source, the same source the compiler serializes - Handlebars is never parsed or executed in the browser. Helper shim retained for the Appendix B surface, with img_url now emitting Ghost-shaped sized URLs and srcset (not pass-through). All Content-API values render as text nodes per NFR-3. Shim fidelity enforced by contract tests against recorded real-Ghost output, replacing the golden DOM diff
- (decision) FR-H7 NEW (final text agreed): context-aware binding, prevention not warning. Editor offers ONLY bindings valid in the current template context per normative Appendix B.1. Matrix keyed on three axes - template (single-resource templates expose their resource only inside {{#post}}/{{#page}}, never at top level; list templates expose posts and pagination at root), scope (top level vs inside foreach), and connected Ghost version (fields added after a target's version, e.g. the seven social fields added in 6.36.0, are not offered). @site.* and @member available in every template. Moving or duplicating a section re-validates every binding; unavailable ones must be re-pointed or reverted to static before the move completes. Rationale: Ghost compiles without strict mode, so an out-of-context binding renders empty with no error at build, deploy or runtime
- (decision) FR-H8 NEW (final text agreed): empty-value handling. Every bound prop compiles inside a guard, unguarded state unreachable. Text bindings FALL BACK to the static value the prop held before binding; media bindings HIDE the element, guarding the element never the attribute. Mirrors Casper/Source behaviour (14 fallback chains found; they substitute where an alternative exists, hide where none does). Guards use {{#if}} exclusively - {{#has}} is not a null check and reports false for numeric/boolean values. Media guards must enclose any srcset: an unguarded srcset renders a malformed attribute the browser resolves as a relative URL, producing live 404s
- (decision) Appendix B.1 NEW: Template Context Matrix, normative, generated from research-ghost-binding-contexts.md - the table FR-H7 points at
- (decision) Research files FOLDED IN as normative companions with status frontmatter: research-ghost-binding-contexts.md (source of truth for FR-H7 + Appendix B.1) and research-ghost-empty-values.md (evidence base for FR-H8). 178 and ~90 citations respectively, verified against Ghost v6.58, Casper 5.12.1, Source 1.7.1, GScan 6.4.2, v5.130.6-to-v6.0.0 diff
- (decision) NEW 7.6 VERIFY-AT-BUILD ITEM: stand up a scratch Ghost install and empirically confirm the four docs-vs-code conflicts found in research - chiefly @even/@odd parity, which is documented backwards (code makes the FIRST item odd) and would invert zebra striping across the whole library. Nothing in either research file was executed at runtime; all claims derive from source, theme code, GScan rules or docs
- (decision) NOTE for PRD: Ghost docs claim {{#foreach}} shows public posts only; the code special-cases posts to 'all', so members-only and paid posts DO appear in public feeds by default (cards show, content stays gated). Ghost's decision, not a leak - but must be documented so it does not read as a paywall failure
- (decision) CORRECTION to earlier session assertion: on post.hbs/page.hbs the top-level context is a WRAPPER, not the resource - {{title}} at top level renders EMPTY, {{#post}} must be opened first (both official themes do this on line 4/7). List templates are the opposite shape, with posts and pagination flat at the root
- (event) Critical batch (13) fully triaged and closed. Moving to High batch (41)
- (decision) HIGH Group A (Ghost platform truths) ALL ADOPTED as recommended: H3 FR-C2 auto-probes hostSettings.limits.customThemes with fallback to asking; H4 connect-time validation calls an AUTHENTICATED endpoint (/config/) not the unauthenticated /site/; H5 FR-I4 rewritten - guided card is the only upload path, verification is a byte comparison of live routes.yaml, 'Routes unverified' limbo state deleted; H6 verify whether the Staff token lifts GET /themes/ before scoping the collision claim (added to 7.6); H7 snapshot re-upload acceptance added to 7.6 with a designed fallback when Ghost rejects it; H8 no-recapture rule keys on the package.json marker not the theme name
- (decision) HIGH Group B (missing specs) ALL ADOPTED, with H11 DEVIATION: owner authors ALL ~30 font pairings now rather than cutting to Appendix D's 12. Also H9 author control sets for the 219 undeclared variants (A17-A34); H10 author the string catalog keys + English defaults as a normative appendix; H12 author the 8 field-to-NQL mappings and DROP relative dates (not expressible in NQL); H13 scope the gscan message mapping to the reachable shortlist; H14 delete the never-deleted-vs-no-longer-resolves contradiction and scope to failed migrations
- (decision) HIGH Group C (section library structure) ALL ADOPTED: H15 add bindingContext/compileTarget to the FR-G3 registry, Shuffle and Site Remix cycle only within matching values, Picker filters on it (shares the FR-H7 matrix); H16 FR-G2 restated as 2 Free variants per compile target, A30 #11 / A29 #7 / A31 #10 re-tiered Free, 68 Free becomes 71; H17 channels added to FR-H2's paginated-context list; H18 FR-D6 switcher gains a conditional Private canvas mirroring members
- (decision) HIGH Group D (translations) ALL ADOPTED: H19 exactly one locale file named for the project language containing defaults overwritten by overrides, en.json identical when language is English; H20 JS-consumed strings emitted as data-i18n-* attributes on mount elements, {{t}}-only rule scoped to .hbs output; H21 FR-J14 compatibility contract extended to the catalog - keys append-only, never reworded in place, superseded keys ship a migration map; H22 {{t}}/catalog/locales block added to Appendix B; H23 RTL language-code list with blocking acknowledgement + pre-deploy warning
- (decision) HIGH Group E H26 ADOPTED: verify whether Dodo sends the annual renewal notice as merchant of record; if not it becomes FR-P1 email #6 with an explicit FR-P2 carve-out. Statutory in several target markets (California ARL, EU/UK)
- (decision) HIGH Group E H27 ADOPTED: map FR-L2 to Dodo's real event names, detect cancellation from subscription.updated plus a direct subscription read, state that the 7-day grace is Inflozo's window reconciled against Dodo's on_hold rather than assumed equal
- (decision) HIGH E H25 OWNER POLICY (replaces recommendation): NO refunds. NO mid-cycle cancellation. Cancel = stop auto-renew; access continues to end of billing cycle, then downgrades to Free and enters the existing block-until-user-deletes flow (P2 decision) forcing the user to delete over-limit projects/assets. Inflozo never auto-deletes. NOTE flagged to owner: chargebacks still occur regardless of refund policy, so FR-L2 still needs a dispute-opened transition; and a no-refund policy needs a lawful implementation at checkout (explicit consent to immediate delivery + waiver of withdrawal right) in EU/UK markets
- (decision) HIGH F H30 OWNER DECISION: DO NOT measure Lighthouse. Drop the Lighthouse targets from NFR-2 and G4 entirely rather than naming a host. Replacement proposed: assert the things Inflozo controls and can test in CI without a host - srcset/sizes present and correct, fonts subset + preloaded woff2, no render-blocking JS, a CSS budget per template, no layout shift from unguarded elements
- (decision) HIGH F H31 OWNER DECISION: de-prioritise accessibility. Generated-theme appearance is the user's responsibility; Inflozo's own UI optimises for beauty over a11y compliance. CONSEQUENCE flagged: the PRD's WCAG AA claim on presets/defaults (v2.2 M9) and G4's Accessibility >= 95 must both be REMOVED, not merely unenforced, or the PRD asserts something untrue that would reach marketing
- (decision) HIGH F H32 RESOLVED BY H3: Preview-only is not an Inflozo preview feature - it is Inflozo's state for a connected Ghost(Pro) Starter site, which Ghost forbids from custom theme uploads, so the user can design but not deploy. T4 is the Starter test site verifying the block. Once H3's automatic probe is adopted the flag both sets and clears automatically, giving a testable second clearing path and removing the user-declared-plan question entirely
- (decision) HIGH Group G (ownership gaps) ALL ADOPTED: H33 FR ranges added to every epic line; H34 GDPR export gets an FR or names Supabase tooling as the mechanism; H35 UX delegation list extended with the five designed flows and their FR anchors; H36 undo-journal clearing scoped to hydrates that supersede the local doc; H37 token list + component inventory produced for E1.S3 (UX pass owns it); H38 thumbnail capture stated as client-side from the canvas; H39 one clause in FR-A5 on subscription state at soft-delete and restore; H40 section 1.5 rewritten to what is verifiable and dated, with a re-verify-at-publish rule for any competitor claim reused in marketing
- (decision) H25 CLOSED: both guards implemented alongside the no-refund policy - (1) FR-L2 gains a dispute-opened transition (pro_active -> free, immediate, not at period end) because chargebacks occur regardless of refund policy and currently leave status pro_active with the full term remaining; (2) checkout gains explicit consent to immediate delivery plus waiver of the withdrawal right, so the no-refund policy is lawfully enforceable in EU/UK; confirm with Dodo whether they already handle this as merchant of record
- (decision) H30 CLOSED: Lighthouse targets removed from NFR-2 and G4. Replaced by CI-testable assertions over what Inflozo controls, no Ghost host required - srcset/sizes present and correct on every image, fonts subset + preloaded + woff2 only, no render-blocking JS, a CSS budget per template, no layout shift from unguarded elements (already prevented by FR-H8 guards)
- (decision) H31 REVERSED BY OWNER (supersedes the previous de-prioritise decision): every pre-made section variant MUST be WCAG 2.1 AA compliant, SEO requirements implemented, plus both facilitator suggestions adopted (alt attributes + heading order in generated themes; iframe focus model paragraph in 7.3). The PRD's AA claim and its marketing use therefore STAY and are strengthened
- (decision) H31 implementation: NFR-5 gets a named tool (axe-core), ruleset (WCAG 2.1 AA), and scope (all 487 pre-made variants + the Inflozo app). Runs on the EXISTING NFR-6(a) render matrix - the 487 x 3 packs x 2 modes x 3 viewports renders already exist, so axe bolts onto the same renders at near-zero marginal cost. G4's 'Accessibility >= 95' score is replaced by a pass/fail: zero axe-core violations at WCAG 2.1 AA on every shipped variant. Lighthouse stays dropped - axe-core is the correct a11y tool and Lighthouse's a11y score is a shallow subset. M9's transfer of responsibility for USER token edits is retained, with a live contrast checker added to the colour picker so users are warned before they break it
- (decision) H24 FACILITATOR DECISION (owner delegated): Appendix F adopts a stated plan-mix assumption of 60 percent yearly / 40 percent monthly. Blended net approx $12.61/mo per Pro customer (yearly approx $11.81, monthly approx $13.82), which is 8.8 percent below the $13.82 currently assumed. Break-even moves from the stated 8-9 to 9-10 against the existing $105-125/mo fixed cost, BEFORE the Test-environment cost the M15 split introduced. Sensitivity stated explicitly: the full range from 100 percent monthly to 100 percent yearly is only plus or minus 8 percent, so the mix assumption is NOT the model's dominant uncertainty - the uncosted Test environment is. Appendix F must state the Test set's actual monthly cost as a line item rather than folding it invisibly, and the mix assumption is flagged for revisit after 3 months of real checkout data
- (event) High batch (41) fully triaged and closed. Moving to Medium batch (50)
- (decision) MEDIUM batch (50) ALL ADOPTED as recommended, no deviations. M1/M2/M3 were already closed by earlier decisions (H3 probe deletes the Preview-only declaration step; C8 moved the notifications table to E1; the partial model defined 'instance group')
- (decision) MEDIUM Group A - financial model: M4 relabel the 1000-Free/200-Pro mix as a capacity-headroom scenario not a revenue projection (it implies 16.7 percent paid share vs G9's 3 percent); M5 recompute Appendix F with Test as permanent, as its own line item; M6 correct stale Ghost(Pro) prices (Starter understated ~60 percent); M7 put a real churn number in Appendix F (e.g. 25 percent month-1, 4 percent steady-state) and restate G10 as steady-state from month 2; M8 add an 'Assumed, unvalidated' block under 7.6 listing every behavioural/market figure with its basis, even if that basis is 'founder estimate'
- (decision) MEDIUM Group B - goal coherence: M9 restate G2 as 'from an existing connection' so it no longer contains G1 (currently leaves 5 minutes for everything after connect); M10 make the 487 microcopy count-agnostic ('hundreds of gorgeous sections') and restate G5/FR-G1 as '487 at GA, grows post-launch per FR-J14'; M11 give the play loop one gate in E5's exit - content preserved across N shuffles plus a full re-roll on the 40-section fixture, zero prop loss, testable without telemetry; M12 FR-G5 uniqueness gets a MECHANICAL proxy (no two variants in a category share a structural descriptor tuple), same treatment for FR-C7 variety (proposals share no variant id in an equivalent slot)
- (decision) MEDIUM Group C - Ghost platform: M13 rewrite all four 'safe installs' locations against the verified Staff-token capability once H6 verification lands, not against the old hedge; M14 bind snapshots to site record id, give orphans a TTL, state the overwrite rule; M15 keep accepting 5.x CONNECTIONS but reframe T3 as a compatibility target and the first candidate to drop under schedule pressure - Ghost 5.x reached EOL January 2026; M16 enforce a theme-size budget surfaced pre-deploy, probe real host limits and set below the lowest observed; M17 batch and de-duplicate client-side Content API reads with a per-session ceiling, fall back to sample content on 429 with a named cause
- (decision) MEDIUM Group D - synthesis and emission: M18 one clause in FR-J1 and one in FR-I1 invoking the Synthesis Defaults (an untouched template has no doc, so without this the v2.1 C2 fix is spec-only and the compiler emits nothing); M19 narrow FR-D6 to the six synthesizable templates - it currently contradicts its own companion on members/private/custom and re-breaks the confetti moment; M20 state the general emptying rule - emptying returns a template to untouched, standard templates re-synthesize, conditional templates stop emitting; M21 bind the error numeral and message to the error context in every A31 variant and re-describe #5 as a treatment not a status code
- (decision) MEDIUM Group E - translations and settings: M22 store language on the PROJECT (defaulted from the site at first link, always editable), warn on site switch when locales differ; M23 warn at Style Pack switch when the accent is promoted and surface promoted-token state in the Style panel (promotion silently disarms the pack switcher on the live site); M24 add the Translations gating row to Appendix F
- (decision) MEDIUM Group F - data model: M25 DEMOTE 7.5 to illustrative, the Architect owns the full schema, and add a schema story to section 8. Absorbs M26 deploy_jobs (plus Vercel function duration/memory), M27 asset-usage tracking with the stated staleness consequence and delete re-verifying against the live doc, M28 an export record, M29 the admin claim provisioning path, M30 a per-site advisory lock for deploy+activate
- (decision) MEDIUM Group G - compiler mechanics: M31 state the control-to-CSS mechanism in 7.3 (closed-valued controls make data-attribute selectors viable and are almost certainly the intent, but that must be written not inferred); M32 name the section CSS authoring format and explicitly exclude it from the app's Tailwind pipeline; M33 state the rich-text engine, storage format and sanitization point - facilitator inclination recorded as text-plus-marks in a JSON structure, which fits the annotated-HTML compiler, avoids emitting unescaped user HTML, and makes the M13 mark-stripping rule trivial
- (decision) MEDIUM Group H - epic and story mechanics: M34 name the five pilot sections in section 8, chosen to span the hard cases (site-wide singleton, dynamic feed, members-aware); M35 define E1's dashboard-skeleton cut line alongside the H33 FR assignment; M36 assign FR-H2's main-feed designation lifecycle to E5; M37 story granularity rule stated as ONE STORY PER CATEGORY (34), not per variant (487)
- (decision) MEDIUM Group I - test gates: M38 state NFR-1's frame criterion, define 'warm', and declare the perf gate MANUAL-ONLY against the reference laptop (not reproducible on CI runners); M39 FR-D14 lockup bounded as no main-thread block over 5 seconds; M40 'docs complete' points at FR-N4's enumerated pages; M41 name which of the four automated gates actually run against live Ghost (only two do); M42 align FR-N2's sandboxed gallery iframe with 7.3's same-origin canvas - simpler now that C10 removed untrusted HTML; M43 exempt the section 4 test targets from FR-J11's 10/hour rate limit (30 serialized deploys would take 3+ hours per DoD run)
- (decision) MEDIUM Group J - naming: M44 rename FR-J13's heading, P8 and E7 to the glossary's 'pre-Inflozo snapshot' - the surviving heading names the activation-time trigger the hardening pass rejected and would reopen the deploy-only bypass; M45 rename section 4's label to 'Production-infrastructure testing (pre-launch)' and add Test/Live environment rows to the glossary, so a post-launch reader cannot point the E2E suite at customer data; M46 unsynced-EDIT count is canonical (it is what the user-visible string promises), state the ops-to-edits mapping in AD2; M47 name the two read-only states distinctly (locked elsewhere vs plan-locked) and glossary both
- (decision) MEDIUM Group K: M48 FR-K2 uses the NATIVE WebP path with the encoder as fallback and corrects the stated reason - Safari has supported canvas.toBlob webp since well before the stated browser floor, so the PRD as written ships ~300KB of dead wasm; M49 one line in section 4 - suggestions, votes and the dogfood project are exported from Test and imported into Live at cutover, everything else starts empty (otherwise the public board resets to empty on launch day)
- (event) Medium batch (50) fully triaged - all adopted as recommended. 104 of 135 findings decided. Moving to Low batch (31)
- (decision) C7 SPIKE RUN AND PASSED (2026-08-18): option D empirically validated. 16/16 renderer assertions pass; the generated theme scores 0 errors 0 warnings on gscan 6.4.2 against BOTH the v5 and v6 specs. Artifacts at spike-compiler/ (sections/, compile.js, test.js, build.js, theme-output/, README.md) - runnable, re-runnable, folded in as a normative companion
- (decision) C7 spike proved the risky mechanic: Handlebars expressions containing quotes survive HTML serialization intact inside attributes via opaque ASCII tokens swapped after serialization. Block helpers inject as HTML comments carrying tokens. Repeating elements extract to real partials invoked with NO parameters because foreach supplies the context
- (decision) C7 spike DESIGN CONSTRAINT discovered: comment-wrapped markers must be unwrapped BEFORE token substitution. Substitute first and a marker Inflozo inserted becomes indistinguishable from a comment the section author wrote. Must be stated in the compiler spec, not left to the implementer
- (decision) C2 CONFIRMED EMPIRICALLY: omitting engines.ghost-api removes the standing gscan warning and the 0-errors-0-warnings gate IS reachable - it was unreachable only because the PRD mandated emitting that key. GS110 is silenced by gating page.hbs on @page.show_title_and_feature_image, exactly as the C2 fix specified. The v2.1 critical is now closed with evidence, not argument
- (decision) C7 SCOPE CLARIFICATION (owner): the owner writes NO section code. All 487 sections are authored by the dev team as project work under E9-E11. This reinforces option D - annotated HTML is the most approachable authoring format for a dev team, and section files are directly viewable in a browser during authoring. The authoring format and its directive vocabulary must therefore be DOCUMENTED as a deliverable, not left tribal
- (decision) C5 CLOSED 1(a): A32's 12 paywall designs SURVIVE as selectable designs inside the new paywall template editor. Nothing is cut; they move out of the section library and become non-placeable treatments alongside A33 Koenig card treatments and A34 pagination styles
- (decision) C5 CLOSED 2(b): the generic CTA is a new CONTROL (show to: everyone / logged out / free members / paid members) added to EXISTING CTA sections, not a new category. Adds zero new variants and makes the whole existing CTA range member-aware
- (decision) C5 ARITHMETIC CONSEQUENCE: 487 total is UNCHANGED (nothing cut, nothing added), but the placeable/non-placeable split moves - A32 (12) joins A33 (6) and A34 (10) as non-placeable, so 28 of 487 are treatments rather than placeable sections. Every requirement quantifying over 'every variant' (Section Picker, Shuffle, FR-G5 uniqueness, render matrix, E14 gallery) needs the carve-out clause from LOW-12, now covering three categories. Synthesis Defaults post stack drops A32. E9-E11 wave counts need re-checking against the new grouping
- (event) NEW FINDING during encode, verified 3 ways (research file citing Ghost services/rendering/templates.js; Lyra provenance of the stale docs example; gscan 6.4.2 spec list): Ghost 6 has NO members template family. members/signup.hbs, members/signin.hbs and members/account.hbs do not exist and never load. A30's 15 variants, FR-D6's Members canvases, FR-I1's conditional members emit, 7.4's tree, the Synthesis Defaults NEVER-synthesized list, FR-L3's Free-variant remediation and the v2.1 H1 starter decision are all built on a false premise. Ghost 6's actual mechanism is a routes.yaml route to custom-{name}.hbs. Encode agents instructed to LEAVE all members-template text untouched pending an owner decision; H16's A30 #11 re-tier withheld so Free lands at 70 not 71. NOT one of the 135 findings - discovered during encode
- (change) ENCODE COMPLETE: prd.md v2.3 (593 lines) -> v3.0 (970 lines). All 135 validation findings plus ~20 architectural decisions encoded across 6 agents. sections-inventory.md rewritten (A17-A34 control sets, C5 paywall move, non-placeable carve-out). addendum.md rewritten (AD1 supersession test, AD2 edits canonical, new AD4 defaults-not-requirements). Two new normative companions authored: appendix-h1-string-catalog.md (131 keys, 29 JS-consumed, 3 locked) and appendix-b1-template-contexts.md (18 template rows x 3 scopes). v2.3 backed up at .v2.3-backup/
- (change) POLISH COMPLETE: structure-then-prose lenses applied to prd.md (21 structural + 18 prose fixes) and to the three companions (10 fixes). Verified by before/after token diff: zero FR/NFR/G/P/E/T identifiers renumbered, zero numeric tokens changed, zero modal verbs changed, zero cross-references removed
- (change) POLISH CAUGHT A REAL DEFECT: the encode had orphaned FR-I2's enumeration - three inserted normative sentences split 'rich filter builder' from 'and drag-to-reorder collection precedence', leaving the collections/channels/custom-routes/taxonomy list dangling mid-sentence. Repaired with all mappings and qualifiers preserved
- (change) FINAL FIXES applied by facilitator after polish: FR-D16 golden-fidelity reference retargeted to NFR-6(c); NFR-3's absolute codeinjection ban distinguished from the presence-check that makes 1.2's notice possible; 7.2's routes-upload-attempt corrected to read-only; 1.4 #5 snapshot trigger corrected from activation to first upload; 7.6 orphan assumption row removed; FR-E1 ~30 fixed to exactly 30; FR-D8 preview-only toggle disambiguated from the defined Preview-only connection state; companion manifest gained appendix-b1 and appendix-h1
- (change) VERIFIED FINAL STATE: 119 FR definitions, zero duplicates, zero gaps, zero undefined references. Appendix A matches sections-inventory.md on all 34 rows (ID, name, count, group). 487 total = 47+199+109+104+28. Waves 156/199/132. Free 70 (A30 hold). Placeable 459 + treatments 28. Story counts 10+13+11=34. Render matrix 487x3x2x3=8766. Appendix D file tally reconciles over 30 pairings. All 119 FRs owned by exactly one epic across E1-E14
- (change) OPEN - ONE ITEM ONLY: the members-template question. Ghost 6 has no members template family; FR-I1 and appendix-b1 now disagree in print about the compile target. Held in prd.md at FR-D6/D13/G2/H6/I1/I5/O1, 7.4's tree, Appendix A's A30 row and Appendix E; and in sections-inventory.md at A7/A30/Synthesis-scope/NEVER-synthesized. A30 #11 re-tier withheld so Free is 70 not 71. Owner options: (1) members pages become custom templates via routes.yaml to custom-{name}.hbs with A30's 15 variants as designs for that surface, or (2) cut to Portal and drop 15 variants from 487
- (event) PRD v3.0 encode and polish complete; one open item (members templates) pending owner decision
- (decision) MEMBERS-TEMPLATE DECISION (owner): Option A - membership pages become ordinary Ghost Pages with a designed template, NOT a members/*.hbs family and NOT necessarily a routes.yaml route. A30's 15 variants are re-pointed at that surface. Owner instruction: research thoroughly before baking so that it actually works
- (decision) Facilitator correction to the options as presented: Option B was described as requiring routes.yaml, which is only true for a URL with no Ghost Page behind it. For a real Page, a theme can ship custom-{name}.hbs and the user selects it from the Template dropdown in Ghost's page editor - no routes.yaml. That is likely MORE robust than page-{slug}.hbs, which silently stops applying if the user renames the page. Research commissioned to settle page-{slug} vs custom-{name}-via-picker vs routes.yaml
- (decision) Research commissioned: research-ghost-membership-pages.md - template resolution order, how custom templates are surfaced and labelled in the editor, render context, the exact data-portal attribute set a designed page must emit, whether Ghost serves any native member URL a theme can influence, tiers/prices on a page, members-disabled and restricted-signup behaviour, reserved slugs, what real membership themes ship, and bluntly which of signup/signin/account are genuinely designable versus Portal-only
- (decision) FOUR AGENT-CHOSEN DEFAULTS CONFIRMED BY OWNER: (1) section CSS is plain flat CSS - no preprocessor, no nesting, no build step; (2) N=20 in the E5 play-loop gate; (3) E14 retains Lighthouse >= 95 for the MARKETING site on Inflozo's own host, which does not conflict with NFR-2's no-Lighthouse rule for generated themes on the user's host; (4) the FR-J11 rate-limit exemption is deliberately stated in both section 4 and FR-J11, accepted as duplication with a drift risk
- (change) MEMBERSHIP DECISION ENCODED AND VERIFIED. Mechanism: membership pages are ordinary Ghost Pages whose template is picked from Ghost's page-editor Template dropdown, compiling to custom-{name}.hbs at theme root. NO routes.yaml and NO Admin routing step - this REMOVES a manual step rather than adding one, retiring the v2.1 H1 starter finding on its own terms. page-{slug}.hbs explicitly forbidden: it is matched at render time so it detaches silently on retitle, it outranks the user's explicit dropdown choice, and Ghost Admin disables the dropdown when it matches
- (change) ACCOUNT SCOPE CUT ENCODED: 'design your account page' is not deliverable and is gone from the PRD. The account surface is display-only - every mutation hands off to Portal, there is no protected URL, no logged-out redirect, and server-rendering member PII is unsafe under Ghost's members-content caching. A30 #11 Account Panel + #12 Account Minimal List merged into one display-only Member Home; #15 Magic-Link Sent is not a page (Ghost routes no URL to it) and survives as the .success state of the on-page data-members-form carried by variants #1-#10
- (change) SIGNIN SEAM STATED: the signin form is themeable but the one-time-code step hands off to an undesignable Portal modal (data-members-otc, off by default, exposed as a control)
- (change) COUNT RECOMPUTATION: A30 15 to 13, Group 4 Template-Specific 104 to 102, TOTAL 487 to 485, E11 wave 132 to 130, render matrix 8766 to 8730, placeable 459 to 457 (treatments 28 unchanged), Free stays 70 (A30 #1 and #2 survive; the held third re-tier is moot). 34 categories unchanged; story counts 10+13+11=34 unchanged
- (change) DEFECTS FIXED FROM THE MEMBERSHIP RESEARCH: Appendix B listed Portal action 'upgrade' which has no branch in Ghost's code - corrected to account/plans and the verified set enumerated (Ghost's own Source 1.7.1 ships the broken value; called out as not to be copied). FR-F6's Upgrade action corrected. Appendix B's tier include= corrected as a documented no-op. NEW RULE: tier presence never implies purchasability - Ghost seeds an active $5/mo tier at install, so every paid CTA compiles inside {{#if @site.paid_members_enabled}} and tier queries filter type:paid+visibility:public
- (change) NEW CONSTRAINT IN 7.4: custom template filenames are a FROZEN PUBLIC API and are the one exception to the layer-names-become-filenames rule. Ghost stores the chosen template string on every page selecting it and never repairs it, so a section partial may be renamed freely but a shipped custom-*.hbs may never be
- (change) FINAL VERIFICATION PASSED (facilitator-run, independent of the encode agents): 119 FR definitions with zero duplicates, zero gaps across all 17 letter blocks, zero undefined references. 34 categories summing to exactly 485. Appendix A matches sections-inventory.md on every ID, name, count and group. 70 [Free] = 68 base + the A29 #7 and A31 #10 re-tiers. Zero stale counts (487/104/132/8766/459) anywhere in prd.md. All seven normative companions present, stamped with status frontmatter, and referenced from the PRD preamble
- (event) PRD v3.0 COMPLETE - all 135 validation findings, ~20 architectural decisions and the membership correction encoded, polished and verified. Zero open items
- (event) STRESS TEST v3.0 opened (Validate intent). Owner axes: (1) deep Ghost CMS/theme/data research, (2) every pre-made section detailed enough to build, (3) exported theme code quality highest, (4) canvas is truly WYSIWYG. Six reviewers dispatched: rubric, ghost-truth, section-buildability, theme-quality, wysiwyg-fidelity, adversarial-regression
- (event) STRESS TEST v3.0 RESULT: 175 findings, 23 CRITICAL / 61 HIGH / 63 MEDIUM / 28 LOW, grade Poor. Six reviewers. Arithmetic ledger reconciled (119 FRs, 34 categories, 485 variants, 70 Free, waves 156/199/130, matrix 8730) - scope integrity held, unlike v2.1
- (event) NEW FAILURE PATTERN (v3.0): prior REVIEWERS' unverified assertions entered the PRD with the authority of research. FR-I2's 'NQL has no relative-date syntax' traces to review-st-buildability.md:222, was never checked against NQL, and deleted a working feature. Same for FR-I2 per-collection limit and FR-I4 routes-upload. Rule for future rounds: a reviewer claim about an EXTERNAL platform is a hypothesis until verified against source, never a finding to encode
- (event) CONVERGENT FINDING (3 reviewers independently): the research companions are MORE accurate than the prd.md body. Every spot-check of research-ghost-membership-pages.md and research-ghost-binding-contexts.md matched current Ghost source exactly. Regression risk lives in the DISTILLATION step, not the research step
- (event) EMPIRICAL: gscan gate proven near-worthless as a quality bar - a deliberately terrible theme (no lang, no viewport, no alt, inline onclick, scrambled headings, 1.5:1 contrast, missing templates) scores 0 errors 0 warnings on both v5 and v6, while real Casper and Source each FAIL a rule with an error. NFR-7's gscan-clean gate certifies almost nothing about the 'marketplace-quality' claim
- (event) EMPIRICAL: spike-compiler reproduced exactly (16/16, gscan 0/0 v5+v6, theme-output byte-identical to fresh build) BUT contains two undetected defects - compile.js:36 canvas date renderer discards the format arg (canvas 2026-03-14 vs theme 14 Mar 2026) and compile.js:154 wrapGuard emits {{#if YYYY}}. Both invisible to test.js because its assertions compare class skeletons and its date helper is a passthrough stub. The spike proves MARKUP agreement, which is narrower than the 'canvas is the page' claim resting on it
- (event) Facilitator-verified independently: 119 FR definitions (rubric reviewer's 116 was a miscount), contiguous across 17 blocks, zero dupes, zero dangling refs, 119/119 owned by exactly one epic Owns clause; 19 of 34 category headers declare no Data: line; stale 487 in two NORMATIVE companions (appendix-h1:287, appendix-b1:267); sections-inventory self-contradicts on Portal action 'upgrade' (:421/:588 offer it, :557 says it is not a Portal action)
- (event) Consolidated stress-test report synthesis dispatched
- (decision) D1 OWNER OVERRIDE (better than the facilitator recommendation): do NOT make declaration conditional. The theme ALWAYS reads all three dark built-ins with real fallback logic baked in - dark_logo falls back to the wordmark, dark accent falls back to the light accent, color_scheme drives a body class. Because the template always references each key, GS100 can never fire, and the setting works the moment a user fills it in. One mechanism in the theme instead of a compiler special-case
- (decision) D2 CONFIRMED: pin a normative image_sizes table in FR-J2 (proposed xs150/s400/m750/l1200/xl2000) + compile assertion that every size= used exists as a key; NFR-6(c2) recordings must be made against an Inflozo-emitted package.json
- (decision) D3 OWNER RULING: skip screenshots entirely. Code quality and functionality are what matter for the marketplace, not submission artifacts. Consequence: reword the quality bar so it does not hinge on marketplace SUBMITTABILITY
- (decision) D4 OWNER RULING: BOTH - reword 1.2's promise to the true markup claim now, AND rebuild NFR-6(c3) with an exclusion set, region-scoped threshold and a rotating fixture cohort; fund it by shrinking the 8730 render matrix
- (decision) D5 CONFIRMED: add an E0 verification spike ahead of E4 that executes all 14 of 7.6's verify-at-build items against a real Ghost, plus an owning-epic column in the 7.6 table
- (decision) D10 CONFIRMED: add the five missing data-* constructs ({{#get}}, member gating, {{t}}, srcset, control->data-attr write), add a third worked spike example exercising all five, relabel 7.3 as 'designed with a proven core' not 'validated'. E4 exit criteria
- (decision) D12 OWNER RULING: the canvas dummy post/page content IS a style-guide page showing every Ghost editor card, so users can see and design the card treatments. Merges cleanly with D11's per-card design module
- (decision) D14 CONFIRMED: name Ghost's own injected surfaces in 1.2's carve-out AND shim Portal's floating button on canvas (ask at connect if the setting cannot be read); announcement bar, comments and search carve out
- (decision) D11 OWNER SCOPE ADDITION: a dedicated module to design EVERY Ghost editor (Koenig) card - toggle, bookmark, audio, file, video, image, gallery, and the rest. Inflozo emits CSS that reliably overrides Ghost's. Research commissioned on the correct override mechanism (card_assets false vs exclude vs specificity vs load order) rather than defaulting to !important
- (decision) D13 OWNER RULING: canvas geometry must follow Webflow/Elementor practice and be judged on UX, not implementation cost. Research commissioned into what those editors actually do, incl. Gutenberg's documented iframe migration rationale, chrome-positioning technique and how device preview gets a genuinely mobile viewport
- (decision) D9 OWNER PUSHBACK on all six contested variants, plus a proposal to store an ADMIN API KEY in a Ghost theme custom setting (costing one of the 20 slots). Facilitator flagged the admin-key proposal as a security stop BEFORE research: admin keys sign JWTs granting full site control and custom settings are not a secret store. Research commissioned to settle the blast radius and to establish which of the six are reachable via the PUBLIC Content API (which is browser-safe and already used) rather than the Admin API
- (decision) D9 RESOLVED BY RESEARCH - ALL SIX VARIANTS SURVIVE, count stays 485 (not 483). Admin API key CONFIRMED unsafe and CONFIRMED unnecessary: the Admin Integration role carries member:all, member_signin_url:read and theme:all (site takeover), Handlebars has no server-side fetch so a theme can only use the key by PRINTING it into the page, and Ghost's Admin CORS allowlist includes the site's own hostname so the exploit works. Custom-setting cost across all six: ZERO, because Ghost ships {{total_members}}, {{total_paid_members}}, {{content_api_key}} and {{content_api_url}} as core helpers - all four verified present in current Ghost source by the facilitator
- (override) STRESS-TEST REVIEWER ERROR, caught by owner pushback: critical finding #18 claimed member counts are Admin-API-only and never in template context. FALSE - {{total_members}} is a core keyless server-side helper (verified in source, rounds down and appends +). A7 #12 Founding Member KEEPS as designed. This is the SECOND reviewer platform-assertion in this session that failed verification, and it is the same failure class the stress test itself identified. Reinforces the standing rule: a reviewer claim about an external platform is a hypothesis until read in source
- (decision) D9 per-variant: A7#12 KEEP via {{total_members}} (copy must treat the value as a string, Ghost rounds down with a + suffix). A32#10 KEEP per owner's clarified intent - blur the LAST VISIBLE free-preview block via a theme-supplied partials/content-cta.hbs wrapper plus a mask-image gradient, since Ghost emits preview blocks bare with no wrapper. A32#12 REDEFINE as an approximate tease - reading_time is computed BEFORE gating so it reflects the full post, but is coarse to +/-137 words, so 'About X%' bucketed to 5%, hidden under 3 min, server-rendered no-JS baseline. A29#13 REDEFINE as a progressive filter strip - server-rendered tag/author links, JS refetch via Content API, limit caps at 100, sort only on real schema columns. A27#11 REDEFINE as client-side Fisher-Yates over a <=100-post pool, honestly 'random from the 100 most recent', degrades to a latest-posts row - OWNER RULING PENDING on renaming since 'Discover Shuffle' overclaims. A19#11 REDEFINE as Excerpt Quote from custom_excerpt only, never {{excerpt}} whose 500-char plaintext fallback yields a mid-sentence fragment in quote marks
- (decision) BATCH APPROVED (8 mechanical criticals): always emit en.json; restore NQL relative-date filtering; routes.yaml auto-upload becomes primary path; fix wrapGuard guard-field derivation + add helper-bound test; engines.ghost >=5.0.0; CSP frame-ancestors self; close 7.6 item 9 with citation + risk row; GDPR purge proceeds on the 14-day deadline regardless of snapshot download
- (decision) Q1 OWNER OVERRIDE of facilitator recommendation: choose FULL PER-VARIANT SPECS for all 485 sections, not the 34-header union pass. Rationale: every section must carry all its data and be properly planned; take as much time as needed; sections are perfected BEFORE launch. Order: Heroes (A4) first, then onward. TWO CONSEQUENCES RECORDED: (a) the category-level union header must STILL exist underneath as the shared content model, because FR-D17's zero-prop-loss shuffle gate stores props against it - per-variant specs sit ON TOP of the union, they do not replace it; (b) 'all sections done before we go live' converts E9-E11 from waves into a hard launch gate and changes section 4's DoD and the epic sequencing
- (decision) Q2 CONFIRMED: write the behavior-module rulebook. JS ships WITH its section and is removed when the section is removed. Each module is authored alongside its section during the Q1 per-variant spec pass, not as a separate module project
- (decision) Q3 OWNER RULING: NO hard JS size cap. NFR-2's CI-enforced <40KB gate becomes a WARNING threshold, not a build failure. Threshold value still to be set
- (decision) Q7 rename Discover Shuffle to match what it does; Q8 card_assets exclude per designed card (never !important); Q9 vendor Ghost's 416 lines of MIT card JS for audio/video/gallery/toggle; Q10 ~20 card panels, no colour controls on header_v2/signup/cta which write inline per-post colours; Q11 viewport-sized iframe scrolling internally; Q12 split chrome - outlines/badges as element pseudo-elements inside, only the floating mark toolbar outside; Q13 two-axis real device resize plus a separate cosmetic zoom layer; Q14 disclose the desktop-mode 100vh canvas-vs-visitor gap in 1.2's carve-out; Q15 re-examine FR-I5 now that routes auto-upload lands
- (decision) Q17 OWNER RULING: walk ALL 50 high findings with the owner in the same numbered layman format (not the facilitator-filters-first option). Q18 OWNER RULING: owner reviews the 79 medium/low findings BEFORE they are applied. Q19: sequencing approved - batch fixes, then the three restored features, then sections, then gates
- (event) Q4/Q5/Q6/Q16 sent back for deeper layman explanation before ruling
- (decision) Q4 OWNER RULING: option 3 - behaviours OFF while designing, live behind a Preview toggle, PLUS each module declares whether it is edit-safe (sticky-shrink, animate-on-scroll, tabs, accordion run always; marquee, typewriter, rotator, countdown, confetti wait for Preview). Roughly 1.2x the per-module cost of the off-always design instead of 2-4x for full-live
- (decision) Q5 SETTLED (owner): layout = variation = design, ONE term. The picker selects among a section's designs (Reading A) - it is NOT a sub-arrangement within one design. Navigation is [ and ] keys, Previous/Next design arrows, or clicking a thumbnail in the sidebar. VERIFIED: the design directory already ships this - 'Previous design'/'Next design' appear in S4 Editor and S6 Variant Shuffle, 'Layout' in the Editor Sidebar Kit. Only prd.md was ambiguous. Recommend renaming the sidebar label from Layout to Design so it stops implying a sub-arrangement
- (decision) Q5 MAJOR CONSEQUENCE (new, owner-stated): CONTROLS ARE PER-VARIATION, NOT PER-CATEGORY. Each design of a section carries its own sidebar control set (number of posts, typography, image size...), kept MINIMAL per the design directory. This changes sections-inventory.md, whose Controls: lines are currently declared at CATEGORY level for all 34. It folds into the Q1 per-variant spec pass rather than adding new scope. Two things must now be written: (a) a control-carry rule for design switching - control present in both designs carries its value, present only in the old one is PARKED so returning restores it, present only in the new one takes its default; (b) FR-D17's zero-loss gate must be defined over CONTROL values as well as content props
- (decision) Q6 OWNER RULING: post content becomes a DEDICATED SECTION with a design picker, loaded by Ghost, giving control over drop caps, table of contents and its design. Applies to BOTH posts and pages - so it is placeable on post.hbs and page.hbs, not post-only as A25 currently is. Pairs with the D11 Koenig card design module and the D12 style-guide dummy content: all three are the post-body feature area and should be specified together
- (decision) Q16 OWNER RULING: option 1 - build the canvas-vs-real-Ghost comparison as proposed. Nightly rotating batch, all 485 designs compared within ~30 days, risk-weighted toward sticky/fixed, card treatments and member-state sections; exclusion regions defined (post body, Portal button, comments, code injection); animations frozen; one-click approval for intentional changes. Full 8730 render matrix moves from per-commit to nightly-and-before-release, with per-commit covering only designs that commit touched
- (decision) Q16 INFRASTRUCTURE (owner): the real Ghost target is a SELF-HOSTED Ghost on a DigitalOcean droplet, provisioned by the owner and dedicated to tests. Permanent cost line for Appendix F. SEQUENCING CONSEQUENCE: NFR-6(c3) currently lands in E15 hardening, but the new per-category gate needs real-Ghost comparison from the FIRST category, so the droplet and the comparison harness become a dependency of E9/E10/E11 rather than of E15. This front-loads the fidelity check, which is the correct direction given the v2.3 and v3.0 failure history
- (decision) NEW HARD GATE (owner): after each section CATEGORY is developed there is a blocking owner gate - the owner tests it personally, compares it against real Ghost, approves, and only then does the next category begin. 34 categories = 34 sequential owner gates. Converts E9-E11 from parallel waves into a gated sequential pipeline and makes the owner the explicit bottleneck by design. Owner also states they will eyeball sections manually in addition to the automated tests, which are complementary not a replacement
- (assumption) GAP RAISED BY THE DROPLET DECISION: a self-hosted droplet cannot exercise Ghost(Pro)-specific behaviour. The PRD's T4 target is a Ghost(Pro) STARTER site and is the only way to test the hostSettings.limits probe, the Preview-only connection state and the FR-C2 deploy-refusal path, because theme upload restrictions and plan limits exist only on Ghost(Pro). A Ghost(Pro) Starter account is therefore still required alongside the droplet, or the Preview-only path ships untested. Owner ruling needed
- (decision) GHOST(PRO) TESTING DEFERRED (owner): no Ghost(Pro) Starter target before MVP. Owner will test Ghost(Pro) plans once an MVP exists. REQUIRES A BLOCKING GATE - capture the real GET /admin/config/ hostSettings.limits payload from a Ghost(Pro) Starter site and verify FR-C2's Preview-only detection against it BEFORE any public launch or any marketing aimed at Ghost(Pro) users. Until that gate clears, the Preview-only path is written against an unseen payload shape and must be recorded as a KNOWN UNTESTED PATH, not an assumed-working one. Owner asked to be reminded - carried in project memory as well as here
- (decision) SHELL BLOCK CONFIRMED (owner): build the COMPLETE shell first, not a minimal one - all templates plus every header variation (A1, 16), every footer variation (A3, 16) AND every announcement bar (A2, 15). That is all 47 of Group 1 Structure and Chrome. The shell block lands before Heroes, and Heroes is then the first OWNER-GATED category
- (decision) Q3 SETTLED (owner): the JS size signal is DEVELOPER-FACING ONLY - the user never sees it in the editor. Warn at 50KB gzipped per page, flag loudly at 100KB per page. No hard build failure. Plus binding authoring constraints: NO jQuery; vanilla JS by default; a lightweight library only where vanilla is genuinely a bad idea (owner named Swiper for carousels as an acceptable example); libraries must be researched per section rather than chosen ad hoc; target modern browsers and versions in real use, with no support for negligible-share legacy versions
- (event) Research commissioned: research-section-js-libraries.md - the real ~30 module list from the inventory, vanilla-vs-library per module with measured gzipped sizes, the Swiper-vs-scroll-snap carousel question, budget arithmetic for a 12-15 section page against the 50KB threshold, a HARD PERMISSIVE-LICENCE filter (generated themes are redistributed and possibly sold by users, so GPL/AGPL/LGPL is disqualifying), a concrete testable browser-baseline policy and what it unlocks for the no-build-step section CSS, and a no-JS degradation line per module
- (decision) R1 OWNER RULING: single JS threshold of 40KB gzipped per page (keep NFR-2's existing number; supersedes the 50/100 the owner set earlier this session, at the facilitator's recommendation, so the PRD carries one number not two). Developer-facing warning only, no build failure. Context: a realistic 15-section marketing home measures 10.3KB and a maximal all-31-module page 17.5KB, so nothing realistic approaches the threshold
- (decision) R2 OWNER APPROVED: browser baseline is Baseline 'Widely Available', pinned via browserslist-config-baseline with widelyAvailableOnDate 2026-08-18, computing to Chrome/Edge 121, Firefox 122, Safari/iOS 17.2. Enforced by stylelint-plugin-use-baseline (runs on flat CSS with no build step), eslint-plugin-compat and size-limit. This baseline is what makes the zero-library result reachable and it governs what the 485 section designs may use in BOTH CSS and JS
- (decision) R3 ACKNOWLEDGED: CSS scroll-driven animations are NOT Baseline (no Firefox) so reading-progress and scroll-reveal use IntersectionObserver; popover is Newly-available only (needs iOS 18.3) so modals use native <dialog>. Both already assumed by the 31-module plan
- (decision) JS LIBRARY DECISION SETTLED: 31 modules (30 feature + 1 shared core), ALL VANILLA, ZERO third-party runtime dependencies in a generated theme; 22 libraries measured and rejected. 10 further behaviours need no JS at all (details element, :has(), server-side {{#if @member}}, CSS sticky). Swiper REJECTED on size - facilitator measured the v11 bundle at 41.9KB gzipped plus 3KB CSS, about 2.4x Inflozo's entire 17.5KB maximal runtime; scroll-snap plus ~1.2KB of vanilla wins and is the more literal reading of the inventory, whose carousel designs all say 'snap'. It also removes the cloned-slide aria-hidden failure class structurally. Conditional fallback if desktop mouse-drag proves essential: Embla 8.6.0, MIT, 7.16KB. LICENCE LANDMINE AVOIDED: typed.js relicensed MIT -> GPL-3.0 at v3.0.0 and GPL is now npm latest - facilitator verified against the npm registry; hard reject, it would have shipped GPL code inside themes users own and resell
- (event) CONTEXT FOR THE PERFORMANCE STORY: Ghost's own sodo-search measures 86.5KB gzipped - five times Inflozo's entire maximal section runtime, injected by Ghost, and not Inflozo's to remove. Inflozo's sections are not the weight problem on a Ghost site
- (decision) HIGH FINDINGS RULED (H1-H22). H1(3) both an automated emitted-theme code-quality gate AND the 34 manual owner gates. H2(2) the compiler emits correctly-indented code directly - no Prettier, which throws on Ghost partials and deletes the doctype. H3(1) STRIP DEAD CSS, and owner extends it: each VARIATION owns its own CSS, and removing a variation removes its CSS and its JS with it. H4 ADD THE SPIKE for FR-D4 rich-text mark emission, scheduled BEFORE the shell block since every user text path depends on it. H5(1 then 3) canvas previews a fixed fixture, overridden once a site is connected by whatever the user picks in the post list/grid control. H6(1) the canvas loads the same font subset the theme ships. H8(1) Tab Deck redefined visual-only. H9(1) Empty Tag State becomes a state of the tag feed. H10(1) structural descriptors, falls out of Q1. H11 extend Free-tier blocking to all 22 uncovered items. H12 author a custom-template naming and collision scheme NOW, it is unfixable after ship. H13(1) build the in-product membership-binding flow. H14 define the one true upload-succeeds/activate-fails behaviour. H15(3) both a graceful no-token path and the token path, and change the security promise to match. H16(3) defer two of the three credential steps until first deploy. H18(2) static placeholders for dashboard thumbnails in v1. H19(1) assign and gate G1/G2. H20 thicken the personas before the UX phase. H21 rebuild the cost model around authorship
- (decision) H22 OWNER RULING - GOVERNING CONSTRAINT: 'I am okay if it takes time. But it needs to be perfect and work flawlessly without bugs.' Quality and correctness outrank schedule. Consequence for the PRD: the solo-founder risk row is about TIMELINE, not about cutting scope - it must not be written as a prompt to descope. Appendix F's model must show a longer runway rather than a reduced library
- (decision) H7 + H17 OWNER RULING: responsive behaviour is not to be specified in prose - it needs DEDICATED DESIGN done in Claude Design. Facilitator to author a new Claude Design prompt covering (a) responsive variations of all section categories, (b) every page/surface missing from the existing design directory, and (c) the paywall editor surface from H17. Owner's intent: everything is designed beforehand, nothing improvised during build
- (change) DELIVERED: _bmad-output/planning-artifacts/design/claude-design-prompt-2.md (195 lines) - the H7+H17 Claude Design prompt. Companion to the original, which stays binding for the design system, voice and core components; prompt 2 wins wherever they disagree because it encodes decisions made after prompt 1 was written. Part A responsive system via 15 STRUCTURAL ARCHETYPES with three-frame collapse strips plus a bespoke-mobile exception list and 7 stress frames - this is the key move, since 485 x 3 breakpoints is 1455 frames but the library reduces to ~15 archetypes and responsive behaviour is a property of the archetype not the design. Part B 25 never-designed surfaces. Part C the post-body trio (card design module, post content section, paywall editor) plus the style-guide fixture, designed together because they are one surface. Part D corrections to existing frames including every stale count
- (decision) OWNER RULING (option 2, overriding the facilitator's recommendation of option 1): ALL 485 section designs are to be fully designed in Claude Design at three breakpoints WITH their per-design sidebar control panels, before building. Facilitator's stated cost was ~1500-2000 frames; owner accepted it, consistent with the H22 governing constraint that quality outranks schedule
- (decision) STRUCTURAL CONSEQUENCE - THE DESIGN PASS AND THE Q1 SPEC PASS MERGE INTO ONE PASS. Each category design session now produces BOTH the designs and the specification (content fields with types and optionality, the per-design control list with allowed values, data binding with 0/1/many behaviour, the responsive collapse rule in words, empty states, a11y notes) plus the category's shared field list. This removes the duplicated workstream where the owner would have specified the same 485 sections twice, and it makes the design session the place where the union content model is decided - which is what FR-D17's zero-prop-loss gate depends on
- (change) DELIVERED: design/claude-design-prompt-3-library.md (180 lines) - the complete section library prompt. Runs ONCE PER CATEGORY, 34 times, one category per session, deliberately aligned with the 34 owner build gates so each category is designed immediately before it is built and approved. Self-contained design system. Eight coherence rules with token-drivenness first (a section that only looks good in one pack is a defect). Per-design deliverable: desktop 1440, mobile 390, tablet only where it genuinely differs, dark mode, the sidebar control panel, and the written spec. Per-category: one design across 3 packs light+dark as the tokenisation proof, one stress frame, and the shared field list. Order follows the build order - shell block A1/A2/A3 first, then A4 Heroes, then the rest. A33 explicitly designed WITH prompt 2's card module since they are the same surface
- (decision) MEDIUM/LOW FINDINGS RULED (M1-M14). M1 name a companion PRECEDENCE ORDER in the preamble so future contradictions resolve themselves. M2 emit home.hbs and delete the FR-H2 SEO workaround. M4 synthesized templates appear on the canvas marked auto-generated, since the canvas-is-the-page promise cannot have six templates that ship unseen. M5 canvas gains a page-2 preview state so A34's ten pagination designs are designable. M6 comments render against a fixture with a stated comment count, consistent with the style-guide fixture decision. M7(2) keep the member-state toggle and add a 'you have not checked X' nudge - the real failure is forgetting, not switching. M8 the first suggestions-board admin is seeded from the owner account at setup. M9 a compatibility redeploy carries ONLY the compatibility fix, otherwise FR-J14's consent promise is false. M10 define the dispute-reversal path explicitly, with manual support as the mechanism. M11 keep 'most beautiful UI/UX in the Ghost ecosystem' as stated ambition, marked as such, not masquerading as a requirement. M12 keep a permanent Ghost 5.x instance - nearly free now that testing is self-hosted on the owner's droplet - so NFR-7's 5.x claim is actually tested. M13 add BMAD assumption/open-question/non-goal markers, especially where decisions rest on research. M14 define a settings-level fallback so card and pagination treatments keep a selection surface when their host section is removed
- (decision) OWNER INSTRUCTION on the 50 mechanical fixes: RESEARCH THOROUGHLY BEFORE BAKING THEM IN - 'ensure you are 100% right on them'. Two verification agents dispatched with an explicitly skeptical posture toward the CLAIM rather than the PRD, because two reviewer platform assertions have already failed verification in this project and each one deleted or damaged something real. Verdicts required per claim: CONFIRMED / REFUTED / PARTIALLY CORRECT / UNVERIFIABLE, with source paths, quoted lines and permalinks; gscan rule SEVERITIES to be read from lib/checks and proven by running gscan on fixtures rather than trusted from docs, because severity decides whether a deploy is blocked. Arithmetic to be recomputed independently from the PRD's own inputs rather than checked against the report
- (decision) M3 OWNER RULING: option 1 - drift detection compares file CONTENT not filenames. Every Inflozo-generated file carries a fingerprint; drift is 'did this content change from what Inflozo wrote', so a layer rename produces no signal because nothing on the live site changed. Keeps both promises intact - layer-named files stay navigable (hand-editability) and FR-J16's zero-false-positives holds. Frozen filenames rejected because they decay: after a year of renames hero.hbs holds a layer the user calls something else. FREE COROLLARY: the drift check must compare the live site against WHAT INFLOZO LAST DEPLOYED, never against what it is about to deploy - comparing against the pending build makes every rename read as drift regardless of naming scheme
- (decision) V1 CONFIRMED (both): validate translation-override ICU syntax at ENTRY, refusing to save a broken one, AND at compile as a backstop. Escalated by verification - a stray brace throws in the MessageFormat constructor outside the try/catch and escapes into res.render, producing a WHOLE-PAGE 500 rather than a graceful fallback. This is a user-input path with no validation on it. V2 normative rule on the FR-I2 filter builder: never emit redundant or nested parentheses, flatten single-child groups - facilitator verified against nql 0.13.1 that ((tag:news)) parses to the garbage key {yg:{...}} SILENTLY, with no error, and All/Any nesting is exactly what the builder emits. V3 replace gscan's swallowed-error signature (19 errors incl. a bogus GS010-PJ-PARSE on a VALID package.json) with an Inflozo-authored explanation at the FR-J6 gate
- (event) TRIAGE COMPLETE: all 152 stress-test findings ruled (23 critical, 50 high, 54 medium, 25 low) plus ~50 owner decisions and 5 commissioned research companions. The mechanical set was VERIFIED rather than trusted, which caught six wrong 'fixes' across two verification passes - three Ghost/gscan sub-assertions and three arithmetic corrections, including one (F.6's 19.75-month tenure) whose 'fix' would have broken the only table in Appendix F that reconciles end to end. Ready to encode
- (change) ENCODE PROPAGATION MAP COMPLETE: encode-propagation-map.md, 1635 lines, 53 decision blocks - 24 LARGE (L1-L25), 10 MEDIUM (M-1..M-9), 19 SMALL (S-1..S-19) - plus a binding encode order, a post-encode verification checklist and an authored-not-edited list. Built by subagent for the L and M blocks; the S blocks, verification checklist and authored list were written by the facilitator after the agent died twice to API 529 overload errors mid-write. Ordered LARGE first because a small fix applied before a structural change gets overwritten. The map's standing rule: no decision is done until every propagation target is visited and either changed or explicitly ticked
- (event) MAP FINDING worth recording: FR-J16 ALREADY specifies a content manifest of path -> SHA-256 compared against what Inflozo LAST deployed, so both halves of the M3 recommendation were already in the document. The real gap is narrower than the facilitator described - the manifest is keyed on PATH, so identity still moves when a path changes. Fix is adding per-file fingerprint identity, not rebuilding the mechanism. Recorded because the facilitator's recommendation to the owner overstated the work
- (change) ENCODE BATCH 1 COMPLETE (facilitator-encoded after 3 subagent API-529 failures): prd.md 971->999 lines. Section 4 gains the design-and-specify-before-build rule, the four-step build order, and the category owner gate definition (6 automated checks then the owner's hands-on review); T3 becomes permanent; T4 carries an explicit KNOWN UNTESTED PATH marker with its pre-launch blocking gate; NFR-6(c3) moves out of hardening into the library epics with a nightly risk-weighted rotation and a 30-day coverage window; the DoD adds the gates, FR-J17 and G1/G2-measured; the no-descope clause is rewritten around the owner's governing constraint. Section 8 retires the wave model entirely, adds E0 (mark-emission spike + platform-verification spike), rebuilds E9/E10/E11 as Shell Block / Gated Library Pipeline / Starters, and narrows E15 to final sign-off. VERIFIED AFTER EDIT: 119 FRs contiguous, zero dupes, 119/119 owned by exactly one epic, zero 'wave' build units, 16 epics E0-E15
- (assumption) ENCODE DEBT, MUST CLOSE IN BATCH 3: batch 1 deliberately created two forward references - FR-J17 (emitted-theme code-quality gate) and FR-Q7 (Koenig card design module) - cited in 7.6 and section 8 but not yet defined. The document currently carries exactly two dangling FR references. Both are on the map's authored-not-edited list. The post-encode verification checklist must confirm they resolve
- (change) ENCODE BATCH 3+4 COMPLETE. Batch 3 authored 7 new FRs (119->126, contiguous, zero dangling, 126/126 singly owned): FR-D19 design navigation carrying the carry/park/default rule, FR-D20 preview toggle with per-module edit-safe declaration, FR-D21 paginated preview state, FR-Q7 Koenig card design module with card_assets exclude as the normative mechanism (card_assets:false explicitly forbidden; audio/video/gallery/toggle need vendored MIT JS; no colour controls on header_v2/signup/cta), FR-Q8 translation ICU validation justified by the whole-page-500 failure mode, FR-Q9 treatment selection fallback, FR-J17 emitted-theme quality gate justified empirically by the deliberately-broken-theme-scores-0/0 result. FR-Q7 assigned to E7 not E10, because the library pipeline cannot both build the card treatments and the surface they are built against. Batch 4 propagated per-design controls to FR-F3 (cap governs a DESIGN not a category), FR-F7 (design-level schema over category union schema), FR-G3, FR-D13, FR-D17, FR-D9 undo, the E5 play-loop gate (now asserts the park-and-restore round trip explicitly), section 7.3 (a design switch REPLACES the attribute set), section 7.5 (project_templates.doc gains parkedControls). OWNER-DECIDABLE POINT RESOLVED AT ENCODE: shuffling away from a section carrying a Ghost-custom-setting binding PARKS the binding with its value rather than breaking it - breaking a promotion because the user browsed the design ring would be hostile; deleting or hiding still warns. sections-inventory.md preamble restructured around the explicit two-level model (category union as the storage/parking domain, per-design lists as what the user sees) plus a normative per-design specification schema with archetype and structural-descriptor fields, and an explicit statement that the one-line descriptors are the SEED and the full spec arrives from each category's design session
- (change) ENCODE BATCH 5 COMPLETE. Section 1.2 rewritten: the promise is now 'the same markup Ghost will render, styled by the same CSS Inflozo ships', agreeing BY CONSTRUCTION, with four honest carve-outs enumerated (user code injection; Ghost's own injected surfaces with Portal's fixed button SHIMMED because it lands where edge-rail designs put their furniture, the other three scoped out; real content via the style-guide fixture; and desktop 100vh, which can never be pixel-exact because the canvas is a frame inside the app). 'Nothing else is carved out, and NFR-6(c3) diffs under exactly this exclusion set' - the claim is now testable. 'Most beautiful UI/UX' kept but tagged [AMBITION - not a requirement] with G1/G2 named as the gated measurable claims. Section 7.3 gains the binding canvas geometry (viewport-sized, scrolls internally) and the chrome partition (outlines/badges/insertion INSIDE as ::after on data-inflozo-* attributes so they inherit their element's stacking and a sticky header's outline sticks with it; only the floating mark toolbar outside), device preview resizing both axes with zoom as a separate non-viewport transform, and the five missing data-* constructs named as E4 exit criteria with the vocabulary relabelled 'designed with a proven core, not fully validated'. Section 7.4 gains the normative image_sizes table with the three deliberately-different ceilings stated, card_assets exclude, the vendored cards.js, and a normative stylesheet-order rule forbidding the move after ghost_head because that would override every customer's Code Injection
- (change) ENCODE BATCH 6: verified mechanical corrections applied to prd.md - FR-J2 (package.json keys gscan errors on, posts_per_page coerced to a JSON integer because a form-field string blocks the deploy at error level), FR-H2 ({{#get}} capped at 100, version-scoped to Ghost 6, compiler never emits limit=all), FR-J5 (post_class on the article never body, and the theme emits post-access-* ITSELF because post_class does not carry it - writing the report's version would have silently lost members-only card styling on every members site; srcset is theme-authored because img_url emits none), FR-Q2 (group buckets to Site wide and RENDERS THERE - the setting is not misplaced, only a gscan recommendation; colour defaults 6-digit hex at error), FR-I1 (home.hbs emitted, FR-H2's SEO workaround retired), FR-J6 (states what gscan does not certify; the malformed-visibility signature that cascades into 19 errors incl. a bogus parse failure on a VALID package.json is replaced with Inflozo's own explanation), FR-I2 (never emit redundant or nested parens - NQL corrupts ((x)) silently), NFR-5 (image-link accessible name as a real WCAG failure, with the alt-under-h1 half explicitly NOT a defect since Casper ships it), FR-H1 (Content API resources vs the narrower {{#get}} set kept distinct so Settings is not deleted), FR-D16 (@member gains uuid/firstname/avatar_image/subscriptions; {{#has any=@member}} always false), section 7.4 (main/robots/AMP-removed-in-6.0/README conventions, each LABELLED as house style vs platform constraint), FR-J1 (the escaping rule must escape the backslash too - a functional break of inert emission, not cosmetic)
- (change) ENCODE BATCH 7 COMPLETE (L6 + L18, the post-body feature area). L6: A25 rewritten as the Post Content section - a dedicated designable section with its own design picker on post.hbs AND page.hbs. THE bindingContext QUESTION IS RESOLVED WITHOUT A NEW ENUM VALUE: verified from appendix-b1 §3/§4.2 and research-ghost-binding-contexts.md that page.hbs's root carries post and page as two keys onto ONE object and the block scope is {{#post}}, not {{#page}} - a page IS a post whose data object is called post. So FR-G3's enum stays at 10 values, and the §7.4 byte-identity hoist needs no post/page exception either. Two real defects fell out of that verification: FR-H7 said single-resource templates expose their resource inside '{{#post}}/{{#page}}' - factually wrong, both official themes open {{#post}} on page.hbs - and the genuine page.hbs-only obligation (@page.show_title_and_feature_image) belongs to A24, not A25, and is a GUARD not a binding context. A24 now carries that rule normatively and it is what keeps the two synthesized headers from merging into one partial. A25->A33 coupling deleted in all three places (inventory A25, inventory A33, prd non-placeable table); card selection now lives in FR-Q7's module with FR-Q9's Theme Settings fallback. New normative rule: every .kg-* selector belongs to cards.css and an A25 design never emits one into screen.css, asserted at compile - which is how A25 #9/#12's collision with A33 is settled without an order-dependent contest. L18: FR-H3 rebuilt as dataset + fixture set. ORBIT WEEKLY GROWS 12 -> 26 FEED POSTS, and the number is derived: posts_per_page defaults to 12, so 12 posts paginate to exactly ONE page and A34's ten pagination designs, FR-D21's page-2 state and FR-H2's main-feed designation had no renderable state between them; 26 = 2x12+2 gives a first page, a TRUE MIDDLE page (the only state with both previous and next) and a partial last. Style-guide post (generated from Ghost's render*Node functions, never hand-written, covering the full class-affecting card enumeration, loading theme CSS then simulated cards.min.css then the 4 vendored scripts, regenerated on target bump), style-guide page, and comments fixture at a stated 14 comments across 9 threads. FR-H4's undefined 'dummy placeholder prose' and FR-H5's 'styled placeholder' both retired. NEW FR-D22 preview subject (126->127 FRs, contiguous, zero dupes, zero dangling, all owned). TWO DUPLICATE IDS FOUND AND MERGED, both created by batch 6 appending instead of editing: FR-D16 existed twice (@member shape vs member-state preview) and NFR-5 existed twice (image-link accessible names vs accessibility). Also encoded: L23 #16 (GS110 is a WARNING on both specs, trigger is anywhere except orphan partials, ref is cosmetic) into FR-I1; M4's auto-generated marker; FR-D11 states explicitly that the new canvas states carry no shortcuts; two new §7.6 verify items (what a theme may style inside {{comments}} output - A28's ten designs are scoped to thread chrome until it is answered; and fixture regeneration on target bump) with the 'fourteen items' count de-restated to a derived one; F.1 rows for A25 and FR-Q7; glossary gains 6 terms; stale 487 fixed in appendix-b1 and appendix-h1. Verified: 127 FRs contiguous zero-dupe zero-dangling, 485 sum, 70 [Free] on variant lines, every FR owned (the 6 multi-owned are the documented deliberate splits).
- (change) ENCODE BATCH 8 COMPLETE (L10, the JS runtime block). Two new FRs authored: FR-G7 the behaviour-module rulebook (31 modules = 30 feature + core, all vanilla, ZERO third-party runtime dependencies, the hard permissive licence filter with the typed.js MIT->GPL-3.0 relicence as its standing lesson, ship-with-its-section lifecycle, per-design CSS+JS ownership with a MEASURED dead-CSS strip, per-module no-JS degradation as an acceptance criterion, core as E4 platform work carrying the reduced-motion gate, and the ten behaviours that need no JS at all) and FR-G8 the browser baseline (Baseline Widely Available pinned by DATE at widelyAvailableOnDate 2026-08-18 -> Chrome/Edge 121, Firefox 122, Safari/iOS 17.2; three tiers with the Tier-2 enhancement allowlist; the CLOSED three-prefix list that makes 'no autoprefixer' safe; stylelint-plugin-use-baseline + eslint-plugin-compat + size-limit, all no-build-step). 127->129 FRs, contiguous, zero dangling, all owned. FR-J4 rewritten - its old ~10-module list was a 3x undercount - and it now states the TWO-BUNDLE distinction explicitly (main.js is repo-authored only; cards.js is Ghost's own vendored MIT code), which is what makes the zero-dependency claim literally rather than approximately true. NFR-2: maximal redefined as all 31 modules + cards.js with the measured 10.3KB realistic / 17.5KB maximal against 40KB; 50KB and 100KB retired so the document carries ONE JS number; the gate is now an explicit developer-facing warning with its reason stated rather than slipped in; assertion 2's font-subsetting contradiction with Appendix D.b resolved (non-latin ships the full face); assertion 4's CSS budget given a real subject (screen.css is one file, so the budget measures the per-template reachable subset the compiler already computes for the dead-CSS strip); sodo-search's 86.5KB added as proportion. NFR-7's 'Safari 16.4+' replaced by the pinned floor; FR-K2's 16.4 reference reframed as margin. FR-G4 gains the per-module degradation rule AND prefers-reduced-motion for GENERATED THEMES - 13 variants animate indefinitely and nothing had covered them (Appendix H only covered Inflozo's own confetti). New §7.6 risk row for dependency relicensing and verify item 17 for recomputing the floor. appendix-h1 JS-marked keys RE-DERIVED against 31 modules, not 10: four modules had visitor-facing text and nowhere to put it, so V6 (no literals in a module) was unsatisfiable for them - added countdown.* (6 keys), card.play_video, card.shuffle_again, a11y.reading_progress, and marked post.table_of_contents as JS since toc is built client-side from {{content}}. Catalog 131->140 keys, 29->38 JS, Translations surface 128->137, with the rule stated as normative and the count as derived.
- (change) ENCODE BATCH 9 COMPLETE (L12 + L13). L12: all six contested designs rewritten and two RENAMED - A7 #12 Founding Member keeps {{total_members}} with the string-not-number constraint stated on the descriptor AND on A7's Data line; A32 #10 Blurred Preview states the theme-supplied partials/content-cta.hbs wrapper + mask-image gradient (Ghost emits preview blocks BARE, so there is nothing else to attach to); A32 #12 Progress Tease redefined as approximate - +/-137 words, 'About X%', 5% buckets, suppressed under 3 min, server-rendered no-JS baseline; A29 #13 redefined as a progressive filter strip (server links work JS-off, Content API refetch, limit 100, sort only on real schema columns); A27 #11 'Discover Shuffle' RENAMED to Fresh Picks (Fisher-Yates over a <=100-post pool, honestly labelled 'random from the 100 most recent'); A19 #11 'Quote Feature' RENAMED to Excerpt Quote (custom_excerpt ONLY - {{excerpt}}'s 500-char plaintext fallback would put a mid-sentence fragment inside quote marks). Appendix B gains the four core helpers ({{total_members}}, {{total_paid_members}}, {{content_api_key}}, {{content_api_url}}) with the rounding-and-plus-suffix warning, and the excerpt-vs-custom_excerpt distinction as a general trap rather than an A19 detail. FR-H5's shim covers all four; {{content_api_key}} NEVER renders a real key on canvas. NFR-3 now carries the ADMIN-KEY PROHIBITION as normative text with all three grounds (role scope = site takeover, no server-side fetch in Handlebars so a theme can only PRINT it, Admin CORS includes the site's own hostname) plus why {{content_api_key}} is different and permitted. 485 confirmed, 483 appears nowhere, 20-setting cap explicitly ticked at zero cost. New risk row + verify item 18. L13: ONE TERM applied as a real rename, not a note - 179 occurrences of variant/variants converted to design/designs across prd.md, sections-inventory.md, appendix-b1, appendix-h1 and addendum.md, with exactly three fixed identifiers protected and enumerated in the glossary (Variant Shuffle the shipped feature name, variant manifest and variant id as deploy-record fields). THE LAYOUT PICKER COLLISION IS RESOLVED BY COLLAPSE, not by renaming: the evidence is in the inventory itself - 30 categories declared a 'layout picker' with no value set while A32 declared a 'design picker' naming the same mini-diagram thumbnail ring, so they were one control under two names. It is FR-D19's design picker, its value set is the category's designs, it is removed from all 30 Controls: lines and from FR-F1's vocabulary and FR-Q3's promotable set, and it now sits outside every Controls: list like the universal controls. That also closes the v3.0 critical that the Layout Picker had no defined value set. FR-F3's sidebar groups become Content / Arrangement / Style / Data. Also fixed a pre-existing duplicated sentence in FR-F3 and a garbled one in FR-P2. Verified: 129 FRs contiguous zero-dupe zero-dangling, 485, 70 [Free].
- (change) ENCODE BATCH 10 COMPLETE (L16, the count-moving block). READING TAKEN AND STATED IN THE DOCUMENT: A31 #8 'Empty Tag State' LEAVES the inventory. It declared no file to compile into (its compileTarget was 'the empty state of a collection template', which is not a file) and no surface could place it, so it was a design in the library no user could reach - it was a STATE trying to be a design. THE SLOT IS RETIRED, NOT RENUMBERED, and A31 now states its ids are non-contiguous by design with the reason: #10 Private Site Gate is [Free] and is the private.hbs target FR-G2's Free-tier distribution depends on, and it is cross-referenced by number throughout; shifting nine ids to save one is how a renumber breaks a reference nobody re-checked. ARITHMETIC RE-DERIVED AND CROSS-CHECKED: A31 10->9, Template-Specific 102->101, library total 485->484 (inventory per-category sum = 484, Appendix A table sum = 484, group sums 47+199+109+101+28 = 484 - all three agree), placeable 457->456, render matrix 8,730->8,712 stated as 484x3x2x3 with the derivation inline so it moves with the inventory, [Free] unchanged at 70 (verified A31 #10 kept its number and its tag). 29 hard-coded 485s in prd.md plus one each in appendix-b1 and appendix-h1 all converted; zero remain anywhere. THE EMPTY-STATE RULE, which is what H9 actually creates: FR-H4's 'a section bound to zero items renders nothing on the live site' was the conflict, and zero-items now has THREE distinguished behaviours - (1) the DESIGNATED MAIN FEED renders its declared empty state, because the main feed is the page and a blank tag archive is a broken page; (2) a SECONDARY {{#get}} feed renders nothing at all, heading and container together, because an empty 'More like this' panel is worse than its absence - the same reasoning that already puts A28 #2 over #1 in the post.hbs Synthesis Defaults; (3) a bound PROP that is not a feed still follows FR-H8 unchanged. FR-H8 now says explicitly that it governs props not feeds - a guard hides an element, an empty state renders one. The empty state is a required field in every feed design's specification (the per-design Empty states field already existed), archive.empty_heading/empty_body repointed from the retired A31 #8 to the feed state, and an EMPTY TAG is pinned as an NFR-6(a) matrix fixture so the state is exercised rather than merely specified. RESIDUAL OWNER QUESTION, FLAGGED NOT INVENTED: H9 said 'the tag feed'; the main-feed-vs-secondary-feed split above is the generalisation I took, and it is stated in the document as such rather than as an owner ruling.
- (change) ENCODE BATCH 11 COMPLETE (L17, the dark built-ins). FR-Q5 rewritten around the owner's override: EVERY project compiles all three built-ins, Light-only included, and the template ALWAYS REFERENCES them - which is what makes GS100 unreachable rather than gating the declaration around it. The three fallback chains are stated as real logic, not a token mention: dark_logo -> light logo -> wordmark, dark accent -> light accent, color_scheme -> body class. The payoff is stated too: a Light-only project ships settings whose logic is present and quiet, so the moment its owner fills one in from Ghost Admin it works with no redeploy and no visit to Inflozo - strictly better than the conditional design, which shipped no setting at all. Deleted both Light-only carve-outs from FR-Q5 and FR-Q2's 'three slots it never compiles' (now false). THE data-mode VS BODY-CLASS CONFLICT IS RESOLVED WITHOUT A 484-STYLESHEET BLAST RADIUS, because they are not rivals - they are layers, and the key realisation is that NO SECTION STYLESHEET SELECTS ON MODE AT ALL (FR-E4 already had sections consuming tokens exclusively). FR-E4 now states three inputs with normative precedence: visitor override ([data-mode], written by the mode-toggle module because a runtime override must beat the class without racing it in the cascade) > owner pin (a server-rendered body class, because a class is the only mode signal a server can write and it composes with {{body_class}}) > system preference (@media prefers-color-scheme, pure CSS, works JS-off - Auto is the ABSENCE of a pin, not a third palette). Both markers resolve into the same token block, so this is a decision about one file rather than 484. Propagated: FR-Q2's reservation reason (a slot in use, not a slot held empty), FR-D7's mode-switch reason, FR-J2's parenthetical, FR-Q4 (the canvas must preview the RESOLVED fallback, not the declared default - previewing the declared one would show the user a state their visitors never see), FR-J6 (GS100 on the reachable shortlist WITH why it can never fire), Appendix D (the fallback resolves unset OWNER OVERRIDES and does not weaken hand-paired packs), §7.4's default.hbs tree comment, E6 (the mode resolution) and E7 (the always-reference emission). New verify item 19: prove GS100's exact trigger on a fixture on both specs, since three keys in every shipped theme rest on 'declared AND referenced never fires'; and the gscan-bump risk row now names GS100 specifically. 3/17/20 cap arithmetic verified consistent. 129 FRs, zero dupes, zero dangling.
- (change) ENCODE BATCH 12 COMPLETE (L19 + L22). L19: §1.2's enumerated carve-out was already encoded in batch 5, so this batch built the MECHANISM behind it. FR-C2's connect probe now also reads whether Portal's floating subscribe button is enabled and ASKS when it cannot be read (one question, defaulting to on, since Ghost enables it on essentially every site) - shimming a button that is switched off would be its own fidelity defect. Stored on the connection: §7.5's sites row gains a site_settings snapshot (code-injection flag, Portal button state, members/paid enabled, comments enabled) read at connect and refreshed by FR-C5, which is the single source the canvas shim and the pre-deploy checks both read. FR-H5 states the shim rule: it renders INSIDE the iframe as Ghost would place it, and is non-interactive, non-selectable, absent from Layers and never compiled - it exists so an edge-rail or corner design is authored knowing what will sit on top of it, which is exactly why it is shimmed rather than scoped out with the other three injected surfaces. NFR-6(c3)'s exclusion regions are now NAMED where the test lives rather than only referenced from §1.2. Appendix B distinguishes Portal's floating button (a site setting, injected by ghost_head, no theme markup produces it) from the data-portal actions a design places. The sodo-search risk row is widened to the whole class of Ghost-injected surfaces. OPEN OWNER QUESTION FLAGGED, NOT INVENTED, on A2: Ghost has an announcement bar of its own, so a site with Ghost's bar enabled AND an Inflozo bar placed shows TWO stacked - legitimate or a defect, and this document does not rule which. Three candidate resolutions written into A2; nothing assumes an answer. L22: §7.4 had a frozen public API with a warning and NO MECHANISM; the mechanism is now authored. Naming - the user types a DISPLAY NAME and never a filename, Inflozo derives custom-{slug}.hbs, and the derivation is shown live with the Ghost dropdown label beside it, because Ghost's label transform is fixed and unoverridable so the user is choosing that label whether they realise it or not; reserved stems refused at entry. Collision - three kinds, each PREVENTED AT CREATION and never resolved at compile, since a compiler that silently disambiguates is a compiler that silently detaches a page: two names slugifying identically (refused, because auto-suffixing would produce 'Member Home 2' in Ghost Admin without ever saying why), a name matching one this project has ALREADY DEPLOYED including a since-deleted surface (deployed names retained per project and never reused), and page-{slug}.hbs which cannot collide because FR-I1 never emits that form. RENAMING A DEPLOYED TEMPLATE IS NOT OFFERED - not warned, not confirmed, ABSENT - and FR-J16 now states explicitly that drift is not the mechanism that catches this, because by the time drift could report it the pages have already detached. Propagated to FR-I3 (the naming UI), FR-I2 (validation), FR-I1 (emptying a custom template now warns, since Ghost's fallback is silent), A30 and A31 #6/#7/#9, the glossary (new Frozen filename term, distinguished from section-partial names and from FR-J10's theme-name freeze) and Appendix H (the permanence copy, said once at the moment of choosing). 129 FRs, zero dangling, 484, 70 [Free].
- (change) ENCODE BATCH 13 COMPLETE (L24 + L25) - ALL 24 LARGE BLOCKS NOW ENCODED. L24: the preamble manifest grows from 8 entries to 14 covering every normative file in the folder plus the three design prompts, and a PRECEDENCE ORDER is authored - (1) the verification files on any claim they checked, (2) the research companions on any external Ghost fact WITH the reason stated (three reviewers independently found them more accurate than the body text; the regression risk in this project lives in the DISTILLATION step, so where prd.md and a research companion disagree about Ghost the PRD is wrong and moves), (3) the normative companions inside their declared areas, (4) prd.md on everything else and on every product DECISION without exception - companions override facts, never decisions, (5) addendum.md mechanism-not-scope, (6) spike-compiler empirical and narrow. Plus the dated-snapshot rule (research is never edited to match the PRD; the reverse would destroy the only independent check this project has) and the research-vs-research tie-break, which precedence CANNOT settle - §7.6 item 14(b) is the live example, resolved by executing E0 and, until then, by choosing the reading that is safe under both. Verified the four pre-existing local precedence sentences all agree with the global order. L25 H15/H16: FR-C1 rewritten - ONE credential pair at connect (the single Custom Integration), the STAFF ACCESS TOKEN DEFERRED TO FIRST DEPLOY and declinable permanently. The reason is stated: three mints against a sub-10-minute G1, two of them justified by capabilities the user has not yet reached, and the token can only be minted on the site OWNER's account, which the primary persona frequently is not and often cannot become in the moment. The graceful no-token path is written as a designed path with all three consequences named and nothing silently skipped (FR-J13's manual snapshot alternative, FR-J16 failing open, FR-I4's guided routes card), and a PARTIALLY CREDENTIALED PROJECT IS A FIRST-CLASS STATE stored on the connection (§7.5 staff_token_vault_ref nullable + credentials_present). FR-C3 now says the honest thing: the Staff Access Token is a FULL-ADMINISTRATOR credential, Ghost has no scoped theme-read grant (which is the entire reason the token is needed), what constrains Inflozo is P8 as a product commitment rather than a permission boundary, and neither §1.4 #5 nor P8 may be written as though the credential were narrower than it is. G1's clock now excludes the token; G2's includes it. L25 GPRO: the E4/E7 joint gate and the §7.6 risk row both asserted T4 verification that CANNOT BE MET since T4 does not exist pre-MVP - both rewritten, with the T4 clause carried forward to the pre-launch gate rather than blocking two epics on a missing target. §7.6 items 1 and 2 marked LAUNCH-BLOCKING (item 2 is the only verify-at-build item that blocks launch rather than an epic), the DoD gains the blocking gate, the glossary's Test environment T1-T4 and Preview-only entries are marked, FR-N4's hosting page is marked as not shippable-as-verified until the gate clears, and Appendix F.3b now states the PRE-GATE BUILD-PERIOD FLOOR of about 2 versus the verified 07 launch floor, since the gate falls before public launch so T4 is inside the launch figure and outside the build-period one - and the build period is the unbounded one the runway is actually spent at. 129 FRs, zero dupes, zero dangling, 484, 70 [Free].
- (change) ENCODE BATCH 14 COMPLETE — ALL NINE MEDIUM BLOCKS (M-1..M-9). M-1a: 'exactly one locale file' retired everywhere (FR-Q6, §7.4 tree, Appendix B, glossary, appendix-h1 S4 and V3). en.json is ALWAYS emitted plus the project-language file when different, and the old reason ('no second file can disagree') is replaced with the true one - both files are emitted from ONE catalog in the same pass so the key sets are equal BY CONSTRUCTION and only values differ. Ghost falls back to the en FILE, so a theme shipping only de.json renders raw keys across a whole site. M-1b: NQL RELATIVE-DATE FILTERING RESTORED - the third reviewer assertion in this project to be verified false and to have deleted a working feature. published_at:>now-30d parses, and routes.yaml filter strings are RE-PARSED PER REQUEST so nothing freezes. Scoped deliberately: relative dates are a routes.yaml capability and NOT a Data-group one, because a fixed-Count {{#get}} feed with a relative filter would render a different set on every request while the design was built against a fixed one. Added a §7.6 RISK ROW FOR THE FAILURE CLASS ITSELF, since it has now happened three times and the standing rule belonged in the PRD rather than only in project history. M-1c: FR-H8 now states the guard is derived from the BOUND FIELD, never a helper argument - the spike emitted {{#if YYYY}} from a date format arg, silent permanent content loss that a presence-check cannot see, so NFR-2 assertion 5 is strengthened from 'a guard exists' to 'every guard resolves to a real bound field'. M-1d: engines.ghost >=5.0.0 emitted, and NFR-7's 'not from a package.json declaration' rewritten rather than left reading as a contradiction - the helper surface is what MAKES 5.x work, engines.ghost DECLARES the floor Ghost checks at upload, and engines.ghost-api is a different key that stays withheld. M-1e: CSP frame-ancestors 'none' -> 'self' (it forbade the very iframe §7.3 mandates), and connect-src stated as composed PER SESSION rather than a static header, since the Ghost origin varies per project. M-1f: FR-A5's purge no longer gated on an action a departed user will never take - gating deletion on a download turns a GDPR commitment into indefinite retention. FR-C6 inherits the corrected rule. No sixth email, deliberately, with FR-P2's no-nudge rule as the stated reason. M-2: V1's ICU validation propagated to the catalog as new assertions V9/V10 plus a new S0 rule (braces are SYNTAX in every override) and migration re-validation; V2's round-trip claim strengthened - asserting the parsed AST IS THE INTENDED FILTER, because a success-only round trip is blind to exactly the ((tag:news)) failure that motivates the rule; three new risk rows. M-3: NEW FR-I6 template-binding flow (130 FRs) - fires from the DEPLOY SUCCESS state because it is a post-deploy step, names each emitted filename and the exact dropdown label Ghost derives, and is a CHECKLIST NOT A VERIFIED STATE because the assignment lives on Ghost's page row and the Content API does not expose it - saying 'we cannot see whether you did this' is the honest version. Journeys mandate five flows -> six. M-4: the upload-succeeds/activate-fails behaviour defined once, with all six consequences stated (status 'uploaded', artifact retained, no active badge, theme name DOES freeze - matching FR-J13's first-UPLOAD snapshot trigger so the two rules agree by design, drift baseline does NOT move since it tracks what went live, FR-J11's claim reconciled, no failure email). M-5: dashboard thumbnails become static Style-Pack-derived placeholders, deferred into Appendix G (which is binding in both directions); the conclusion that mattered survives - still no headless browser and no Chromium line - but now because nothing is rendered at all; thumbnails bucket unprovisioned, E1 cut line flips the card art to IN, E13 loses the capture. M-6: FR-L3's three-option sheet did not apply to 22 of the things it gates; the 28 non-placeable treatments get a FOURTH remediation, 'revert to the Free design', which always exists because FR-G2's first-two rule guarantees it, and the 22 is derived on the page as (12-2)+(6-2)+(10-2). M-7: home.hbs added to the tree and to the synthesizable set (six -> SEVEN in all four places); home.hbs and index.hbs synthesize the SAME stack so the root and its paginated continuation cannot disagree; and the SEO guard is SCOPED rather than deleted - the home case is solved structurally, the ARCHIVE case survives because tag.hbs and author.hbs have no second file to fall through to. M-8: drift gains PER-FILE FINGERPRINTS so identity is content rather than path and a layer rename produces no signal; determinism kept but re-pointed at NFR-6(c1)'s snapshots rather than at drift. M-9 verified already encoded. 130 FRs contiguous, zero dupes, zero dangling, all owned; 484; 70 [Free].
- (change) ENCODE BATCH 15 COMPLETE — ALL 19 SMALL BLOCKS, THEN THE FULL POST-ENCODE VERIFICATION CHECKLIST RUN INDEPENDENTLY. SMALL blocks encoded this batch: S-2 (the compiler's own formatting contract authored - two-space indent, attribute wrapping, blank-line rule and the normative boundary-comment format bound to the same layer-name source §7.4 binds the filename to; stated as a real implementation requirement because outerHTML does not indent, and the spike is LABELLED as not yet meeting it rather than treated as the reference), S-3 (canvas loads the SAME subsets, same unicode-range splits and same variable-axis instances the theme ships - a full face while designing and a subset after deploy is a fidelity defect with a long fuse, and it removes a permanent false-positive class from NFR-6(c3)), S-4 (FR-G4 delegates the collapse ladder to the 15 structural archetypes; prose cannot specify a ladder for 484 designs without being wrong or ignored), S-5 (A1 #16 Tab Deck visual-only, and the SAME treatment applied to A3 #8 and A12 #15 which had the identical cross-section coupling), S-6 (FR-G5's proxy now runs over a machine-checkable structural-descriptor TUPLE - archetype, primary axis, item-count class, media placement, emphasis mechanism - not over the English line, because two designs can be described differently and be the same design; and the tuple must stay unique AFTER per-design control lists are written, since controls no longer distinguish designs), S-7 (G1/G2 owned and gated - E15 owns the measurement as an unwaivable launch gate, E3 and E5 own the paths; they are the ONLY gate on the ease-and-play half of the thesis while the beauty half carries none), S-8 (personas thickened, each carrying its GHOST CREDENTIAL REACH - the multi-site operator is frequently NOT the site Owner, which is precisely the case FR-C1's graceful no-token path serves and which a hard token requirement would have locked out of the product entirely), S-11 (member-state nudge - the failure is forgetting, not switching), S-12 (first board admin seeded from the owner account and surviving the Test->Live cutover, since a board that ships with no admin has nobody who can grant the first one), S-13 (a compatibility redeploy carries ONLY the compatibility fix - the narrowness is what keeps FR-J14's consent promise true), S-14 (the dispute transition gains its reverse edge - manual support restoration of the REMAINDER of the original term, deliberately manual because auto-restoring on an unverified event is how an account flips twice), S-16 (T3 is PERMANENT, not the first thing to drop - a second droplet is the entire cost of testing a claim the document makes publicly), S-17 (the marker convention authored in the preamble - ASSUMPTION, AMBITION, KNOWN UNTESTED PATH, each with what a downstream agent must do differently), S-19 (E1's design pointer now carries the precedence: prompt 2 over prompt 1, and ALL design artifacts non-normative). S-1/S-9/S-10/S-15/S-18 verified already encoded. VERIFICATION CHECKLIST RESULT: 130 FR definitions, 130 distinct, zero duplicates, contiguous 1..n in all 17 letter blocks, zero dangling references across all five normative files; every FR owned, the 7 multi-owned all documented splits; 34 categories, every category's enumerated designs equal its declared count, inventory sum 484 = Appendix A table sum 484 = group sums 47+199+109+101+28, row-for-row match between Appendix A and the inventory with zero mismatches; 456 placeable + 28 treatments = 484; 70 [Free]; render matrix 8,712 = 484x3x2x3; waves gone; string catalog 151 keys / 42 JS matching its stated totals namespace by namespace; 31 modules; baseline floor consistent; Appendix F verified figures intact. ONE REAL DEFECT CAUGHT BY THE STALE-TOKEN GREP AND FIXED: FR-I2 said 'Ghost has no per-collection page size' - FALSE, a routes.yaml route with an explicit limit: overwrites @config.posts_per_page at render time for that route (services/routing/controllers/collection.js), and appendix-b1 §2 already carried the fact while prd.md contradicted it. That is the FOURTH reviewer assertion about Ghost to enter this document as normative text and be verified false; the risk row's count was updated from three to four, and FR-H2 now reconciles the main feed's sizing against a route's own limit, because a feed previewed at 12 and served at 6 is a WYSIWYG defect the user cannot see coming. Also fixed: the three design-prompt manifest paths were wrong by one directory level (../design/ -> ../../design/) and would have resolved to nothing. The other four stale-token hits are deliberate refutations, not stale assertions.
- (change) PRD v4.0 COMPLETE. All 53 propagation-map blocks encoded (24 LARGE, 10 MEDIUM, 19 SMALL), the post-encode verification checklist run independently of the encode, a structure-then-prose polish pass completed, and frontmatter set to version 4.0 / status final / updated 2026-08-19 with companion dates refreshed. prd.md 1,098 -> 1,263 lines. FINAL COUNTS, EACH RE-DERIVED RATHER THAN RESTATED: 130 FR definitions, 130 distinct, zero duplicates, contiguous across all 17 letter blocks, zero dangling references across all five normative files, every FR owned by exactly one epic (the 7 multi-owned are documented splits); 34 categories; 484 designs, agreeing three ways (inventory per-category sum, Appendix A table sum, group sums 47+199+109+101+28) and matching row-for-row with zero mismatches; 456 placeable + 28 treatments; 70 [Free]; render matrix 8,712 = 484x3x2x3; string catalog 151 keys / 42 JS matching its stated totals namespace by namespace; 31 behaviour modules; zero occurrences of 485 anywhere. Structure clean (headings ordered, code fences balanced, no ragged tables, no unbalanced inline markup); prose clean (no hedging in normative text). SEVEN REAL DEFECTS WERE FOUND BY PROPAGATION RATHER THAN BY REVIEW, and each is the shape of thing the map exists to catch: FR-D16 and NFR-5 were each DEFINED TWICE (batch 6 appended where it should have edited); FR-H7 said page.hbs opens {{#page}}, which no Ghost theme does; FR-F3 carried a duplicated sentence and FR-P2 a garbled one; two normative companions still said 487; the three design-prompt manifest paths were wrong by one directory level and resolved to nothing; and FR-I2's 'Ghost has no per-collection page size' was false, the FOURTH reviewer platform assertion in this project to enter the document as normative text and be verified wrong. THREE JUDGMENT CALLS ARE RECORDED IN THE DOCUMENT WITH THEIR REASONING RATHER THAN SILENTLY TAKEN: no 'page' value was added to FR-G3's bindingContext enum, because a page IS a post whose data object is called post; Orbit Weekly grew 12 -> 26 posts because posts_per_page defaults to 12 and twelve posts paginate to exactly one page, leaving A34's ten designs with no renderable state; and the Layout Picker was COLLAPSED into the design picker rather than renamed, on the evidence that 30 categories declared a value-set-less 'layout picker' while A32 declared a 'design picker' for the same control. FOUR ITEMS REMAIN OPEN FOR THE OWNER AND ARE FLAGGED IN THE DOCUMENT, NOT GUESSED: whether the shell block carries its own owner gate(s); confirmation of the P8 rewrite; the A2-vs-Ghost-announcement-bar stacking question; and the main-feed-vs-secondary-feed generalisation of H9's empty-state rule.
- (decision) OWNER RULINGS ON THE SEVEN OPEN ITEMS — ALL ENCODED, PRD v4.0 NOW CARRIES ZERO OPEN QUESTIONS. (1) POST VS PAGE, OWNER CORRECTED ME AND WAS RIGHT: a page CAN have a #page context as well as #post content. I had written 'there is no {{#page}} block scope', which was false and contradicted appendix-b1's own axes table and its custom-route row. VERIFIED TRUTH NOW ENCODED: on page.hbs the root carries post and page as TWO KEYS ONTO ONE OBJECT so either block scope reaches it; both official themes use {{#post}}; Inflozo emits {{#post}} on both targets so one design compiles identically to each, which is what lets the byte-identity hoist merge them; and {{#page}} is the ONLY form on a routes.yaml custom route declaring data: page.x. The owner's product distinction is now normative and lives in compileTarget rather than bindingContext: a post is an article carrying comments, authors, related posts and a reading TOC, a page is a standing page (About, Our Story) carrying none - which is why A26, A27 and A28 declare post.hbs only and are structurally unplaceable on a page. A25's page.hbs defaults now ship TOC off and share rail off, both still available for the long-form exception. (2) SAMPLE DATA: 32 feed posts, not 26. 32 = 12+12+8 at the default posts_per_page, so still a first page, a TRUE MIDDLE page and a partial last. Owner also ruled some be marked FEATURED so filtered criteria return results - encoded as a composition rule rather than a bare number: 8 of 32 featured, every tag carrying >=3 posts, every author >=6, featured spread across tags and authors so a COMBINED filter (featured AND by tag) also returns something, because a Source option that renders empty in the editor is indistinguishable from a broken binding. (3) Layout Picker collapse: agreed, unchanged. (4) SHELL BLOCK GATES = OPTION A: its three categories each carry their own owner gate, so the gate count is exactly the category count with no exceptions in it; E9 now exits on THREE gates taken in turn, not one shell-wide sign-off; §4's build order says the shell is gated rather than waved through, with the reason (a defective header is a defect on every page of every site). DoD gate count changed from the literal 34 to a derived 'every category owner gate'. (5) P8 REWRITE CONFIRMED. (6) ANNOUNCEMENT BAR — RESEARCHED AGAINST GHOST SOURCE BEFORE ENCODING, AS ASKED. Findings: announcement_content is ABSENT from Ghost's public settings allowlist (settings-cache/public.js), so it is on neither @site nor the Content API; Ghost's own bar is a <script defer> injected by ghost_head that fetches the text at runtime from /members/api/announcement/, because visibility is per-member and pages are cached; the three announcement settings ARE readable with an Admin API key (ordinary non-core non-secret group); and no theme can suppress the bar, but Ghost emits NOTHING when content or visibility is empty. So the owner's design is feasible with one change - SEED, DO NOT LIVE-BIND. Encoded: FR-C4 reads the announcement at connect and offers to bring it across (text -> message prop, announcement_visibility -> the show-to control, announcement_background -> Background role), after which it is an ordinary server-rendered Inflozo section; a live binding is REJECTED with the reason stated (it would make the bar's content JS-dependent on an undocumented members endpoint, empty with JS off, and would rebuild member visibility client-side when A2's show-to control already does it server-side); and a one-click 'turn Ghost's bar off' is offered AFTER seeding, safe because the copy is already inside Inflozo, consented rather than silent. P8 now names that write explicitly so it does not become the next unreconciled contradiction. §1.2's carve-out 2 changes character - the announcement bar is the one Ghost-injected surface a user can TAKE BACK, so the product removes that carve-out rather than merely disclosing it. New verify item 21 covers the Admin-API read and the clear-stops-emission behaviour, since both were read from source and not executed. (7) EMPTY FEED = OPTION A, now recorded as the owner's ruling rather than my reasoning. Re-verified after all edits: 130 FRs contiguous zero-dupe zero-dangling, 484 designs, 34 categories, 70 [Free], no ragged tables, no unbalanced markup, zero stale tokens.
- (decision) OWNER RULING: Ghost's announcement bar is SHIMMED ON THE CANVAS as a strip at the very top, above everything the theme renders. Encoded as the second Ghost-surface shim alongside Portal's floating button, reusing that machinery rather than inventing any. FR-H5 rewritten to cover both under one rule - inside the iframe where Ghost puts them, non-interactive, non-selectable, absent from Layers, excluded from Shuffle and Remix, drawn from the connection's stored settings snapshot, refreshed daily by FR-C5, and both NFR-6(c3) exclusion regions since they are Inflozo-drawn approximations of Ghost's own markup. THE TWO SHIMS BEHAVE DIFFERENTLY AND THE DIFFERENCE IS STATED: Portal's button is position:fixed and OVERLAYS, while the announcement strip OCCUPIES REAL VERTICAL SPACE AND PUSHES THE DESIGN DOWN, exactly as ghost_head will - which is the whole point, because a transparent-overlay header, a sticky header or an Inflozo announcement bar all sit BELOW Ghost's strip on the real site and designing them without it on screen is designing against a page that does not exist. The strip shows the site's real announcement text in its real announcement_background, and it HONOURS THE CANVAS MEMBER-STATE TOGGLE per announcement_visibility, so a bar set to paid-members-only is absent while previewing as Anonymous (FR-D16). It disappears the moment the user takes the one-click clear of Ghost's bar, which is the point at which the two-bar stack resolves into one. §7.3's chrome partition now distinguishes these from EDITING CHROME: editing chrome decorates a design and is drawn as ::after pseudo-elements with data-inflozo-* attributes; the shims stand in for markup Ghost injects AROUND the design, carry no data-inflozo-* attributes, no selection, no hit-testing, and are excluded from outerHTML serialization so the byte-comparable-markup guarantee is untouched. §1.2's carve-out now reads two-of-four shimmed rather than one-of-four, with only the comments embed and the search overlay still scoped out. A2 drops its no-warning-needed hedge: no warning is needed once the thing being warned about is on screen. Work assignment stated as a SPLIT, NOT A SECOND OWNER - FR-H5 stays E4's and defines what the shims must render and how they stay inert, while the two shims are canvas work and land in E5 with the canvas; verified FR-H5 still resolves to exactly one owning epic. Re-verified after the edits: 130 FRs, zero dupes, zero dangling, 484 designs, 70 [Free], no ragged tables, no unbalanced markup.
- (change) DELIVERED: _bmad-output/planning-artifacts/build-sequence.md (407 lines) - the six-step handoff from finished PRD to first story, written so any step can be started COLD without reconstructing conversation, on the same pattern as encode-handoff-prompt.md which worked. Each step carries what it produces, what it needs from the owner, what it unblocks, and a self-contained prompt that LEADS WITH ITS BMAD SKILL so the right skill triggers on paste: step 1 /bmad-architecture, step 2 /bmad-build (the two E0 spikes), step 3 the owner's own Claude Design work on prompts 2 and 3, step 4 /bmad-review (verification-gap + adversarial lenses, reconciling designs against the PRD), step 5 /bmad-ux (the four journeys and six flows the PRD mandates but does not author), step 6 /bmad-create-epics-and-stories, then /bmad-sprint-planning as a readiness gate. Every skill name verified to exist in .claude/skills/, and the two deprecations called out explicitly (bmad-create-architecture forwards to bmad-architecture; bmad-create-story and bmad-dev-story are superseded by bmad-build). THE FILE CARRIES THE FIVE STANDING RULES FORWARD into every prompt, because the same failure modes will recur in architecture and stories: external platform claims are hypotheses until read in source or executed (four have already entered this PRD as fact and been verified false), propagate never localise (root cause of ~25 defects across three rounds), counts are derived not restated, flag do not guess, and the companion precedence order with design artifacts explicitly non-normative. SEQUENCING RATIONALE RECORDED: steps 1 and 2 run in PARALLEL with the owner's design work, and that parallelism is the point - the FR-D4 mark-emission spike is the one result that could still invalidate the design investment, so it wants answering while prompt 3 is on category three rather than category thirty. The spike prompt also carries the two known spike-compiler defects to fix (the date renderer discarding its format arg; wrapGuard deriving its guard identifier from a format argument and emitting {{#if YYYY}}) and the escaping case the verification caught (a preceding backslash must be escaped too). Step 3's entry states what prompt 3's output MUST contain so step 4 can check it, naming the two newly load-bearing fields designers are most likely to omit: the structural descriptor TUPLE that FR-G5's uniqueness assertion actually runs over, and the no-JS degradation statement for any design declaring an FR-G7 module. ONE OWNER ASK IS ISOLATED IN ITS OWN CLOSING TABLE: provision T1 (Ghost 6.x) and T3 (Ghost 5.x) droplets at about /mo each BEFORE step 2, since E0 cannot run without them and every category gate needs a real-Ghost comparison from the first category rather than at hardening; T2 later; T4 deferred but its gate remains launch-blocking and the reminder is recorded there. Self-corrected one claim while writing: the draft said 'seven documented splits' where the real count is six (FR-P1, FR-Q6, FR-G1/G4/G5/G6) - rewritten to name the splits by their members rather than by a number that would need maintaining, per the counts-are-derived rule.
- (change) Architecture spine created at ../../architecture/architecture-Inflozo-2026-08-19/ (35 ADs, paradigm: functional core / imperative shell with the core shared verbatim between browser and Node). Four things this run sends BACK to the PRD. (1) §7.3's escaping remedy is verified FALSE against handlebars 4.7.9 - Handlebars' backslash escape is not composable, exactly one backslash escapes and it is consumed, every count >= 2 evaluates live, so 'the backslash is escaped too' cannot work; HTML numeric entity encoding of braces closes it completely and is now AD-5. (2) A mustache abutting a closing brace is a Handlebars PARSE ERROR, and FR-Q5's inline token block in default.hbs emits exactly that shape. (3) §7.6 item 19 is CLOSED by execution: GS100 is an error on both specs, fires when ANY declared key is unreferenced, and {{#if}} guards and partials both count as references - FR-Q5's design is confirmed sufficient. (4) NEW rule the PRD never names: GS051-CUSTOM-FONTS, a warning on both specs requiring --gh-font-heading and --gh-font-body in the same file, without which FR-J6's 0/0 target is unmeetable on all 484 designs at once; honouring it means a Ghost Admin font choice overrides the Style Pack pairing, which is a new entry for §1.2's carve-out list. Also delivered: the full 29-table schema and RLS from §5 (applied clean to PostgreSQL 17.11, 23 assertions PASS, 0 without RLS), the compile function sized from a real 40-section stress measurement (300s / 2048MB, ~2.0s and 289MB measured, so §7.1's gscan-boundary split does not trigger), and all 21 verify-at-build items given a standing owner epic beyond E0(b), plus 7 new items the run found by execution.
- (decision) Umang approved three things on 2026-08-19: (1) honour Ghost Admin's font setting over the Style Pack pairing - it is the owner's site, and declaring support for the feature while ignoring it would satisfy gscan and lie to the user; (2) amend the PRD in place despite status:final; (3) turn the Ghost(Pro) gate from a reminder into an action item placed where it is required.
- (change) PRD amended to v4.1 (backup at .v4.0-backup/prd.md, 360267 bytes). Five surgical edits, each marked inline with its date and its evidence file. EDIT 1 - 7.3 Escaping: the prescribed remedy ('the backslash is escaped too, the emitted form is \{{') is verified FALSE by execution against handlebars 4.7.9, the exact version Ghost main declares; Handlebars' backslash escape is not composable, exactly one backslash escapes and is consumed, every count >=2 evaluates live, so no emission renders a literal backslash followed by a literal brace pair. Replaced with HTML numeric entity encoding, which round-trips every case with zero parse failures. The bug the paragraph identified was real; the fix it named was not.
- (change) EDIT 2 - 7.3 compiler mechanics went from three binding to four. New mechanic 3: a mustache may never abut a closing brace, because Handlebars reads }}} as a triple-stash close and ':root{--a:{{@custom.x}}}' is a parse error - which is exactly the shape FR-Q5's inline token block emits for a promoted colour, and which gscan reports only as 'Templates must contain valid Handlebars'. Compile CI now asserts zero {{{ and zero }}} in emitted output; Inflozo emits no triple-stash by design (FR-D4), so the assertion is exact and closes the class rather than the instance.
- (change) EDIT 3 - GS051-CUSTOM-FONTS added in two places. 7.4 gains a normative rule: screen.css must declare --gh-font-heading AND --gh-font-body in one file, or every emitted theme carries a standing warning on both specs and FR-J6's 0/0 target plus every category gate becomes unmeetable on all 484 designs at once. 1.2 gains carve-out 1b: honouring the variables means the site owner's Ghost Admin font choice beats the Style Pack pairing - Umang's decision, recorded where the WYSIWYG promise is made rather than only where the rule is enforced.
- (change) EDIT 4 - 7.6 item 19 CLOSED BY EXECUTION. Five fixture themes on both specs proved four previously-assumed facts: GS100 is an error not a warning; it fires when ANY declared config.custom key is unreferenced (two-of-three is not partial credit); a reference inside a {{#if}} guard counts; a reference from a partial counts. FR-Q5's always-declare-always-reference design is confirmed sufficient and scores 0/0 on both specs. EDIT 5 - new item 22 registered: enumerate the FULL v5 and v6 rule sets from gscan/lib/specs and diff them against FR-J6's reachable shortlist before E7 exits, because that shortlist was derived from what this document expected gscan to check rather than from what gscan checks - which is how GS051 stayed invisible.
- (change) Preamble amended: version 4.0 -> 4.1, and an amendments block added stating that MEASUREMENTS.md joins the verification files at rank 1 of the precedence order for the claims it checked. Structural integrity verified after editing: heading count unchanged, the 7.6 verify list still numbers 1..22 contiguously, no superseded directive text survives outside the quoted correction.
- (direction) Ghost(Pro) gate is now an action item with a trigger, not a reminder: buy the Starter trial when E13 closes and E14 opens. Not earlier because a trial is time-boxed and one bought during E7 expires before the gate needs it. Not later because E14 is the first epic that cannot ship without the answer - FR-N4's Hosting requirements page describes a Starter limitation inferred from an unobserved payload, and 4 forbids it shipping as verified. E14 acquires and captures, E15 verifies and holds the launch gate.
- (change) step 4a done — reconcile-designs.md written: 1086 findings across all 34 categories, P0 and the S/M screens (spec-incomplete 393, stale-design 268, arch-gap 189, needs-execution 109, ghost-infeasible 66, prd-defect 61); 41 needs-execution probe families (179 per-category probes) for T1/T3; 1078 owner-flag rows as the Ghost Build Room agenda; PRD amendments rolled up by FR. Derived counts: 485 designs on disk (PRD says 484; FR-G1's own 457+28 already sums to 485), 485 tuples unique, 70 [Free] in the inventory but no spec marks any. Registered in doc-audit; findings array in reconcile-designs.findings.json.
- (change) Tier-3 inventory merge, normative half. Probe item 47 run against T1/T3 and it refuted its own premise: no per-template {{#get}} budget exists — Ghost races each get against 5000ms on both majors and 150 gets on one template resolved in full (MEASUREMENTS §30, item 47 closed, item 52 added for Ghost 6 query dedup). R-20's cap stands at twelve on a latency budget, not a platform limit; raising it is left as an owner decision. R-16 made structural: tools/export-roster.py + tools/inventory-gen.py generate Appendix A's totals and all 33 rosters and prd.md Appendix I from the export, gated by doc-audit. A23 deleted per the owner's tightened R-24, taking A1-9 and A4-15 with it: 33 categories, 468 designs, 66 [Free]. Every restated count in prd.md derived or removed. tuple-check reads the merged library and is mutation-tested red. derived-fields-A1-A12.md retired. NOT done and recorded as such: the 34 specs, the per-category Content/Controls/Data unions, research section 7's two design-list columns.
