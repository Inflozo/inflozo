---
topic: The Ghost Build Room — Inflozo architecture decisions
updated: 2026-08-27T19:49
---

## 2026-08-20 — Round 4 security stress test (four Ghost-specific attacks)

- (constraint) Ravi, flat: **there is no scoped Ghost integration.** An Admin API key is `kid:secret` minting a JWT with `aud:/admin/`; Ghost verifies the signature, not the intent. No scope claim, no read-only integration exists. So P8's four-write allowlist is a real invariant about *Inflozo's behaviour* and not a boundary on the *credential*. Executed both majors: members incl. email, users, settings, POST /posts all 200/201.
- (decision) AD-10 must state the credential is all-or-nothing and name its compensating controls: the decrypt chokepoint (holds), key rotation (`admin_key_rotated_at` exists and nothing writes it — "that column is a confession"), and logging on the proxy (does not exist; Murat: detection time on credential misuse is currently unbounded).
- (question) Sally: Ghost offers no narrower key, but the *consent screen* can still say plainly that connecting hands over the subscriber list. Architecture is honest in the docs and silent in the UI. P3 expose-deliberately.
- (constraint) Escaping is a CHARACTER control; a URL scheme is a SEMANTIC property. `javascript:alert(1)` contains nothing to escape and survives every escaper intact. The Conventions row scheme-validates **Ghost-sourced** values only; the reasoning error was concluding user-typed values are therefore safe. The user is also a stranger to the visitor.
- (constraint) **The helper-argument breakout is Handlebars source built by string concatenation** — `{{img_url ${path} size="${arg}"}}`. Ravi: same class as SQL injection by concatenation. AD-5 made user text un-mustache-able and then the compiler generates mustaches from design-controlled strings by templating.
- (constraint) **AD-2 "the library is data, never code" is false twice**: `behaviour.js` is code, and `design.json` binding strings compile into template *source*. The second is new. D13's CI lint (the control for actor C) is unbuilt.
- (decision) Reframed by Ravi and it changes the priority: the *malicious* author is rare, the **typo** author is Thursday — `size:800px` instead of `size:800` emits a broken mustache and gscan blames the customer's theme. Same root cause: interpolating instead of parsing. Fix is a correctness fix and a security fix at once, which is also how to justify it.
- (constraint) Ghost's `accent_color` validation is "a regex that got lucky twice" — rejects `background:url(https://…)`, accepts `#fff;background:url(x)`. Decision B itself is sound (the per-tag compile-time alternative genuinely rots when the owner adds a tag); the wrong part is the *assumption underneath* it, that the bound value is a colour.
- (constraint) Murat's structural point: the accent_color check **cannot live in the FR-J17 gate** — the gate sees `style="--tag-accent: {{accent_color}}"` and correctly passes it; the value arrives at runtime on the customer's site after every gate has run. Winston: a data-plane risk in a system whose entire quality apparatus is a build-time gate. **This architecture has no home for that category of check.**
- (constraint) AD-21's iframe: Ghost data is controlled by the customer **or anyone who has ever held a Contributor/Author/Editor account on that Ghost** — an Author can set a title, excerpt or tag name. So the threat is not self-inflicted; it is a staff-account holder attacking the *owner's Inflozo session*. Same origin means anything executing there reads localStorage (session token), calls Inflozo's API as the owner, and reads the IndexedDB journal. `frame-ancestors 'self'` governs who may embed the frame and nothing about what runs inside it.
- (event) The moment of the session: Amelia pointed out `marks[].href` has no scheme check and **both renderers call `marks.ts`** — so the `javascript:` finding is not merely a visitor problem, it is a same-origin `javascript:` URL in the owner's authenticated session. Ravi stopped and made her repeat it. The iframe risk and the href finding are the same finding.
- (decision) **The unifying frame, Murat's wording, and it is what should reach Umang:** every finding tonight is *a value crossing from an untrusted source into a sink that interprets it, at a place where the only control was escaping.* Not four bugs — one missing idea, four times.
- (decision) Winston: there is **no AD for that idea**. AD-5 is it applied to braces; AD-4 is it applied to marks; both are instances of a rule nobody wrote down. Ravi: "Then write it down. That's your thirty-sixth." → proposed **AD-36**.
- (dynamic) 2026-08-27 step 4b opened: 1,086 findings from the design reconciliation. Ravi took the 66 ghost-infeasible first and collapsed them to nine rulings + a corrections list. Murat's round-4 unifying-frame move repeated: not 66 bugs, four missing ideas — Handlebars has no memory or arithmetic, CSS cannot see content, Ghost's data has fields we invented, and we do not own the markup inside {{content}}.
- (moment) 2026-08-27: Umang overruled Ravi on the subscribe form — 'read docs.ghost.org/themes/members, I think they don't need JS.' Ravi executed instead of arguing: A/B/C/D/E/F against T1 (6.58.0) and T3 (5.130.6). Refuted three independent ways — the endpoint never parses form-encoded bodies (G5 said 'Email is required' WITH a valid token), G6 needs an integrityToken from a separate GET, and there is no redirect back (a native submit lands the visitor on raw JSON). Amelia found the owner: portal.min.js does querySelectorAll('form[data-members-form]') + addEventListener('submit') and adds the loading/success/error classes itself — so Umang's CSS instinct was RIGHT, JS is just what applies the class. Ghost's docs genuinely never say JS is required; that is a docs gap, not a misread. Probe family 2 CLOSED by execution: 41 families become 40.
- (outcome) 2026-08-27 owner overruled twice, both toward less build: (1) A15 Upload CUT — embeds only, so D27's Ambient loop dies with it and FR-K needs no video asset type; (2) D26 REVERSED — gap names stay Tight/Normal/Loose, Appendix C does not move, A11's prompt does. Ravi also closed probe family 31 from Portal's source: getPageFromLinkPath has 'share' in portal@2.69 (X/Facebook/LinkedIn/Threads/Bluesky, fixed order, shadow-DOM iframe = unthemeable) and NOT in portal@2.51 — so on Ghost 5 a Share button opens the sign-in modal. D15 stands unchanged, our own list, both majors.
- (outcome) 2026-08-27 step 4b CLOSED — reconcile-designs-decisions.md written, registered in doc-audit.py, gate PASS. 29 rulings; all 66 ghost-infeasible findings and all five decision-collisions closed. The night's big reversal was Umang's, not the room's: NATIVE SEARCH ONLY. Ravi proved sodo-search renders in an iframe (contentDocument.head/.body, own stylesheet) and its index is id/slug/title/excerpt/url/updated_at/visibility — no post body — so every A23 design that draws results had nothing left to draw. A23 15 designs -> 2-3 openers, D3 reversed, search-overlay deleted from the registry, and FR-G7's zero-third-party-JS rule restored whole. Winston called that the best trade of the night. Also banned render-time design hand-off (closes R2, the largest unowned mechanism) and ruled adjacency a compile-time fact (closes R1) — both follow from one idea: what the editor shows is what the visitor gets, because both come from the same build.
- (callback) Running bit established: Ravi stopped arguing and started pressing the button. Four probe families settled in one evening from the room itself — family 2 (JSON-only, token-gated signup), family 31 (portal 2.69 has a share page, 2.51 does not, so a Share button opens sign-in on every Ghost 5 site), half of family 1 (filter=id:[...] discards the requested order on both majors), and families 8+9 deleted by the search ruling. Murat: 'two closed by pressing the button instead of arguing, I'm going to keep bringing that up.' Sally lost Facet Rail and the word 'Even' in the same session and mentioned both.
- (outcome) 2026-08-27 Tiers 1+2 propagated the same night — the rulings reached MEASUREMENTS §29, VERIFY-AT-BUILD 47-51 (families 2/8/9/31 struck), the spine (AD-37 'the build decides the page, the render never re-decides it' = R-8 + R-19 in one invariant; AD-38 member PII never server-rendered; amendments to AD-3/AD-4/AD-10/AD-36), CONTROL-PROMPTS (D3/D17/D26/D27 marked reversed in place), and research §7 which finally has the edit-safe column FR-D20 had always cited. Gate PASS. Two self-inflicted lessons worth keeping: a regex meant for research §7 leaked into every table in the file (caught by counting 80 rows where 32 were expected, reverted via git, redone against an isolated slice) — and category-prompts.py's 'assert len(names) == 31' broke the moment the registry changed, which is EXACTLY the hardcoded-class-membership failure doc-audit.py's own header records finding twice. Now derived. Murat, inevitably: the gate caught the incomplete propagation, which is the first time it has caught one live rather than in an audit.
- (outcome) 2026-08-27 late: item 47 ran and REFUTED ITS OWN PREMISE — there is no per-template {{#get}} budget. Ghost races each get against 5000ms on both majors; 150 gets on one template resolved in full (1.855s T1). Marginal cost ~10ms/pick. So R-20's provisional cap of twelve had no platform basis and Umang withdrew it: NO CAP, the panel warns past 25, and the warning must say the budget is per PAGE not per section. Also found: Ghost 6 dedups identical gets within a render, Ghost 5 does not (register item 52). Umang also confirmed command-palette's deletion — A23 was its only consumer and the ruling's own lint rule (sodo owns ⌘K) forbade its entire job. Ravi's line about pressing the button held one more time: the probe that was supposed to lower a number deleted the number instead.
