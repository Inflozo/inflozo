// GENERATED by `python3 tools/probe/record-contexts.py` — do not edit by hand.
//
// FR-H7's recordings, reachable from inside a core package: AD-1 bans `node:fs` there, so a static
// import is the only door. Derived from what is on disk, so a recording removed cannot go unasserted
// through a stale list — `contexts.test.ts` refuses by name when one is absent.

import ghost_source from './ghost-source.json' with { type: 'json' }
import ghost5 from './ghost5.json' with { type: 'json' }
import ghost6 from './ghost6.json' with { type: 'json' }

export const CAPTURE_COMMAND = "python3 tools/probe/record-contexts.py"

export const RECORDINGS: Readonly<Record<string, unknown>> = {
  "ghost-source": ghost_source,
  "ghost5": ghost5,
  "ghost6": ghost6,
}
