// GENERATED by `python3 tools/probe/record-cards.py` — do not edit by hand.
//
// The style-guide recordings, reachable from inside a core package: AD-1 bans `node:fs` there, so a
// static import is the only door. Derived from what is on disk, so a recording added or removed
// cannot go unasserted through a stale list.

import ghost5_article from './ghost5/article.json' with { type: 'json' }
import ghost5_capture from './ghost5/capture.json' with { type: 'json' }
import ghost5_page from './ghost5/page.json' with { type: 'json' }
import ghost5_variations from './ghost5/variations.json' with { type: 'json' }
import ghost6_article from './ghost6/article.json' with { type: 'json' }
import ghost6_capture from './ghost6/capture.json' with { type: 'json' }
import ghost6_page from './ghost6/page.json' with { type: 'json' }
import ghost6_variations from './ghost6/variations.json' with { type: 'json' }

export const CAPTURE_COMMAND = "python3 tools/probe/record-cards.py"

export const RECORDINGS: Readonly<Record<string, Readonly<Record<string, unknown>>>> = {
  ghost5: { "article": ghost5_article, "capture": ghost5_capture, "page": ghost5_page, "variations": ghost5_variations },
  ghost6: { "article": ghost6_article, "capture": ghost6_capture, "page": ghost6_page, "variations": ghost6_variations },
}
